# Где всё стоит

Обновлено 2026-08-29. Короткая сводка: что собрано, что не доделано, чего не
делать. Подробности - в `NEXT.md` (рабочий журнал, по разделам), `DEAD_ENDS.md`
(что пробовали и почему отвергли), `PLAN.md` (исходный план, на него ссылаются
комментарии в коде), `PROGRESS.md` (таблица по итерациям).

## Сборка

`python3 -m cgr.build` собирает всё и сам себя проверяет. Сейчас зелено:

- `alpha/sat: within tolerance` - 0 предупреждений на 58 кадрах
- `quality: clean, 16 known debt`

Гейт отдельно: `python tools/analyze.py --check data/metrics-baseline.json --jobs 4`.
`bad` - стало хуже базовой линии, останавливает сборку. `debt` - не дотягивает
до цели, но не хуже прежнего. `unmeasured` - прибор ничего не разрешил; это
состояние прибора, а не рендера, и оно намеренно видно.

## Складку меряют заново, 2026-08-22

Старый трекер (`_fold_track`) искал тёмную линию: самый тёмный пиксель строки с
проверкой на prominence. Автор такой линии не рисовал. Его сечение - переход
между двумя уровнями граней с неглубокой зарубкой на переходе, и старым
прибором его собственные кадры читаются как `unmeasured` на всех размерах и всех
клиньях. Значит цели `fold_gap`/`wander`/`jag`/`luma_step` были догадками о
форме, эталона у которой не было.

Новый прибор - `tools/foldfit.py`: сечение по нормали к хорде, две грани
отдельной робастной прямой, детренд, потом ступень `tanh` по сетке. Тот же код
у `tools/fold_tracker.py` (человеку) и у `analyze` (гейту). Приёмка теперь -
`fold_cover`, `fold_unres`, `fold_s_*`, `fold_curv`, `fold_step`, `fold_notch`,
`fold_jitter` и отдельным классом `inner_tip`. Все семь калиброваны по автору:
у него `s = 0.60` LU на 128/256/512, `unres = 0`, зарубка 4-5 уровней.

Старые числа остались как `legacy_fold_*` и `legacy_inner_jitter`: их меряют,
печатают и пишут в baseline, но PASS/FAIL они больше не решают.

Прибор смотрит на продукт, а не на промежуточное представление (NEXT.md 50). У
Hand, Wait и AppStarting отгружаются 27 кадров, зажжённых с одного канонического
рендера, а `frame_image` по девяти авторским индексам не отгружается нигде -
именно его контракт складки и мерил до 2026-08-22. Один вход `product_cycle`
отдаёт кадры вместе с фазами, эталон на дробной фазе - `author_at`, четыре
гармоники по девяти авторским отсчётам.

## Складка перерисована, 2026-08-22

`_fold_restep` принят (NEXT.md 51). У шести клиньев переход складки теперь 0.60
логической единицы - авторская величина - вместо 0.02..0.05, то есть вместо шага
аппаратного пикселя. Поправка равна нулю вне 1.60 LU от перехода и ближе 1.50 LU
к контуру, где живёт внутреннее остриё; это контракт, а не пожелание, и его
меряет `test_restep_support`.

Долг после приёмки 32 -> 16. `inner_tip` не сдвинулся ни у одного курсора. Оба
`fold_notch`, которые новый прибор нашёл долгом (AppStarting 0.355, Wait 0.396),
закрыты: 0.448 и 0.467.

Три контроля старого dark-line трекера теперь пропускаются, а не проходят:
портить нечего, рендер тёмной линии не рисует. `legacy_fold_*` по-прежнему
меряются и печатаются.

## Долг, 12 позиций

| что | где |
|---|---|
| `fold_unres` > 0.10 | NO 0.70, Wait 0.25, Help 0.19, Handwriting 0.17, AppStarting 0.15 |
| `fold_s_thin` < 0.50 от авторской ширины | NO 0.02, AppStarting 0.35, Wait 0.43 |
| `fold_s_conv` > 1.6 между 128 и 512 | NO 20.0 |
| `fold_curv` выше авторского вдвое | Handwriting 2.00 при 0.90 |
| `fold_step` < 0.45 от авторской ступени | NO 0.44 |
| `delta_e` выше 5.0 | NO[5] 5.54 |

Было 16, стало 12 - 2026-08-30 `_fold_restep` распространён на Help и
Handwriting (NEXT.md 61). Они несут в силуэте тот же указатель, что Arrow, их
хорды сходятся с его в пределах 0.25 логической единицы, и складка у них была
той же самой - просто стадия их не охватывала. Ушли четыре позиции: `fold_s_thin`
и `fold_s_conv` у обоих (0.033 -> 1.00 и 5.5 -> 1.00 у Help, 0.022 -> 0.625 и
7.5 -> 1.60 у Handwriting). `fold_unres` у обоих резко улучшился (0.57 -> 0.19,
0.75 -> 0.17), но порога 0.10 не достиг и остался долгом.

Охват сделан отдельной константой `_FOLD_RESTEP_ON`, а не расширением
`_WEDGE_TIPS`: тот же набор гейтит ещё `_notch_declutter`, `_temper` и
`_match_author_level`, и расширение включило бы четыре стадии там, где мерена
одна.

**NO закрыт как реальная регрессия, 2026-08-30 (NEXT.md 62).** Проверено
двумя независимыми способами по протоколу владельца: синтетический профиль
постоянной амплитуды на ширинах 0.02/0.15/0.60 через сам `foldfit.measure`
прибор не обвиняет (расхождение 0.4%, в другую сторону); прямой замер
facet-уровней на реальных кадрах NO, минуя поиск ширины, падает так же, как
и `fold_step`. Причина геометрическая: у NO дальний конец хорды складки
лежит вплотную к кругу запрещающего знака, и касательная грани на кадрах
2-3 частично строится по пикселям круга, а не клина - `_RESTEP_PROTECT`
защищает от контура силуэта, но не от соседней фигуры внутри того же
кадра. Не артефакт прибора и не повод для исключения: порог `0.45` и
`metrics-known-issues.json` не менялись, NO остаётся вне
`_FOLD_RESTEP_ON`.

Wait и AppStarting получили 0.40 вместо 0.60: остаток локализован и принят,
чинить его тем же заходом не стали.

Остаток по NO - та же вещь, названная тремя числами: складка отрисована как
разрыв, и её ширина следует шагу аппаратного пикселя вместо того, чтобы стоять в
логических единицах.

Ушло 2026-08-21: `SizeNS tip_contrast` (0.039 -> 0.045 при цели 0.042) - его
закрыл подъём стекла на тонких элементах, а не правка бевела, ради которой он
числился в маршруте 2 (NEXT.md 40).

`tip_contrast` переименован в `tip_extreme_contrast` (NEXT.md 41): формула та
же, имя - по тому, что она на самом деле берёт, экстремум по одному пикселю
диска. Рядом появился `tip_profile`, кольцевое сравнение с автором; он в
таблице и в ратчете, но не в гейте.

Ушло 2026-08-21: `UpArrow tip_extreme_contrast` (0.077 -> 0.155 при 0.085).
Закрыл `_tip_level` - остриё четырёх статических клиньев отгружалось на 18-30
уровней светлее авторского и на сером фоне растворялось. Поправка намеренно
одним числом на полосу: точные варианты выпрямляют профиль и ломают складку,
см. NEXT.md 45.

`unmeasured` пуст с 2026-08-22. Обе позиции AppStarting - складка и дрожание -
были состоянием старого прибора, а не рендера: новый читает у него 20 станций из
24 на всех трёх размерах и все 27 кадров цикла при полном покрытии пар.

Локализовано и принято 2026-08-21: талия IBeam (NEXT.md 42). Недобор массы там
геометрический - четыре строки, где обводка на 0.15-0.22 единицы уже авторского
покрытия, при недоборе к перебору 8:1. Уровень стекла у IBeam уже выше всех в
семье, со стороны рендера брать нечего, `traced.json` под запретом. Заодно
поправка к пункту 40: масса IBeam 0.962, а не 0.928 - у него 480 из 605
пикселей эталона имеют alpha <= 2, и старая цифра мерилась против этой дымки.

## После v1.1.0: две ветки, не четыре долга

Решение владельца 2026-08-21, по итогам лестницы H/J (NEXT.md 45). Складка и
остаточный профиль яркости у остриёв - **одна задача, а не две**: обе живут
вдоль хорды и читают одни и те же пиксели, поэтому две отдельные стадии за них
драться будут. Владелец у них один - `_tip_relight`, у которого уже есть модель
жёлоба вдоль хорды.

- **`fold-relight`** - начинать с `_tip_relight`, свести туда форму складки и
  продольный уровень света. Цель сразу двойная: `fold_gap`/`wander`/`jag` и
  остаток +2.7..+8.1 уровня у клиньев, без отдельной правки острия.
- **`no-ring`** - только кольцо NO, локально по альфе, контуру и цвету. С
  моделью клиньев не пересекается, может идти параллельно.

Поправка 2026-08-22: ветка `fold-tracker` закрыта, и цели `fold-relight`
переписаны ею целиком. `fold_gap`/`wander`/`jag` больше ничего не решают, форма
складки у шести клиньев уже авторская (NEXT.md 51), а `_fold_restep` владеет
поперечным сечением. У `fold-relight` остаётся продольная половина - уровень
света вдоль хорды - и она не блокирует ничего. Следующий независимый долг -
`no-ring`, `NO[7] delta_e 5.972`. Закрыт: см. раздел про знак запрета. После
переснятия baseline 2026-08-29 остаток гейта по этому курсору стоит на другом
кадре - `NO[5] delta_e 5.54`, к этому разделу отношения не имеет (см. ниже).

`_tip_level` в этом релизе - сознательно временное и консервативное решение:
одно число на полосу, складку не трогает, закрывает видимый дефект. Если
`_tip_relight` научится владеть уровнем, `_tip_level` **удаляется**, а не
обрастает вторым механизмом поверх.

## Живые маршруты, по убыванию отдачи

**1. Складка и полоса на кромке - маршрут закрыт, 2026-08-29.** Была самой
крупной нерешённой задачей. Перенос формы радиального профиля
(`corr(u) = (b(u) - b(e)) - (m(u) - m(e))`, `_rim_transfer` в `hybrid.py`)
работает **только в откалиброванном месте** - в конце девятистадийного
пайплайна, где стоит сегодня, включён на четырёх (`_RIM_XFER = {"Arrow",
"Help", "NO", "AppStarting"}`), гейт на них зелёный.

Обе ветки постобработки построены и отвергнуты числом и глазом: (а) чинить
складку тем же переносом вдоль излома (`_fold_transfer`/`_FOLD_XFER`, код в
дереве, выключен) - общий потолок не примиряет UpArrow с Arrow/Hand; (б) тот
же перенос на выходе `_master_raw`, до пайплайна - `rim_layers` на мастере
хуже почти везде, включая все четыре живых курсора. Разбор и цифры - раздел
"Складка и полоса на кромке" в `DEAD_ENDS.md`, дословный - NEXT.md 25 и 58.

Единственное не закрытое - не постобработка `art/ai512`, а сам апскейл:
другая подготовка/маска входа для Real-ESRGAN, чтобы сеть не рисовала
двойной слой на кромке с самого начала. **Отдельный будущий эпик** (веса,
`tools/upscale512.py`, пересчёт всех 58 мастеров), не ветка этой задачи -
без отдельного решения владельца не начинается.

**2. Остриё - SizeAll проверен и закрыт без результата, 2026-08-30.**
По гейту открытых позиций не осталось: UpArrow и SizeNS оба ушли 2026-08-21
(см. выше). Кольцевой `tip_profile` (доля авторского контраста на самом
слабом кольце самого слабого угла, 1.0 - на уровне автора) на живом дереве
сейчас:

| курсор | `tip_profile` | | курсор | `tip_profile` |
|---|---|---|---|---|
| SizeAll | **0.693** | | Handwriting | 1.283 |
| AppStarting | 0.835 | | NO | 1.289 |
| SizeNWSE | 0.895 | | SizeNESW | 1.053 |
| UpArrow | 0.927 | | Cross | 1.046 |
| SizeWE | 0.990 | | Wait | 1.047 |
| SizeNS | 0.968 | | Help | 1.040 |
| Handwriting | 1.283 | | IBeam | 1.033 |
| Arrow_Down | 1.656 | | Arrow | 1.709 |
| Hand | 1.709 | | | |

Старые числа этого раздела (UpArrow 0.42, Arrow 0.50) относились к дереву до
`_tip_level` (2026-08-21) и с тех пор не сверялись - `tip_profile` эту правку
не отслеживал, хотя `tip_extreme_contrast` рядом обновлялся. Живая картина
другая: Arrow и Hand выше авторского контраста вдвое, большинство курсоров на
уровне автора или чуть ниже. Ниже 0.9 держится по существу один курсор -
**SizeAll (0.693)** - плюс три мягких отстающих (AppStarting 0.835, SizeNWSE
0.895, UpArrow 0.927). Широкой задачи "остриё" на нескольких клиньях сразу
больше нет; открыт вопрос почти только по SizeAll.

Расхождение не в альфе - наша выше авторской - а в светимости: у автора
остриё закрыто тёмной шапкой в 2-3 пикселя, у нас цвет доходит до самой точки
при волосяном контуре. Диагноз проверен на SizeAll отдельно, 2026-08-30 -
не подтвердился как решаемый постпроцессом: два кандидата на заполнение
поперечника у нижнего угла (радиальный и по сечению вдоль медианы, разбор
целиком в `DEAD_ENDS.md`) подняли `tip_profile` до 0.819 и 0.756 - лучше
базы, ниже приёмки 0.9. Маршрут для SizeAll закрыт без третьего варианта;
0.693 остаётся живым числом.

Два названных в старом плане рычага отпали, проверено 2026-08-20: `_band_level`
работает только на `INTERP` (AppStarting, Hand, Wait), а обе прежние позиции
долга - статические курсоры на скалярной ветке; у UpArrow перебор доноров и
радиусов острия снят целиком (NEXT.md 36), Arrow_Down 2.0/3.5 это максимум,
который не ломает сходимость.

**3. Симметрия и освещение SizeNS/SizeWE.** `trace.SYMMETRY` правит Cross,
SizeAll и IBeam; `analyze.SYMMETRY` меряет ещё эти два - намеренно, чтобы долг
был виден. Метрика ушла, видимая асимметрия осталась, и она фотометрическая.

Локализовано и доказано 2026-08-21, разбор целиком в NEXT.md 46: авторская
основа на обоих концах SizeNS одна и та же, всю разницу делает
`_BEVEL_LIGHT = (-0.6, -0.8, 0.55)` - нижнее остриё отвёрнуто от света, разброс
22 уровня там, где у автора асимметрии нет. Обвод ни при чём: зазор покрытия
оседает по лестнице размеров к нулю, то есть это антиалиасинг вершины, не форма.

Названный адрес - затухание бевела у остриёв - **проверен и закрыт как тупик,
2026-08-30.** Fade действительно почти снимает перекос (SizeNS 21.2 -> 2.4), но
делает это не тем способом: он затемняет и правильное остриё тоже, 140.2 ->
120.8 при авторских ~151, то есть выравнивает две стороны, опуская обе ниже
автора вместо того, чтобы поднять отставшую. Расплата по метрикам: `tip_profile`
SizeNS 0.97 -> 0.69, SizeAll 0.69 -> 0.50, `tip_extreme_contrast` SizeWE 0.106 ->
0.087. Экспериментальный код удалён, рендер совпадает с `4053dba`, гейт зелёный
(bad 0, прежние 16 долгов). Разбор в `DEAD_ENDS.md`.

Симметрия SizeNS/SizeWE остаётся открытым фотометрическим долгом без известного
адреса со стороны рендера. Силуэт при этом по-прежнему не трогать: усреднение
контура сдвигает остриё на 0.2 логической единицы, альфа и угол острия не
двигаются вовсе, уходит только яркость. `SYM_PULL` есть и на 0.5 обе оси SizeNS
садятся на авторскую асимметрию - но включать его нечем, пока затенение острия
не научится следовать за силуэтом, а способ научить как раз и не найден.

**4. Мелочи.** `delta_e` NO[7] сейчас 5.97 при базовой 6.56 и цели 5.0 -
недостача маски на запрещающем кольце, правка живёт в `trace.py` +
`traced.json` одним коммитом, не в рендере (NEXT.md 28.1). У Help и SizeNESW
просел IoU к автору на 0.014 при лучшем цвете - посмотреть, что сдвинулось в
силуэте. Наружная полуединица серого семейства была около 0.85 авторской
плотности - закрыто 2026-08-21 (`_thin_lift_glass`, NEXT.md 40), и правило 7
сработало буквально: попиксельная правка в манере `thin`, не ещё один якорь.
Масса Cross 0.954 -> 0.992, SizeWE 0.969 -> 0.999; по IBeam см. пункт про талию
выше - там 0.962, а 0.928 мерилось против авторской дымки.

`Handwriting delta_e` 3.55 -> 4.68 - объяснено и закрыто 2026-08-21, разбор в
NEXT.md 47. Старая база на кадрах 3-5 мерила авторский кросс-диссолв,
растянутый вверх и усреднённый обратно к его же 32 пикселям; низкое значение
было свойством ресемплера, а не рендера, и возвращать 3.55 как цель нельзя.
Текущий рост яркостный: `dL` 5.50 при `dC` 0.10 и `dH` 0.05, причём цветность
material улучшает. Остаток - кадр 4 держится примерно на +2.13 уровня L светлее
автора.

Уровень при переносе материала - **закрыто 2026-08-21**, NEXT.md 48. Перенос
нарушал собственный контракт: `_gauss` затягивал фон из-за силуэта, остаток
выходил положительным по всей кромке, и кадр 4 отгружался на 2 L светлее. Чинят
две вещи вместе - размытие через `_mblur` (он лежал в файле ровно для этого, а
перенос его не звал) и явная проекция DC после keepout, промодулированная
`keep`. `delta_e` 4.682 -> 3.895, складка не сдвинулась ни на знак, деталь цела.
Контракт держит `test_material_dc` в selftest.

Впуск Handwriting в `_match_author_level` **не нужен** и не сделан: он лечил бы
последствие второй стадией. Если к нему когда-нибудь вернутся - цена записана в
`4789ef7` (`fold_wander` 0.225 -> 0.287), мерена до переноса материала.

Базовая линия переснята 2026-08-21, отдельным коммитом. Прежние 3.553 у
Handwriting **не переносятся как цель**: они принадлежали подмене авторским
кадром через ресемплер, а не рендеру. Заодно в снимок вошёл дрейф шести
коммитов, накопленный с `dd46b61`: `density` у всех вырос с 0.109 до 0.205
процента при пороге 2.0 (подъём стекла на тонких), `tip` на 64 упал с 3.2 до 0
у четырёх клиньев - складка стала доходить до острия, - а `delta_e` улучшился у
NO 6.56 -> 5.97, IBeam 4.27 -> 3.95, SizeAll и SizeNESW. Ни одного ухудшения
через порог; предупреждение о самой цифре лежит в `metrics-known-issues.json`,
раздел `notes`.

## Знак запрета у NO перерисован, 2026-08-22

Ветка `no-ring`. Автор рисовал знак шаблоном, и это доказано его же альфой:
центр (15.93, 16.02) стоит на месте во всех четырёх кадрах с точностью до
тысячной, некруглость 0.042, штрих - один плоский цвет без поперечного профиля
и без углового члена. `_no_ring` перерисовывает кольцо покрытием настоящего
кольца плюс этот цвет.

Альфа и цвет включаются вместе и раздельной конфигурации не имеют - см.
`DEAD_ENDS.md`, раздел про насыщенность: пересвеченный красный и недобор
покрытия гасили друг друга, и каждая половина в одиночку делала хуже.

NO[7]: `delta_e` 5.972 -> 4.626, край 8.867 -> 4.299, альфа rms 47.4 -> 20.9,
цвет 1.274 при плоской константе. Обводка 1.872 против авторских 1.853 по
одному прибору. Кадры 8, 9, 10 идут тем же порядком.

Стадия владеет только знаком: кадры 0-6, где круга ещё нет, проходят побитово,
и всё дальше `R + margin + fade` от центра тоже. На это есть selftest.

Переход 5->6, который выглядел вторым долгом, оказался дефектом прибора и закрыт
вместе с ним (NEXT.md 54). Перечёркивание тоже закрыто замером: наклон у нас
авторский с точностью до четверти градуса, а «42.75 против 45.00» было
артефактом старого способа подгонки (NEXT.md 55). Остался указатель, видимый
сквозь дыру, - 9.5..10.1 против 3.2..4.0 у самого знака.

Baseline переснят 2026-08-29 (два новых замера), и зона дыры получила
собственный храповик без порога - `no_hole_alpha_err` (старт 25.30) и
`no_hole_delta_e` (старт 8.855, NEXT.md перед разделом 57): до этого
`delta_e` у NO мерился только по кадру 5, а дыры на кадре 5 ещё нет, так что
указатель сквозь дыру гейту не был виден вовсе. Текущий остаток в таблице
долга, `NO[5] delta_e 5.54`, - это цвет и уровень самого знака запрета, кадр
рисуется раньше появления дыры; с указателем в дыре не путать, у него теперь
отдельная защита.

## Чего не делать

- Не строить коррекцию уровня альфы по глубине от кромки: проверено на всех 58
  кадрах, знак остатка расходится по семействам (+0.034 у клина против -0.121 у
  NO), а зона NO не описывается и собственной кривой курсора (NEXT.md 57).
- Не размывать. Резкость каждый раз терялась ровно там, где её несёт растр.
- Не подкручивать пороги и не заносить числовые регрессии в
  `metrics-known-issues.json` - это дыра в гейте. Туда только претензии без цифр.
- Не возвращаться к разделению полосы и складки по когерентности вдоль дуги:
  закрыто двумя независимыми методами, когерентной полосы нет.
- Не трогать тёмную кромку Wait и AppStarting - это авторская черта, его
  собственный замер даёт 0.81 и 0.65 против 0.03-0.16 у остальных четырнадцати.
- Не включать `_smooth_along_fold` - решение принято, оставлен выключенным.
- Не искать восьмой рычаг для острия UpArrow со стороны рендера: семь провалены
  с числами, а решение оказалось переносом затенения Arrow_Down (NEXT.md 36).
- Не чинить наклон красного острия со стороны рендера: разобрано как дефект
  мастера, то есть апскейлера (NEXT.md 1, 2, 8). Адрес - переупскейл или ручная
  правка мастера.

## Как здесь работают

- Репозиторий на SMB. `git stash` запрещён - работа уже терялась. Снимки делать
  копией файлов в сторону. Git-конфиг не менять, флаги передавать команде:
  `-c safe.directory='*'`, `-c core.fileMode=false`.
- `git fsck` перед push.
- `trace.py` и `traced.json` - всегда одним коммитом.
- Проверять воспроизведением, а не сравнением. Сначала рендер и глаза, метрики
  вторым номером.
- `DEAD_ENDS.md` читать до новой попытки.
- Не судить временную морфологию по `morph_peak`: это отношение, оно краснеет,
  когда рендер перестаёт двигаться там, где автор стоит. И не мерить шаги морфа
  маской по доле собственного пика альфы - выборки у автора и у нас выходят
  разного размера, на третьем шаге NO у него в маске один пиксель. Решают
  `morph_cadence_err` и `morph_peak_abs`, считаемые на композите поверх трёх
  фонов по всей сетке 32x32 (NEXT.md 54).
