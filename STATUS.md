# Где всё стоит

Обновлено 2026-08-21. Короткая сводка: что собрано, что не доделано, чего не
делать. Подробности - в `NEXT.md` (рабочий журнал, по разделам), `DEAD_ENDS.md`
(что пробовали и почему отвергли), `PLAN.md` (исходный план, на него ссылаются
комментарии в коде), `PROGRESS.md` (таблица по итерациям).

## Сборка

`python build.py` собирает всё и сам себя проверяет. Сейчас зелено:

- `alpha/sat: within tolerance` - 0 предупреждений на 58 кадрах
- `quality: clean, 10 known debt, 2 unmeasured`

Гейт отдельно: `python tools/analyze.py --check metrics-baseline.json --jobs 4`.
`bad` - стало хуже базовой линии, останавливает сборку. `debt` - не дотягивает
до цели, но не хуже прежнего. `unmeasured` - прибор ничего не разрешил; это
состояние прибора, а не рендера, и оно намеренно видно.

## Долг, 10 позиций

| что | где |
|---|---|
| `fold_gap` > 0 | Hand 2.50, NO 1.25, Wait 0.875, Handwriting 0.50, Arrow 0.25, Arrow_Down 0.25, SizeAll 0.25, UpArrow 0.125 |
| `tip_extreme_contrast` ниже цели | UpArrow 0.077 при 0.085 |
| `delta_e` выше 5.0 | NO[7] 5.97 |

Ушло 2026-08-21: `SizeNS tip_contrast` (0.039 -> 0.045 при цели 0.042) - его
закрыл подъём стекла на тонких элементах, а не правка бевела, ради которой он
числился в маршруте 2 (NEXT.md 40).

`tip_contrast` переименован в `tip_extreme_contrast` (NEXT.md 41): формула та
же, имя - по тому, что она на самом деле берёт, экстремум по одному пикселю
диска. Рядом появился `tip_profile`, кольцевое сравнение с автором; он в
таблице и в ратчете, но не в гейте.

`unmeasured` (2): у AppStarting не читаются ни `inner_jitter`, ни складка - её
впадина глубиной 58 уровней даёт трекеру 5 строк из 257 (NEXT.md 37).

Локализовано и принято 2026-08-21: талия IBeam (NEXT.md 42). Недобор массы там
геометрический - четыре строки, где обводка на 0.15-0.22 единицы уже авторского
покрытия, при недоборе к перебору 8:1. Уровень стекла у IBeam уже выше всех в
семье, со стороны рендера брать нечего, `traced.json` под запретом. Заодно
поправка к пункту 40: масса IBeam 0.962, а не 0.928 - у него 480 из 605
пикселей эталона имеют alpha <= 2, и старая цифра мерилась против этой дымки.

## Живые маршруты, по убыванию отдачи

**1. Складка и полоса на кромке.** Самая крупная нерешённая задача и главная
претензия владельца. Доказано: переносить надо не заливку, а форму радиального
профиля, `corr(u) = (b(u) - b(e)) - (m(u) - m(e))`. Без ограничений это даёт
Arrow 76% -> 61%, Hand 67% -> 50%, Help 45% -> 26% при авторских 5%, и тёмная
полоса вдоль верхнего ребра Arrow исчезает. Упёрлось в то, что поправка ломает
складку (UpArrow `fold_luma_step` 1.0 -> 5.7): на тонком клине полоса кромки и
окрестность складки - одни и те же пиксели. Две неопробованные ветки, в порядке
предпочтения: (а) чинить складку тем же переносом, построив эталон профиля
вдоль излома, чтобы поправке нечего было ломать; (б) пустить перенос выше по
цепочке, на `src/ai512`, откуда дефект и родом - провал несут 97% станций уже в
мастере. Код не в дереве, воспроизводится по описанию из NEXT.md за час.

**2. Остриё.** Открытая позиция одна - UpArrow 0.077 при 0.085 по
`tip_extreme_contrast`; SizeNS ушёл (см. выше). Аудит определения 2026-08-21
(NEXT.md 41) переставил задачу: экстремум по одному пикселю ничего не говорит о
форме острия, а кольцевой `tip_profile` даёт UpArrow 0.42, Arrow 0.50, SizeAll
0.69 от авторского. Расхождение не в альфе - наша выше авторской - а в
светимости: у автора остриё закрыто тёмной шапкой в 2-3 пикселя, у нас цвет
доходит до самой точки при волосяном контуре. Формулировка задачи, которую
стоит проверять следующей: на острие контур должен съедать поперечник целиком,
там где фигура тоньше удвоенной ширины кромки. Ни один из семи прежних рычагов
этого не пробовал.

Историческая часть пункта, оставлена как список тупиков. Оба названных в старом
плане рычага отпали, проверено 2026-08-20:
`_band_level` работает только на `INTERP` (AppStarting, Hand, Wait), а обе
позиции долга - статические курсоры на скалярной ветке; у UpArrow перебор
доноров и радиусов острия снят целиком (NEXT.md 36), Arrow_Down 2.0/3.5 это
максимум, который не ломает сходимость.

Живое здесь одно - SizeNS, и оно локализовано заново. Связывает метрику нижний
конец стержня (0.0391) при верхнем 0.0615, у одного и того же курсора с одним
мастером. На 256, вдоль оси острия, у нижнего конца два дефекта сразу, и оба
асимметричны его же верхнему:

**Тёмное ядро под нижним остриём - это аналитический бевел, не мастер.**
Яркость в логической единице от вершины: низ 111.6-117.4, верх 139.0-142.3, у
автора на обоих около 151, и разница не зависит от размера (проверено на 96,
128, 192, 256, 384, 512). Источник назван точно: SizeNS входит в `_SYNTH_BEVEL`,
то есть его цвет это авторский кадр плюс `_bevel_shading`, а AI-мастер в цвете
не участвует вовсе. Сложение сходится до десятой:

| u | автор | бевел низ | кадр | бевел верх | кадр |
|---|---|---|---|---|---|
| 0.5 | 135.8 | -14.7 | 121.1 | +2.9 | 138.5 |
| 1.0 | 131.6 | -16.3 | 115.3 | +5.7 | 140.9 |
| 2.5 | 141.1 | -8.2 | 132.9 | +14.2 | 161.4 |

Авторская основа на обоих концах одна и та же. Всю разницу делает
`_BEVEL_LIGHT = (-0.6, -0.8, 0.55)`: верхнее остриё повёрнуто к свету, нижнее
отвёрнуто, разброс 22 уровня там, где у автора асимметрии нет. Модель работает
как задумана, вопрос только в том, что у самого острия она перебирает.

Адрес - затухание бевела у остриёв. Правка трогает все семь курсоров
`_SYNTH_BEVEL`, поэтому это решение владельца, а не разработчика.

**Старая запись «ядро живёт в AI-мастере» (PLAN_NEXT, NEXT.md 15) неверна** для
SizeNS и снята 2026-08-21.

**Обвод тут ни при чём, проверено.** Сперва показалось, что нижний обвод короче:
на 256 половину покрытия он набирает к 0.154 логической единицы против 0.053
наверху. Но по лестнице этот зазор оседает - 0.172 на 96, 0.158 на 128 и 192,
0.102 на 256, 0.053 на 384, 0.063 на 512, и обе вершины сходятся к нулю. Разница
формы в логических единицах так себя не ведёт; так себя ведёт антиалиасинг
вершины на сетке. Вершины стоят на 31.75 и 0.34 с полууглами 24.7° и 23.5° -
одна и та же форма. Правка в `trace.py` здесь не нужна.

Адрес, значит, один: затенение выше по цепочке или локальный перезасвет. Это же
объясняет, почему усреднение контура (маршрут 3) роняет метрику - оно двигает
вершину, не трогая ядро.

**3. Симметрия SizeNS и SizeWE.** `trace.SYMMETRY` правит Cross, SizeAll и
IBeam; `analyze.SYMMETRY` меряет ещё эти два - намеренно, чтобы долг был виден.
Усреднение контура сдвигает остриё на 0.2 логической единицы и роняет
`tip_contrast` SizeNS. Разложено: альфа, ширины клина и угол острия не двигаются
вовсе, уходит только яркость - тёмное ядро лежит в мастере и к силуэту не
привязано. Значит задача не в векторе: если затенение острия научится следовать
за силуэтом, симметрия включается одной строкой, `SYM_PULL` уже есть и на 0.5
обе оси SizeNS садятся на авторскую асимметрию. До тех пор не трогать.

**4. Мелочи.** `delta_e` NO[7] 6.56 - недостача маски на запрещающем кольце,
правка живёт в `trace.py` + `traced.json` одним коммитом, не в рендере
(NEXT.md 28.1). У Help и SizeNESW просел IoU к автору на 0.014 при лучшем
цвете - посмотреть, что сдвинулось в силуэте. Наружная полуединица серого
семейства была около 0.85 авторской плотности - закрыто 2026-08-21
(`_thin_lift_glass`, NEXT.md 40), и правило 7 сработало буквально: попиксельная
правка в манере `thin`, не ещё один якорь. Масса IBeam 0.901 -> 0.928, Cross
0.954 -> 0.992, SizeWE 0.969 -> 0.999. У IBeam остаток семь процентов, и он не
в стекле, а в стержне: площадь 0.913 при уровне 0.988, то есть адрес - контур.

## Чего не делать

- Не размывать. Резкость каждый раз терялась ровно там, где её несёт растр.
- Не подкручивать пороги и не заносить числовые регрессии в
  `metrics-known-issues.json` - это дыра в гейте. Туда только претензии без цифр.
- Не возвращаться к разделению полосы и складки по когерентности вдоль дуги:
  закрыто двумя независимыми методами, когерентной полосы нет.
- Не трогать тёмную кромку Wait и AppStarting - это авторская черта, его
  собственный замер даёт 0.81 и 0.65 против 0.03-0.16 у остальных четырнадцати.
- Не включать `_smooth_along_fold` - решение принято, оставлен выключенным.
- Не искать восьмой рычаг для острия UpArrow со стороны рендера: семь провалены
  с числами, а решение оказалось переносом затенения Arrow_Down (NEXT.md 36).
- Не чинить наклон красного острия со стороны рендера: разобрано как дефект
  мастера, то есть апскейлера (NEXT.md 1, 2, 8). Адрес - переупскейл или ручная
  правка мастера.

## Как здесь работают

- Репозиторий на SMB. `git stash` запрещён - работа уже терялась. Снимки делать
  копией файлов в сторону. Git-конфиг не менять, флаги передавать команде:
  `-c safe.directory='*'`, `-c core.fileMode=false`.
- `git fsck` перед push.
- `trace.py` и `traced.json` - всегда одним коммитом.
- Проверять воспроизведением, а не сравнением. Сначала рендер и глаза, метрики
  вторым номером.
- `DEAD_ENDS.md` читать до новой попытки.
