# Что осталось починить

Составлено 2026-08-08 по итогам сессии. Рендер откачен к предрелизной версии
(`f1769ec`), которую владелец оценил как «почти идеально». Ниже - живые
претензии к этой версии, а не к откаченным экспериментам.

---

## 1. Красный кончик уползает вправо

**Что видно.** На Wait и UpArrow остриё выглядит наклонённым вправо, линия от
верхней вершины уходит в ту же сторону.

**Что измерено.**

| замер | наш рендер | автор (32px) |
|---|---|---|
| силуэт, центроида строк y=3..9 | совпадает | ±0.12 единицы |
| светлое ядро, y=4 | 4.99 | 4.70 |
| яркость вершины, y=3 | 71 | 14 |
| граница освещённого листа, y=8..13 (доля ширины строки) | 0.97 / 0.99 / 0.86 / 0.72 / 0.66 | 0.20 / 0.17 / 0.24 / 0.25 / 0.25 |

Силуэт не смещён, трассировка ни при чём. Дефект в цвете мастера: освещённый
лист прижат к верхней кромке у острия и раскрывается только ниже. Этот наклон
глаз и читает как уползание.

Добавлено 2026-08-08: складка у UpArrow и AppStarting не просто смещена, она
почти стёрта. Глубина провала относительно кремового листа на 256 (медиана по
строкам трека, в уровнях яркости): Arrow 18.0, Hand 18.8, UpArrow 8.0,
AppStarting 5.0. Строк мельче шести уровней: у Arrow и Hand ни одной, у UpArrow
7 из 40, у AppStarting 31 из 42. То есть на этих двух курсорах апскейл не сдвинул
складку, а размазал её в плоскую заливку с бликом. Тот же корень, что в пункте 3.

**Пересмотрено 2026-08-09 - диагноз выше неверен для UpArrow и AppStarting.**
Разобрали сырые пиксели родного 32px оригинала (`hybrid.original`) вдоль хорды
`_fold_chord`, билинейно, на t=0.05..0.75 от вершины. У Arrow и Hand в этой зоне
чёткий неглубокий жёлоб (скат-плоское дно ~148-151-скат). У Arrow_Down - глубокий
острый V-образный залом (до 17-35), не плоское дно. **У UpArrow, Wait и
AppStarting в этой зоне складки нет вообще ни на одной из проверенных точек**
- один гладкий пик без всякой бимодальности, от края до края. Значит "апскейл
размазал складку" для этих трёх - не подтверждается: у автора её здесь и не
было, размазывать было нечего. Прежняя формулировка ("апскейл не сдвинул
складку, а размазал её в плоскую заливку") отражает разницу с формой Arrow/Hand,
а не с собственным оригиналом этих трёх курсоров.

**Зона у выемки хвоста (t=0.80..0.95) проверена отдельно - там иначе.**
У всех шести, включая UpArrow/Wait/AppStarting, складка здесь реальна:

| курсор | у вершины (t<0.3) | у выемки (t>0.8) |
|---|---|---|
| Arrow, Hand | плоский жёлоб ~148-151 | тот же плоский жёлоб, без изменений |
| Arrow_Down | глубокий V (17-35) | тот же глубокий V, местами глубже (до 21) |
| UpArrow, Wait, AppStarting | складки нет (один гладкий пик) | складка нарастает к выемке: UpArrow до ~123-129 при плато ~250, Wait/AppStarting до ~42-57 при плато ~250-254 |

Значит у UpArrow/Wait/AppStarting складка не отсутствует у автора вообще - она
**нарастает от нуля у вершины до полной глубины у выемки**, другая форма вдоль
хорды, чем у Arrow/Hand (где жёлоб примерно постоянной глубины от самой
вершины). Прежний диагноз "апскейл стёр складку" не был полностью неверен -
относился к зоне у выемки, где глубина реальная и существенная, а не к зоне
у вершины, где её нет и не должно быть.

**Вывод: единой модели формы складки на все шесть курсоров нет** - три разных
сценария (плоский жёлоб постоянной глубины; глубокий V; складка, нарастающая
от нуля к выемке), любая правка должна учитывать это, а не применять один
профиль ко всем.

**Сделано для Arrow/Hand (2026-08-09).** `_tip_relight` получил вторую форму
подсветки - `_TROUGH_CURSORS = {"Arrow", "Hand"}` теперь красят не
двух-плато `tanh`-ступеньку, а жёлоб с плоским дном (`_TROUGH_HW0=0.3` логических
единицы у самой вершины, растёт до `+_TROUGH_HW_GROW=2.0` на удалении
`_TIP_RELIGHT_TAPER`, глубина `_TROUGH_DEPTH=40` уровней), аддитивно поверх
мастера, а не заменой уровня. Форма и рост ширины взяты из прямого замера
пикселей оригинала (см. выше), не подобраны на глаз.

Правка ограничена зоной у вершины (`t≲0.6`, где хорда ещё надёжна) - дальше
`_fold_offsets` уже показывал, что реальная складка отходит от прямой хорды,
и жёлоб по хорде там был бы не на своём месте. Arrow_Down (V-залом),
UpArrow/Wait/AppStarting (складка только у выемки, вне текущей зоны) оставлены
на старой `tanh`-ступеньке - для них нет ни проверенной формы, ни надёжной
геометрии в этой зоне.

Замерено `tools/analyze.py`: `tip_contrast` Arrow 0.121→0.329, Hand
0.075→0.185 - оба выше значений на начало сессии (Arrow 0.367 недостижимо,
но близко; Hand 0.108 - жёлоб превзошёл дозамерный уровень). `delta_e`
чуть лучше (Arrow 2.90→2.75, Hand 3.14→3.12). `fold_gap`/`fold_wander`/
`fold_jag` не сдвинулись ни на сотую - жёлоб слишком узкий и слишком близко к
вершине, чтобы задеть трекер складки. `tools/selftest.py` проходит. Владелец
посмотрел на весь набор целиком.

**Не сделано, следующий шаг при возврате к этому пункту.** V-залом для
Arrow_Down (другая форма - острый, без плоского дна) и нарастающая складка
у выемки для UpArrow/Wait/AppStarting (вне текущей зоны хорды, нужен трекер
типа `_fold_offsets`, а не прямая линия вершина-выемка).

## 7. Расслоение вершины у Wait - дефект мастера, не рендера (2026-08-09)

**Что видно.** Владелец заметил на вершине Wait: внешний контур силуэта в
порядке, но внутри яркого клина у самой вершины проходит тонкая тёмная
трещина, будто раскалывающая клин на два лепестка, сходящихся в одной точке -
"два внутренних расслоенных кончика".

**Изолировано.** Дефект виден во всех трёх вариантах: полный пайплайн, без
`_match_author_level`, без `_tip_relight` - обе новые стадии этой сессии ни
при чём. Проверено на сыром `src/ai512` (до маски, до всех стадий `hybrid.py`)
- структура, похожая на раздвоение, там тоже прослеживается (менее очевидно
через фон-инпейнтинг снаружи силуэта, но клин уже выглядит расщеплённым на
входе в пайплайн).

**Согласуется с находкой по п.3.** У Wait в этой самой зоне (t<0.3 от вершины)
у автора в родных 32px нет складки вообще - один гладкий пик без раздвоения
(см. п.3 выше, замер 2026-08-09). Значит сеть при апскейле 32→512
**придумала** складку, которой не было в оригинале, расколов один клин на
два лепестка. Тот же класс дефекта, что крен в п.2 - брак в мастер-цвете
(`src/ai`/`src/ai512`), не в рендере; ни одна стадия `hybrid.py` его не
вносит и, по опыту DEAD_ENDS.md с креном, вряд ли сможет достоверно убрать
рендер-правкой поверх.

**Не сделано.** Требует работы с апскейлом (см. п.2 - паддинг уже пробован
и не помог; направляющая от вектора или ручная доработка мастера остаются
неопробованными). Не проверено на остальных пяти курсорах `_WEDGE_TIPS` -
неизвестно, локальный дефект Wait или общая проблема сети на этой геометрии.

**Куда копать.**

1. Мастер, а не рендер. Дефект внесён апскейлом; всё, что делается поверх, - это
   борьба со следствием. Варианты: другая нарезка на тайлы и паддинг при
   апскейле, апскейл с направляющей от вектора, ручная доработка головы стрелки.
2. Вести разделительную линию как объект трассировки. Сейчас она выводится из
   цвета (`_fold_track`), то есть из того же испорченного источника. Если внести
   её в `traced.json` наравне с контуром, она станет геометрией и получит
   корректное сглаживание на любом масштабе.

**Уже пробовали, не помогло.**

- `_draw_tip` - рисовать вершину из поля расстояний. Три круга правок, три
  дефекта подряд: белёсый засвет, крен вправо, проблеск. Метрика контраста
  росла, потому что стадия делала остриё **ярче фона**; глаз это отвергал.
- Попиксельный запрет светлеть. Гасит ту грань, где нарисованное ярче (на
  стрелках всегда левую), и сам кренит остриё: 4.70 у автора против 5.63.
- Срез верхушки выброса (98-й процентиль). Крен снимает, но контраст Arrow не
  возвращает.
- `_match_author_level` - подтяжка уровней к автору. Смещение средних рядов
  чинит (Hand +0.62 → +0.21), но портит метрики складки на пяти курсорах.
- Резкая локальная коррекция у вершины. Доводит остриё с 49 до 37 при авторских
  14; отвергнута на глаз.
- Подъём `_LEVEL_CAP` с 12 до 90: сдвигает вершину с 61 на 58. Не потолок.
- Сжатие сглаживания резкого члена с 0.5 до 0.05: 37 → 36. Не сглаживание.
- Пороги покрытия: у вершины 0.35..0.79, ничего не отсекают.
- Повторный апскейл другой моделью. Структура сечения та же; яркая полоса есть и
  в родных 32 пикселях автора.

**Важно не повторять.** Авторские 32 пикселя - эталон положения и формы, но не
эталон яркости острия. Его тёмная вершина при увеличении в 16 раз это нехватка
разрешения, а не художественное решение. Три стадии, построенные на обратном,
опущены владельцем подряд.

**Сделано 2026-08-08, по решению владельца.** Из трёх путей выбрана "плоская
заливка у вершин" - аналитическая подсветка, как у семи плоских курсоров
(`_SYNTH_BEVEL`), но только в полосе вдоль хорды складки, а не по всему стеклу.
Новая функция `_tip_relight` в `hybrid.py`, включена на `_WEDGE_TIPS = {Arrow,
Arrow_Down, Hand, UpArrow, Wait, AppStarting}` - все шесть курсоров одного
силуэта "бумажного самолётика".

Механизм: не строить нормаль по полю расстояний до контура (`_bevel_shading`),
это пробовали первым - на этих силуэтах, трассированных с растра, а не
нарисованных вручную, полигон густо усеян вершинами, и освещение по нему
покрывает остриё мелкой гранёной рябью (см. скрин в истории попыток, отдельно
не сохранён). Вместо этого свет строится по самой хорде (`_fold_chord`, две
точки, без полигона вовсе): гладкая ступень `tanh` поперёк хорды, шириной,
сужающейся к остриву, с полом в 2 пикселя, чтобы ступень не уходила
суб-пиксельной и не читалась зерном на сетке.

Смещение (`_TIP_RELIGHT_BIAS = 1.0`) сдвигает эту ступень с самой хорды в
сторону кромки: мастер делил клин пополам примерно по хорде, у автора светлая
грань - узкая полоска у кромки, а тёмная идёт до самой складки. Знак подобран
перебором (единственный из двух сузил светлую грань; другой её расширил).

Проверено визуально на всех шести, кадр 0 и (для Wait) кадры 0/2/4/6/8 -
остриё сходится симметрично, крена нет, шов между заплаткой и AI-мастером не
виден. `tools/selftest.py` проходит.

**Цена - контраст остриёв упал, и это её не пряталось.** `tip_contrast` у
главной вершины (не хвостовых):

| курсор | было | стало |
|---|---|---|
| Arrow | 0.367 | 0.105 |
| Arrow_Down | 0.208 | 0.097 |
| UpArrow | 0.157 | 0.075 |
| Hand | 0.108 | 0.035 |
| Wait | 0.173 | 0.183 |

Тот же выбор, что у `_match_author_at_tips` (см. ниже): у мастера контраст был
завышен ровно тем дефектом, который эта правка снимает - широким пятном яркого
ядра. Подъём амплитуды (`_TIP_RELIGHT_DIFF` с 34 до 95) отыгрывает часть
контраста (Arrow 0.105 → 0.147), но не приближается к исходным 0.367 и на глаз
начинает выглядеть избыточно ярким пятном - тем самым засветом, что и
`_draw_tip` три раунда назад. Оставлено на 34/1.0 - лучший вид, худший
контраст; решение о балансе за владельцем.

`metrics-baseline.json` не переснята (см. пункт 5) - ратчет по `tip_contrast`
сейчас сравнивает с числами до трекера и до разморозки, читать его в текущем
виде нельзя.

**Владелец посмотрел на рендер глазами (2026-08-09) и указал четыре живых
дефекта поверх `_tip_relight`**, не все из них покрыты метриками выше:

1. Красный кончик чуть ярче обычного - незначительно, не мешает, отложено.
2. Крен вверх-вправо у острия всё ещё виден на глаз, несмотря на
   "Проверено визуально... крена нет" из прошлой сессии. Раздел DEAD_ENDS.md
   "The whole line of render-side fixes for the red cursor's tip lean"
   называет это settled: пять раундов правок в рендере отклонены,
   рекомендация - чинить в апскейле, не в `hybrid.py`. `_tip_relight` - более
   узкая правка (полоса у вершины), она не адресует этот крен целиком; не
   трогали в этой сессии.
3. Разделительная хорда от верхней вершины не делит курсор пополам - тот же
   корень, что в разделе DEAD_ENDS.md "The line that slides right" (лист
   прижат к кромке, 0.66-0.99 у мастера против 0.20-0.28 у автора).
   `_match_author_level`, которая это чинила по всему стеклу, отклонена и не
   вошла в код.

   **Сделано (2026-08-09).** `_match_author_level` реализована заново в
   `hybrid.py` (в прошлый раз была отклонена и полностью удалена из кода, не
   осталось даже мёртвой копии) - собственная 32px рамка автора минус наша,
   уменьшенная до 32px, зажата в `±_LEVEL_CAP` (12 уровней), размыта на
   `_LEVEL_SMOOTH` логических единиц при подъёме обратно на полный размер,
   заморожена по циклу для Wait/Hand/AppStarting (та же идиома, что
   `_freeze_lines`). Включена только на `_WEDGE_TIPS`, идёт в `frame_image`
   до `_tip_relight`, чтобы полоса у самой вершины сама поглотила сдвиг через
   собственное усреднение по своей полосе.

   Замерено `tools/analyze.py` на всех шести (баг с собственным кэшем
   `analyze.py` найден и обойдён очисткой `_frame_cache` между прогонами) -
   `delta_e` (цвет) улучшается на всех шести без исключения (Arrow 3.01→2.89,
   UpArrow 4.10→3.54, AppStarting 4.56→4.12 и т.д.). Побочный эффект - на
   `_LEVEL_SMOOTH=3.0` складка UpArrow регрессировала (`fold_gap` 1.69→2.25,
   `fold_wander` 0.094→0.236), Hand потерял 14% контраста острия
   (0.084→0.072). Подобрано `_LEVEL_SMOOTH=4.5`: `fold_gap` UpArrow вернулся
   ровно к дозамерному 1.69, `fold_wander` снизился до 0.198 (не до основы,
   но заметно лучше 3.0). Проверено 6.0 - выгода по wander (0.176) не
   оправдывает дальнейшую потерю цвета (delta_e 3.62→3.70), оставлено на 4.5.
   `tools/selftest.py` проходит. Владелец посмотрел на весь набор глазами.
4. **Внутренний (залитый, непрозрачный) кончик не на одной линии с внешним
   контуром силуэта - править первым, по просьбе владельца.**

**Сделано по п.4 (2026-08-09).** Причина: `_TIP_RELIGHT_BIAS = 1.0` нарочно
сдвигал светлую грань `_tip_relight` с хорды вершина-выемка в сторону кромки
(см. историю константы выше) - хорда сама начинается точно в вершине
трассированного силуэта (`_sharp_corners`, тот же `traced.json`, что и у
маски), но смещённая ступень света/тени рядом с ней уже не сходится к этой
точке, и глаз читает это как "внутренняя заливка целится не в тот угол".
Проверено на глаз парным рендером Arrow/UpArrow, `bias=1.0` против `bias=0.0`
- с нулём внутренняя граница сходится в вершину силуэта, владелец подтвердил
("внутренний курсор нацелился на внешний угол"). Значение переключено на
`0.0`. `tools/selftest.py` проходит.

**Не до конца - отмечено владельцем.** "Сведение кончиков внутренней заливки
не идеальное" даже при `bias=0.0` - сама ступень тона (ширина `tanh`-перехода,
`_TIP_RELIGHT_RIDGE_W`/`_TIP_RELIGHT_TAPER`) не проверялась на сходимость
отдельно, только позиция (bias). Не чинил - следующий шаг, если понадобится
глубже.

**Ещё не пересчитан контраст острия** после смены bias - `_TIP_RELIGHT_DIFF`
и цена по контрасту (Arrow 0.367 -> 0.105 и т.д. из ветки выше) были измерены
на `bias=1.0`; после `bias=0.0` числа другие и не сняты.

**По п.2 (крен): найдена вероятная причина в апскейле, не проверялась
раньше (2026-08-09).** DEAD_ENDS.md уже показал, что смена весов сети не
чинит крен - структура та же в родных 32px автора. Но остаётся неопробованной
гипотезой из "куда копать": "другая нарезка на тайлы и паддинг". Измерено, не
меняя код:

`py_real_esrgan.model.RealESRGAN.predict()` (сторонняя библиотека,
`tools/upscale128.py` вызывает её как есть) перед сетью добавляет
reflect-padding в 15px вокруг всего кадра (`pad_reflect`, дефолт
`pad_size=15`). Главная вершина у всех шести курсоров `_WEDGE_TIPS` сидит в
~3.0px от угла 32px кадра (`Arrow (3.08, 2.95)`, тот же угол у всех - общий
силуэт). Зеркальная копия вершины от reflect-pad оказывается всего в ~6px от
настоящей - глазами видно на сохранённом кропе padded-входа (62x62), который
реально идёт в сеть. RRDBNet (23 блока свёрток) имеет рецептивное поле
намного шире 6px - сеть в буквальном смысле не может реконструировать
вершину, не видя рядом её же зеркальное отражение. Это первое найденное
объяснение, локализованное именно в области, где живёт дефект (у самого
угла кадра), а не общее свойство сети.

**Не сделано.** Только измерение и визуальная проверка (`bleed_extend` +
`pad_reflect` вручную вызваны на `src/orig/cur__Arrow__0.png`), код не менян.
Следующий шаг - проверить гипотезу: перегенерировать хотя бы Arrow через
`tools/upscale128.py` с урезанным/асимметричным `pad_size` (или собственным
паддингом, не зеркалящим так близко к острию) и сравнить структуру сечения
вершины с текущей и с авторской, как это делалось в разделе "Re-running the
upscale" DEAD_ENDS.md. Это меняет `src/ai`, а значит и `traced.json`/весь
силуэт при полной перегенерации - собственник решил делать это через
измерение сначала, не трогая пайплайн вслепую.

**Проверено практически на Arrow, не отражено в `src/ai` (2026-08-09).** Замена `pad_reflect` на edge-replicate
паддинг (монкипатчен `py_real_esrgan.model.pad_reflect`, сама библиотека не тронута) дала
хуже, а не лучше: сечение у вершины стало почти плоским (110-114 везде вместо
тёмной впадины и яркого ядра у текущего reflect). Зеркальный дубль вершины в reflect хотя
и неправильный, но даёт сети хоть какой-то градиент для реконструкции; плоское плато его не даёт
вовсе. Простая смена режима паддинга не фикс.

**Второй вариант тоже проверен и тоже не помог (2026-08-09).** Курсор размещён в холсте 64x64
(реальный прозрачный отступ ~19px до вершины вместо ~3px, `bleed_extend` дорисовывает фон
инпейнтингом как обычно), прогнан через тот же `model.predict`, результат обрезан обратно до
128px, соответствующих исходным 32px. Сечение вершины стало ещё более плоским (102-116 против
baseline 60-189) - хуже, чем при простой смене паддинга. Похоже, сеть чувствительна к масштабу/
контексту, в котором видит курсор (вероятно тестировалась/тюнилась на плотно обрезанных
спрайтах): любое изменение контекста вокруг вершины размывает результат, а не улучшает его.

**Вывод по обоим экспериментам.** Гипотеза "паддинг/отражение около вершины" не подтвердилась
как практический фикс - два независимых способа её обойти дали результат хуже нынешнего. Крен
острия остаётся некинутым: settled-вывод DEAD_ENDS.md ("дело в апскейле, не в hybrid.py") всё ещё
верен по локализации причины, но конкретный механизм (reflect-pad рядом с краем) не единственный
фактор и правка на уровне паддинга - не рабочий путь. Остаются непроверенными: направляющая от
вектора при апскейле, ручная доработка головы стрелки в мастере - обе требуют существенно больше
работы, чем перебор параметров паддинга.

**Отдельный дефект, найден при разборе того же острия (2026-08-08): внешний и
внутренний контур не совпадают.** Не про освещение - про арифметику ресэмплинга.
`_resize` делает предумноженный на альфу Lanczos и делит обратно; Lanczos даёт
отрицательные боковые лепестки, и предумножение их не гасит - каждый канал
звенит на свою величину, по тому, насколько он меняется на резком переходе. У
острия сходятся три границы (кромка, светлое ядро, прозрачность) в паре
пикселей, и на сжатии 512 → 32 канал R (у тёплого стекла - самый большой перепад,
кромка/ядро) звенит в минус и обрезается в ноль, а B (почти не меняется в
исходнике) остаётся с небольшим положительным остатком. После деления на альфу
это читается как синяя крапинка на тёплой кромке: на острие Wait замерено
`(0, 0, 22)` при alpha=44%, притом что автор в обеих соседних точках держит
тёплый почти-чёрный. Видно только на 32px, где остриё - несколько пикселей и
звенят все три канала разом.

**Сделано.** `_declutter_hue_outliers` уже существовал для похожего - обманутый
цвет, торчащий по чужому оттенку - и им не пользовались на уменьшенном рендере,
только один раз на исходных 512. Завёл вторую, более жёсткую проходку в
`_master_rgb` (там же, где сжатие) - тот же тест `sat`/`cos`, порог ниже. На
острие Wait: `(0,0,22)` → `(2,2,7)`, дефолтные пороги давали только `(1,1,16)`.
Гейт не проверял - `delta_e[4]` у AppStarting вернулся под порог сам (был
5.131 > 5.0 после подсветки вершины, до порога не дошёл после этой правки).

**Не до конца.** На пикселях с высокой альфой (a=168, 66%) остаток тона
держится - `(15,32,25)` вместо честного тёплого. Ужесточение порога дальше
(проверено: `sat_floor` 4→2, `cos_span` 1.0→1.4) начинает подъедать настоящий
цвет на соседних, не-дефектных пикселях того же острия. Это компромисс между
"убрать крапинку" и "не тронуть кромку", не решённая задача с одним чистым
ответом - визуально на 32px уже не бросается в глаза, численно ещё есть.

**Проверено по всему набору (2026-08-09).** Прогнал все кадры всех шести
курсоров на 32px через тот же тест (`sat>12`, `cos<0.3`, `alpha>60`). Arrow,
Arrow_Down, UpArrow, Hand - чисто, ни одного пикселя. Wait - один, кадр 1,
слабый (`cos=0.27`, почти на грани порога). AppStarting - семь, до
`alpha=203` (80%), не у главного острия, а ближе к хвостовым уголкам
(`y=19..22, x=8..18` при курсоре 32×32) - тот же звон Lanczos, но в другой
точке контура, которую текущая правка не целила. Цвет там смещается к зелёному,
а не к синему (`(9,37,16)` при жёлтом стекле) - тот же механизм, канал R
обрезается в ноль, G выживает. Порог тот же тест уже видит (`outlier=0.42`),
но не дожимает до конца по той же причине, что и у Wait - дальше только более
жёсткий порог, который начинает есть настоящий цвет. Не чинил дальше -
диминишинг ретёрнс, обозначил как известный остаток.

**Побочная находка: тот же дефект был и на NO (2026-08-09).** NO не входит в
`_WEDGE_TIPS`, вершину ему никто не целил - но вторая проходка
`_declutter_hue_outliers` в `_master_rgb` включена не только для клиньев, она
идёт при любом сжатии для любого курсора. На растущем красном круге NO (кадры
5-10, где круг уже большой) на кромке сидели бирюзовые крапинки - тот же звон
Lanczos, что на остриях, только на кривой линии, а не на угле. Побитовый
diff старого и нового рендера поймал 568 изменённых пикселей там, где их
никто не ожидал; глазами - крапинки исчезли, красный и серый цвет круга и
стрелки не тронуты. Отдельной правки не понадобилось - уже была нужная.

Проверено по всему набору целиком (`sat>12`, `cos<0.3`, `alpha>50`, все 16
курсоров, все кадры, 32px): чисто везде, кроме двух уже названных остатков
(Wait, AppStarting) выше. Cross, IBeam, SizeAll и вся геометрическая семёрка
(`_SYNTH_BEVEL`) не читают `_master_rgb` вовсе, их эта правка не касается ни
одним байтом - проверено побитовым diff, не только тестом на оттенок.

## 2. Разделительная линия изогнута

**Что видно.** Линия от вершины к вырезу хвоста выгибается дугой вместо прямой.

**Что измерено.** Профиль поперёк линии в предрелизной версии: плавный скат без
ступени. Трекер складки на UpArrow неустойчив - в соседних строках прыгает с
x=27 на 14 и на 9, то есть ведёт то одну грань, то другую.

**Трекер починен 2026-08-08** (`tools/analyze.py`). Строка отбраковывается по
prominence найденного минимума, а не по размаху всего окна: старая проверка
складывала кремовый лист с освещённым, получала 78 уровней и пропускала любую
строку. `fold_wander` на 256: UpArrow 1.742 -> 0.088, AppStarting 2.942 -> 0.201.
`inner_jitter` больше не строит якорь по неякоренному треку (тот скачет на 143 px
у Wait и 151 px у AppStarting между кадрами цикла): Wait 0.323 -> 0.023,
Hand 0.096 -> 0.054. У Arrow, Arrow_Down, Hand, NO числа не изменились ни на одну
единицу, `tools/selftest.py` проходит.

**Кривизны в пути линии нет.** После починки трек UpArrow отклоняется от прямой
на 0.24 единицы (среднеквадратично), у Arrow - на 0.29, то есть у «изогнутой»
линии путь ровнее, чем у эталонной. Что действительно расходится - сечение:
`fold_jag` 113 против 15.7 у Arrow. Значит глаз читает не изгиб пути, а то, что
профиль поперёк линии меняет форму от строки к строке. Лечится это вместе с
мастером (пункты 1 и 3), а не геометрией складки.

**Куда копать.**

1. Та же трассировка складки, что и в пункте 1: линия как объект, а не как
   найденное в цвете ребро. С учётом замера выше это даст ровное сечение, а не
   ровный путь - путь уже ровный.
2. ~~Починить сам трекер~~ - сделано.
3. Складка AppStarting теперь не набирает десяти строк на цикл, и `inner_jitter`
   возвращает пустоту. Гейт на это ругается новой строкой `jitter_unmeasured`:
   молчащая метрика хуже отсутствующей.

**Уже пробовали, не помогло.**

- `_draw_fold` - строить линию по хорде от вершины к вырезу. Кривизну по метрике
  чинит (UpArrow 1.742 → 0.29), но принесла подряд: серый клин у вершины
  (выборка цвета доставала до кромки), серое пятно на линии, заусенец на верхнем
  конце, лесенку и размытость. Все сняты по отдельности, но общий вид владельца
  не устроил.
- Сужение полосы к вершине, чтобы линия доходила до конца: тёмный ус через всю
  внутренность (цвет берётся с самой складки).
- Стирание остатка вылепленной складки: зубец не двигается ни при 2, ни при 4,
  ни при 7 единицах сглаживания.
- Растяжка зоны включения (3, 7, 14, 22 единицы): зубец не двигается.
- Полное усреднение поперёк полосы: разделитель исчезает совсем. Перепад между
  листами **и есть** линия.
- Старая стадия `_straighten_fold`: тупит остриё (0.266 против 0.367 без неё) и
  сама источник дрожания, потому что подгоняется покадрово.

---

## 3. Слишком артикулированный глянец

**Что видно.** Перепад между кремовым и жёлтым листом резче, чем в оригинале;
объём читается как вылепленный, а не как стекло.

**Причина.** Апскейлер сочиняет объёмную складку там, где у автора плоская
заливка. Это тот же корень, что в пунктах 1 и 2.

**Куда копать.** Лечится вместе с мастером. Отдельной ручки не нужно:
когда в эксперименте с `_draw_fold` ложная лепка убиралась, глянец спадал сам.

---

## 4. Сборка идёт долго

**Состояние.** 9 мин 36 с целиком. Замер по фазам:

| фаза | время |
|---|---|
| анимации (webp, gif, полосы) | 267 с |
| прогрев кадров, 12 ядер | 183 с |
| превью | 80 с |
| Windows | 36 с |
| Linux, оригинал, сравнение | 7 с |

**Уже сделано** (обе правки побитово не меняют картинку):

- Таблица на 256 входов вместо степени в `srgb_to_linear`. Совпадение проверено
  `array_equal`. Кадр 7.15 → 5.27 с.
- Растеризация и поле расстояний считаются раз на курсор, а не на кадр: у Wait,
  Hand и AppStarting контур одинаков во всех девяти ключевых кадрах. Совпадение
  проверено побитово на всех шестнадцати; морфы условие отсекает само.
  Последующие кадры 4.82 → 1.35 с.

**Куда копать дальше.** Анимации и превью, то есть кодирование webp и gif с
квантованием палитры. Рендер там уже не при чём. Кодирование распараллеливается
по курсорам так же, как прогрев кадров.

**Видеокарта не поможет.** Апскейл вынесен офлайн, в сборке его нет; остальное
упирается в повторный счёт и кодирование, а не в арифметику, ради которой стоило
бы платить пересылкой в память видеокарты.

**Сделано 2026-08-09 (`build.py`).**

1. «Рендер там уже не при чём» оказалось наполовину неверно. `build_preview`
   рисует тайлы на 192px (`PREVIEW_CELL`), полоса в `build_animations` - на
   160px (`ANIM_STRIP_BOX`). Ни тот, ни другой размер не входил в список
   прогрева (`_warm_frames`), поэтому оба тянули холодный, однопоточный рендер
   уже ПОСЛЕ прогрева - то самое время, что выглядело кодированием, а было
   рендером не на том размере. Добавил оба размера в прогрев. Замерено:
   прогрев дороже на ~25-46 с (два лишних размера на все 58 кадров), зато
   `build_preview` падает с 71.7 до 3.8 с - в 17 раз.
2. Собственно кодирование - `_encode_anim_job`, шесть независимых файлов (5
   курсоров + сводная полоса) на `cf.ProcessPoolExecutor`. Полоса и есть узкое
   место, которое не размножить: 121.8 с webp на одном ядре против 17-18 с у
   каждого курсора - но раз файлы не зависят друг от друга, ядра берут по
   одному, а не одно ядро - все шесть по очереди.
3. Побитовое совпадение проверено: старый последовательный код (снят с
   `HEAD`) и новый параллельный - на одинаковом состоянии `hybrid.py` дают
   идентичные по SHA256 файлы, все двенадцать (5×2 + полоса×2 + не забыть,
   что здесь их 6 задач = 12 файлов). Настройки `_WEBP` (`quality=90,
   method=6`) не тронуты - только где физически выполняется `.save()`.

**Замер целиком** (прогрев + превью + анимации, три фазы из таблицы выше):
было 164.3 + 71.7 + 241.4 = 477.4 с, стало 189.3 + 3.8 + 137.6 = 330.7 с -
минус 146.7 с. От заявленных 9 мин 36 с это около четверти всего времени
сборки, без единого изменённого байта на выходе.

**Прогнан целиком.** `python build.py` от начала до конца, засечено оболочкой:
407 с против заявленных 576 с (9 мин 36 с) - минус 169 с, около трети. Гейт
самой сборки (`check_metrics`) прошёл: "metrics: all within tolerance". Пакеты
и Windows/Linux эта правка не трогает - там и не должно было измениться.

Замечание не по скорости: обычный `python build.py` пишет поверх
`assets/*.webp`, `assets/*.gif` и `preview.png` - это отслеживаемые в git
файлы, и после запуска они отражают текущее состояние `hybrid.py`, то есть
разморозку, подсветку вершин и правку оттенка из пунктов 1 и 6 разом, ещё не
подтверждённые владельцем. После замера вернул их к `HEAD`
(`git checkout -- assets preview.png`), чтобы рабочее дерево не выглядело
решением там, где решения ещё нет.

---

## 5. База метрик отстала от рендера

`metrics-baseline.json` снята на коммите 1739343, рендер откачен на f1769ec.
Гейт красный сам по себе, до любых правок: 21 отказ, из них семь - обвал
`tip_sheen` (AppStarting 15.16 -> 0.018, Hand 13.02 -> 0.000, Wait 11.43 ->
0.037). Это значит, что предрелизный рендер держит кончики замороженными -
ровно то, что снимал коммит a935ecc. Пока база не переснята, `--check` меряет
разницу двух рендеров, а не регрессию.

Решать владельцу: пересъёмка базы принимает замороженные кончики как норму,
возврат разморозки тянет за собой всё, что откат снял. CI гоняет `--check` без
`--full`, база же снята на полной лестнице - переснимать надо той же командой,
что стоит в `.github/workflows/ci.yml`.

---

## 6. Кончики разморожены (2026-08-08)

**Что было.** `_STILL_TIP_R = 6.0` в `hybrid.py` глушит блик до среднего по
циклу в радиусе 6 логических единиц вокруг каждой острой вершины. Радиус
подобран под `_tip_pinch` («хватало, чтобы накрыть всю окрестность, откуда
`_tip_pinch` берёт цвет края») - но `_tip_pinch` в файле есть, а вызовов нигде
нет, тот же мёртвый код, что и `_straighten_fold`/`_smooth_along_fold`.
Оставшийся радиус глушил блик у AppStarting/Hand/Wait почти до нуля:
`tip_sheen.amp` 0.02 / 0.00 / 0.04 против авторских 14.7 / 13.6 / 10.7 - отсюда
и обвал в пункте 5.

**Сделано.** Радиус снижен до 1.75 (то самое значение уже стояло в отведённой
ветке дня, см. `DEAD_ENDS.md` про `still_tip_r_6`, но откат вернул 6.0 вместе со
всем остальным). Замер по трём курсорам, `amp`/`author`:
AppStarting 15.23/14.66, Hand 12.75/13.60, Wait 10.62/10.65 - блик доходит до
острия и не превышает авторский размах. `wobble` (гуляние светлого пятна между
кадрами) у всех трёх ниже авторского: 0.32/0.39, 0.29/0.34, 0.30/0.34 - то есть
дрожания остриё не приобретает. Проверено визуально попкадрово (не только
метрикой) - контур вершины неподвижен, гуляет только блеск.

**Побочный эффект.** `AppStarting.fold_jag` на 256 выросло со 118 против 104 у
устаревшей базы (после починки трекера уже было 74, разморозка добавила ещё).
Складка у AppStarting и так мельче порога отбраковки на большинстве строк (см.
пункт 1), метрика там шумит сильнее, чем видит глаз - на филмстрипе всех девяти
кадров целиком, при 512px, разницы с замороженной версией на глаз нет. Не
считаю регрессией, но не проверено на реальном экране в системном трее.

`Hand.tip_contrast` (0.212 → 0.108) и `Wait.tip_sheen` формально ниже устаревшей
базы (пункт 5) - тот же корень: заморозка держала кадр 0 на среднем по циклу,
который случайно контрастнее живого кадра 0. Ратчет по `tip_contrast`/`tip_sheen`
сравнивает с базой от 1739343, которая не видела ни трекера, ни разморозки;
пересъёмка базы (пункт 5) закроет и это.

---

## 8. Крен Wait: попытка через `along` не изменила восприятие (2026-08-09)

После находки в п.2 (крен уже сильно сократился побочным эффектом сессии -
граница освещённого листа 0.36/0.31/0.27/0.29/0.28/0.30 против авторских
0.20/0.17/0.24/0.25/0.25/0.28, разрыв 0.5-0.7 → 0.05-0.10) владелец попросил
дожать остаток точечно.

**Пробовано.** Сузил `along` для Wait/UpArrow/AppStarting с 0.6 до 0.35 -
метрика "граница листа" сдвинулась ещё ближе к автору (AppStarting
0.18/0.15/0.07/0.12/0.11/0.30). Изменение оставлено в коде (`_TROUGH_PARAMS`),
цвет и складка от него не пострадали.

**Не помогло с тем, что видит владелец.** После правки крен на Wait "примерно
так же" на глаз - метрика и зрительное восприятие разошлись. Это третья
попытка обойти крен через `hybrid.py` за эту сессию (после двух неудачных
экспериментов с паддингом апскейла в п.2) - все три ничего не дали визуально.
Подтверждает settled-вывод DEAD_ENDS.md ("The whole line of render-side fixes
for the red cursor's tip lean"): дело в мастере (`src/ai`/`src/ai512`), не в
рендере. Дальше жать параметры `_tip_relight`/`_match_author_level` ради
крена - вряд ли продуктивно; следующий реальный шаг, если возвращаться к
этому - направляющая от вектора при апскейле или ручная доработка мастера
(оба непроверены, оба существенно тяжелее правки параметров).

## Итог сессии 2026-08-09 (кратко)

Все шесть `_WEDGE_TIPS` курсоров:

- **Крен вершины (bias).** `_TIP_RELIGHT_BIAS` 1.0 → 0.0 - внутренняя заливка
  сходится к вершине силуэта, а не к сдвинутой на кромку линии. Подтверждено
  владельцем.
- **Хорда не делит пополам / цвет.** Заново реализована `_match_author_level`
  (была отклонена и удалена в прошлой сессии) - цвет (`delta_e`) улучшился на
  всех шести без исключения.
- **Форма складки.** `_tip_relight` переписана: единая модель "жёлоб" вместо
  двух-плато `tanh`-ступеньки, с параметрами по курсору (`_TROUGH_PARAMS`) -
  плоский жёлоб (Arrow, Hand), острый V (Arrow_Down), растущая от нуля канавка
  (UpArrow, Wait, AppStarting). Контраст острия вырос значительно выше
  дозамерного уровня на Arrow и Hand.
- **Расслоение/несколько кончиков.** Три независимые причины найдены и
  устранены: (1) `hw` не сходилась к нулю в самой вершине (полы на `taper_frac`
  убраны); (2) полоса замены была уже, чем второй провал, придуманный AI-
  апскейлом (`_TIP_RELIGHT_LATERAL` расширен, из линейного ската в плато;
  затем заменён на полное покрытие маски силуэта); (3) `_tip_relight` менял
  только яркость, оставляя цветовую (chroma) полосу мастера нетронутой -
  теперь заменяются оба канала.
- **Обрыв желобка у трёх "гладких" курсоров.** Резкий переход "плоско → сразу
  полная складка мастера" на t=0.3 заменён на собственную растущую канавку
  через весь диапазон (тот же механизм, что у Arrow/Hand, с более длинным
  `taper`).
- **Крен (п.2).** Не решён, признан settled-дефектом мастера после трёх
  безрезультатных попыток в этой сессии (см. п.8). Задокументирован, не
  скрыт под улучшившейся, но не решающей его метрикой.

Всё в рабочем дереве, ничего не закоммичено. `tools/selftest.py` проходит
после каждой правки. `metrics-baseline.json` по-прежнему не переснята (п.5) -
числа из полного `analyze.py --check --full` не сверялись с эталоном, только
попарные сравнения до/после внутри сессии.

---

## 9. Аудит общей картины (2026-08-XX)

Прогнал полную ревизию после починки складки. Найденное, по убыванию важности.

### 9.1. Сборка не проверяла качество

`build.py.check_metrics()` печатала «metrics: all within tolerance», проверяя
**только две** величины: дрейф медианной альфы и насыщенность. Ни `delta_e`, ни
геометрия складки, ни контраст острия в её область не входят. Настоящий гейт -
`tools/analyze.py --check` - сборкой **не запускался вообще**. «Сборка зелёная»
не значило почти ничего: мимо неё проезжали две худшие цветовые ошибки набора и
курсоры, четверть площади которых темнее всего, что есть у автора.

Подключено: `check_quality()` в `build.py` зовёт `analyze.gate()` против
`metrics-baseline.json`. Гейт уже сам различает `bad` (стало хуже базы или
пробит абсолютный порог) и `debt` (цель не достигнута, но не хуже прежнего) -
сборку валит только `bad`, а `debt` печатается счётчиком, чтобы долг не рос
молча. Нет базы - шаг пропускается (свежий клон должен собираться).

**Важно:** запуск `analyze.py --check` **без файла базы** валит всё подряд -
пороги `fold_gap`/`fold_wander`/`fold_jag` равны 0.0 и заявлены как
недостижимый пока идеал. Сравнивать надо только с базой.

**Плюс `metrics-known-issues.json`.** Ратчет сравнивает числа, а часть жалоб
числа не несёт вовсе - вида «эту величину не удалось измерить»
(`jitter_unmeasured`, `fold_unmeasured`). Такие поднимаются безусловно на
каждом прогоне, базой не гасятся и заклинили бы сборку навсегда: первый же
прогон нового гейта упал на `AppStarting jitter_unmeasured`, при том что эта
жалоба была и до всех правок аудита. Они вынесены в отдельный файл с пояснением
на каждую; сборку валит только жалоба, которой там нет. **Числовую регрессию
туда вписывать нельзя** - её ловит база, и заглушить её этим файлом значит
проделать дыру в гейте.

### 9.2. AI-мастер альфы давит слабый хвост, а выравнивание это закрепляло

`_up_alpha_native` подгоняла **медиану** видимой зоны под авторскую. Медиана
сходилась точно - и при этом внутри маски у NO[7] среднее было 115.7 против
авторских 141.9, у Handwriting[6] 134.9 против 161.0. То есть 16-18% авторского
покрытия отсутствовало, а метрика уровня рапортовала успех: сеть сохраняет
середину распределения и подъедает слабый край, медиана этого не видит.
`_ai_tonality` тоже не ловит - обоим кадрам ставит 1.00, потому что хвост
истончён, а не бинаризован. `_ai_dropout` тоже мимо: он ищет жёсткую трещину с
контентом по обе стороны, а тут край съеден равномерно и везде.

Это два худших кадра набора по `delta_e` (8.96 и 6.63) - но не единственные
провальные, см. про усечённую выдачу гейта в 9.4.

**Чем чинить - и чем не получится.** Сначала попробовал скаляр: матчить не
медиану, а взвешенное маской среднее (ту статистику, которую видит `delta_e`
после композита). `delta_e` от этого резко улучшился, но **скаляр не может
вернуть придавленный хвост** - он тянет вверх всё распределение целиком, и
середина стекла ушла на +25% выше авторской медианы у NO[7] и +20% у
Handwriting[6]. Это пробило `build.check_metrics` (порог 8% по дрейфу медианной
альфы) - то есть я разменял один дефект на другой, а не починил.

Правильная починка - по форме распределения, а не по уровню: там, где мастер
тоньше опорного Ланцоша, вес отдаётся обратно Ланцошу пропорционально разнице
(`_THIN_LEAN` в `_up_alpha_native`). `_ai_dropout` делает то же самое для
бинаризованного мастера - жёсткая трещина с контентом по обе стороны, - но эта
вторая, мягкая форма отказа сети не ловилась ничем: середина сохранена,
`_ai_tonality` ставит 1.00, медиана сходится, а слабый край съеден везде сразу.

**Политику уровня в итоге не трогал вообще.** Пробовал перевести обе стадии
(`_up_alpha_native` и посоразмерную подгонку в `_up_alpha`) на авторские 32 -
и это две отдельные грабли подряд:

1. Цель брал на 32, а мерил на 128. Кромка маски всегда в один аппаратный
   пиксель, то есть целая логическая единица на 32 и четверть на 128; у тонкого
   силуэта почти вся площадь - кромка, и «среднее по маске» на разных масштабах
   это разные величины. IBeam на этом раздулся с delta_e 4.4 до 6.7.
2. Свёл обе стороны на 32 - IBeam вылечился, но сборка упала на другом:
   `Handwriting[6]@512 median alpha drift +10.3%`. Гейт мерит на 128 **и на
   родном якоре 512**, а я смотрел только 128. Видимая зона у 512 - почти
   сплошная внутренность, у авторских 32 - в основном сглаженная кромка;
   подгонка на 32 закономерно задирает медиану на 512.

Итог: истончение хвоста - дефект **формы**, и чинить его надо там, где он
живёт (`thin`/`_THIN_LEAN`). Уровень остался ровно прежним, по медиане на
`_LEVEL_REF`. С одной только починкой формы дрейф медианной альфы худший
**+1.8%** (порог 8%) - то есть правка не воюет с существующим гейтом, а
проходит его с запасом.

### 9.3. `_SYNTH_BEVEL` рисовал контраст, которого автор не рисовал

Размах яркости у семи геометрических курсоров: 174-202 уровня против авторских
101-130. 8.8% видимой площади темнее самого тёмного авторского пикселя (SizeWE
22.0%: его пол 125, у нас доходило до 81), 3.3% упиралось в 255 при его потолке
226.

Отход от авторских пикселей у `_SYNTH_BEVEL` заявлен как осознанный, но
придуманная чернота ниже его пола и выбитые клиппингом света - дефекты
независимо от этого. `_BEVEL_DIFF` 52->31, `_BEVEL_RIM` 26->15.5 (x0.6):
клиппинг ушёл (0.4%), подпольная доля 8.8%->3.6%, delta_e по семёрке 5.51->4.46.

Ниже 0.6 `delta_e` продолжает падать - и будет падать до самого нуля, предел
это его собственный размытый кадр. Поэтому точка остановки не метрика, а два
инварианта выше; грани должны остаться гранями.

### 9.4. Осталось нерешённым

- **NO целиком**: было 5 кадров выше допуска (5: 5.86, 6: 5.68, 7: 8.96,
  8: 5.47, 9: 5.15), стало 4 (5: 5.69, 6: 5.13, 7: 6.75, 8: 5.25). Худший упал
  8.96 -> 6.75, но порог не взят.

  Причина остатка не в альфе: у NO[7] кольцо у нас RGB (247, 41, 37) против
  авторских (216, 49, 41) - жёстче и насыщеннее, а стрелка под ним потеряла
  внутреннюю структуру. Это отдельный слой, к уровню альфы отношения не имеет.

  Плюс ~10% авторской альфы лежит **вне** трассированного вектора
  (`Handwriting[7]` 12.5%, `IBeam[0]` 19.0%, медиана по набору 4.3%). Это цена
  чёткого контура, расширять его - терять смысл ремастера.

- **IBeam[0] 5.37** (было 7.04). Тот же корень: 19.0% авторской альфы вне
  вектора - худший показатель набора. Самый тонкий силуэт, у него доля кромки
  максимальна.

  **Отдельно, про метод - и это главный урок аудита.** Гейт печатает только
  худший кадр каждого курсора (`delta_e[7] 8.959`), поэтому по его выводу
  кажется, что провальных кадров во всём наборе два. На деле их было
  **двенадцать** из 58 - остальные десять просто не показаны, они не худшие в
  своём курсоре. Считать долг по выводу гейта нельзя, надо брать
  `delta_e(name)['per_frame']` целиком.

  Я на этом ошибся дважды подряд: сначала объявил, что провальных кадров два,
  потом - что правка ухудшила NO[5]/NO[6] с «проходят» до 5.70/5.85, хотя они и
  до неё были 5.86/5.68. Оба раза источником была одна и та же усечённая
  выдача.

  Итог по набору после правок: кадров выше допуска **12 -> 5** (числа в 9.5).
- **NO - главный остаток** (кадры 5, 6, 7, 8: 5.69 / 5.13 / 6.75 / 5.25).
  Альфа тут больше не при чём, замерено: кольцо у нас RGB (247, 41, 37) против
  авторских (216, 49, 41), а стрелка под ним потеряла внутреннюю структуру.
  Следующий шаг - смотреть слой кольца, не альфу.
- **UpArrow `tip_contrast` 0.061** - ниже даже старого порога 0.085.
  Единственный из шести клиньев без складки в зоне острия (её нет в его
  собственном рисунке), получает только выравнивание полосы.
- **`metrics-baseline.json`** переснята после этих правок, старая была от
  2026-08-08 и предшествовала и починке трекера складки, и разморозке острия -
  сравнивать с ней было бессмысленно.

### 9.5. Что дали правки, в числах

`delta_e` по всем 58 кадрам, до -> после:

```
кадров выше допуска 5.0     12  ->   5
худший                    8.96  -> 6.75
среднее                   4.07  -> 3.78
медиана                   3.66  -> 3.59
```

Осталось выше допуска: NO[7] 6.75, NO[5] 5.69, IBeam[0] 5.37, NO[8] 5.25,
NO[6] 5.13. Ухудшений четыре, все в допуске и мелкие: Handwriting[7] +0.55,
NO[10] +0.50, Handwriting[8] +0.27, NO[0] +0.14.

Промежуточный вариант с подгонкой уровня по среднему давал лучше (4 кадра,
худший 6.71, среднее 3.51) - и пробивал гейт дрейфа на 512. Отброшен: ловить
`delta_e` ценой другого гейта это не починка. Здесь дрейф медианной альфы
худший **+1.8%** при пороге 8%. `tools/selftest.py`
проходит. `metrics-baseline.json` переснята на той же лестнице, что читает
`build.check_quality` (`LADDER`, без `--full`) - см. правило 3 ниже, ратчет и
проверка обязаны идти на одной лестнице.

**Что пересъёмка базы узаконила, а не починила.** Ратчет по определению делает
текущие числа новым полом, поэтому просевшее надо назвать вслух:

```
                delta_e            tip_contrast
              old -> new           old -> new
Arrow        2.47 -> 3.07         0.326 -> 0.171
UpArrow      3.15 -> 3.60         0.170 -> 0.061
IBeam        7.04 -> 5.37         0.105 -> 0.070
Cross        5.85 -> 4.66         0.073 -> 0.053
NO           8.54 -> 6.75         0.249 -> 0.244
```

Оговорка про атрибуцию: старая база от 2026-08-08 предшествует не только этому
аудиту, но и замене жёлоба на ступеньку (коммит `c63c04b`). Рост `delta_e` у
Arrow (2.47 -> 3.07) и UpArrow (3.15 -> 3.60) - оттуда, а не из правок альфы и
бевела: в замере до/после внутри самого аудита оба кадра почти не двигались.

`tip_contrast` просел у Arrow/UpArrow (модель складки: жёлоб -> ступенька, и
UpArrow складки в этой зоне не имеет вовсе) и у IBeam/Cross (ослабленный
бевел). Метрика мерит перепад с фоном и растёт, когда остриё делают ярче
окружения - см. правило 2, именно она хвалила засвет, который глаз отвергал.
Но старая база была снята до починки трекера складки и до разморозки острия,
так что сравнивать с ней было нельзя ни в какую сторону; новая хотя бы
описывает существующий код. Если `tip_contrast` важен сам по себе - его надо
поднимать отдельной работой, а не считать, что он не просел.


---

## Правила, выведенные из провалов

1. **Всякая величина, подогнанная по текущему кадру, становится источником
   дрожания**, даже когда картинка на глаз не меняется. Три случая с числами:
   амплитуда рельефа из живого кадра (1.149 против 0.972), темп скаляром на весь
   диск (1.027 / 1.053 / 1.091 против 0.972 / 1.016 / 0.990), покадровая подгонка
   смещений складки. Проверять новую константу на покадровость первым делом.
2. **Если метрика хвалит правку, которую глаз отвергает, разобрать метрику,
   прежде чем защищать правку.** `tip_contrast` мерит перепад с фоном и растёт,
   когда остриё делают ярче окружения; ровно это и читалось как засвет.
3. **Ратчет и проверка обязаны идти на одной лестнице масштабов.** `--ratchet`
   без `--full` пишет 32/64/128/256, а `--check --full` меряет до 512; один
   `scale_drift` расходится 0.1337 против 0.1409. Симптом - все шестнадцать
   курсоров «регрессируют» одинаково.
4. **Начинать с изоляции, а не с ручек.** Выключение стадии целиком занимает
   минуту и отсекает сразу несколько объяснений; перебор параметров на неверной
   стадии стоил пяти кругов на одном заусенце.
5. **На этой SMB-шаре не пользоваться `git stash`.** Откатил дерево и не смог
   вернуть (`unable to unlink old 'assets/Hand.webp': Invalid argument`), работа
   осталась только внутри стеша. Снимать эталон копией файлов в сторону.
6. **Гейт печатает только худший кадр каждого курсора.** По его выводу нельзя
   считать, сколько всего кадров не проходит: в аудите 2026-08-XX по гейту
   выходило два провальных кадра, а на деле их было двенадцать. Для инвентаря
   брать `delta_e(name)['per_frame']` целиком. И запускать `analyze.py --check`
   **только с файлом базы** - без него пороги `fold_*` равны 0.0 (заявленный
   идеал) и валится всё подряд.
7. **Скаляр не чинит форму распределения.** Если AI-мастер придавил слабый
   хвост альфы, подгонка уровня (хоть по медиане, хоть по среднему) не вернёт
   его - она либо закрепит потерю, либо раздует середину. Правка обязана быть
   попиксельной (`_THIN_LEAN`), иначе один гейт чинится ценой другого.

---

## 10. Складка: сделать её линией, а не затенением (2026-08-XX)

Отчёт владельца после аудита: хорда, делящая курсор пополам, «практически не
видна на сером курсоре», плюс просьба свести внутренние кончики с внешним «на
одном уровне».

**Замер сначала, а не ручки.** Перепад через складку был на месте - 53-58
уровней в стекле, 33-56 на экране после композита (на разных фонах). То есть
контраст формально авторский. Не читалось другое: `edge=0.5` растягивал переход
на 0.92 логической единицы, а это семь аппаратных пикселей плавного ската на
512px. Скат такой ширины между двумя большими плоскими гранями глаз читает как
затенение, а не как линию.

**Что сделано.**

- `edge` 0.5 -> 0.12. Ниже ~0.125 всё равно вступает пол `width` (держит
  ступеньку в два аппаратных пикселя на любом размере, ради антиалиасинга), так
  что 0.12 означает «отдать решение полу»: одна чёткая линия, одинаково узкая
  на всех размерах. Побочно закрылся и второй пункт: переход теперь
  разрешается в 1.8 аппаратного пикселя от точки, где начинается сам силуэт,
  то есть кончики сведены настолько, насколько позволяет сглаживание.
- `diff` 58 -> 85, **осознанный отход от автора** по прямой просьбе. 58 - его
  собственный размах, и одного его мало: mean-anchoring сажает нашу полосу на
  уровень AI-мастера, а он в этой зоне уровней на 25 ниже авторского (наши
  грани 185/127 против его 210/152). Та же ступенька на более тёмной паре
  граней несёт меньше видимого контраста. 110 пробовал - светлая грань тяжелеет.

**Цена, замерена:** delta_e 3.07 -> 3.20 (Arrow), 3.25 -> 3.45 (Hand),
3.81 -> 3.88 (Arrow_Down) при допуске 5.0. `edge` сам по себе бесплатен
(3.07 -> 3.07).

**Урок.** «Перепад правильный» и «складку видно» - разные утверждения. Первое
меряется в уровнях, второе зависит ещё и от того, на скольких пикселях этот
перепад размазан. Мерить надо оба, иначе метрика говорит «всё в порядке» там,
где глаз ничего не находит.

---

## 11. Мёртвая анимация: заморозка держала сам блик (2026-08-XX)

**Как смотрели.** Контактные листы: авторские 32px (NEAREST) над нашим
рендером - шесть клиньев в статике, три анимации покадрово, кропы всех трёх
вершин, плюс карта «отклонение кадра от среднего по циклу» для нас и для
автора.

**Что показала карта.** У автора вся анимация - это тонкая яркая полоса,
бегущая по кромке силуэта, с вспышками на остриях. У нас - широкое размытое
пятно по телу, кромка почти не участвует, а вершины тёмные, то есть мертвы.
Числом (`tip_sheen.amp`, худшая вершина, уровни яркости после композита):

| курсор | наш | автор | доля |
|---|---|---|---|
| Hand | 4.52 | 13.60 | **0.33** |
| Wait | 8.07 | 10.65 | 0.76 |
| AppStarting | 11.45 | 14.66 | 0.78 |

Гейт требует 0.75, Hand его не проходил.

**Две независимые причины, обе найдены изоляцией.**

1. `_freeze_lines` держал вообще всю деталь мельче `_FREEZE_UNIT` из кадра 0 -
   а блик автора и есть деталь этого масштаба. Заморозка писалась против
   дрожания внутренних линий и с этим справляется, но заодно выключала то
   единственное, из чего сделана анимация.
2. `_tip_relight` привязывал свою полосу к **одному числу** - средневзвешенному
   уровню полосы. Две плоские грани, у которых на весь цикл есть ровно одна
   степень свободы (общая яркость), и есть мёртвая вершина: по вершинам хвостов,
   которых стадия не касается, размах у нас был **больше** авторского
   (Hand 17.2 и 14.2 против его 13.6 и 16.2), а на главной вершине - 4.5 против 13.8.

**Что сделано.**

- `_freeze_weight` - заморозка стала пространственной, а не сплошной: единица
  внутри стекла, ноль вдоль кромки (`_FREEZE_RIM = 1.5`, растушёвка
  `_FREEZE_RIM_FADE = 1.2`). Прямо по правилу владельца: «внешние дрожания -
  ничего страшного, главное чтобы не дёргались слои внутренние».
- Складка держится отдельно (`_FREEZE_FOLD = 1.2` логической единицы от своей
  хорды), потому что у выемки хвоста она сама подходит к кромке ближе, чем
  `_FREEZE_RIM`, и отпуск по одному расстоянию до края её терял: замерено
  `fold_wander` Hand 0.015 -> 0.033, `temporal_fold` 1.001 -> 1.043, `fold_jag`
  Wait 44.4 -> 51.3. С явным удержанием - 0.009 / 1.007 / 35.6.
- `_band_level` - якорь полосы `_tip_relight` стал полем, а не скаляром:
  взвешенное по полосе размытие на `_TIP_ANCHOR_SMOOTH = 3.5` логической
  единицы. Всё, на чём жил дефект мастера (выдуманная вторая линия - волосок,
  второе остриё - около единицы шириной), по-прежнему заменяется; блик, который
  метёт треть курсора, проходит.
- Поле - **только для `INTERP`**. У неподвижного курсора блика нет, а цена
  есть: у Arrow локальный уровень у вершины ниже среднего по полосе, заплатка
  садится темнее и контраст острия падает 0.215 -> 0.157 (проверено и 5.0:
  0.132). Покупать этим на неподвижном кадре нечего.

**Результат, числами.** Размах у главной вершины: Hand 4.63 -> 10.75 (автор
13.83), Wait 8.68 -> 9.64 (10.65), AppStarting 12.25 -> 13.81 (15.04). Хвостовые
вершины не тронуты и по-прежнему выше авторских. `fold_wander`, `fold_gap`,
`inner_jitter` - не хуже прежних. Побитовый diff всех шестнадцати курсоров на
32px против `HEAD`: изменились ровно три анимированных (Hand 823 пикселя,
Wait 661, AppStarting 578), остальные тринадцать - **байт в байт**.
`tools/selftest.py` проходит.

**Цена, названная вслух.** `tip_contrast` Hand 0.220 -> 0.154 - это та же
метрика, про которую правило 2 говорит, что она хвалит засвет; глазами парный
кроп вершины (автор / `HEAD` / новое) читается лучше нового: складка у Hand
из мягкой серой размывки стала линией. Остальные расхождения с базой -
`fold_jag` AppStarting +0.3, `fold_luma_step` Wait +0.17, `temporal_fold`
Hand +0.006 - на уровне шума самой правки. `AppStarting jitter_unmeasured`
проверено на коде `HEAD` - было и до правки, не отсюда.

**Проверено и отвергнуто в этой же сессии.**

- **Выдавливание сечения к хвостовым остриям** (сэмплировать цвет с глубины
  `d + a(d)` и ставить его в `d`, с поперечным масштабом по ширине клина).
  Идея - довести внутреннюю заливку до самой вершины. На глаз даёт гребёнку:
  тёмная линия кромки у самого острия рассыпается на штрихи. Перебрано четыре
  набора (`pull` 0.9..2.4, `reach` 5..7, ограничение масштаба 2.5..6, удержание
  последних 0.5 единицы) - гребёнка остаётся во всех. Это тот же класс, что
  `_taper_tip_rim` в DEAD_ENDS.md, и с тем же исходом.
- **Поперечное усреднение у хвостовых вершин** (убрать все боковые полосы,
  оставив градиент вдоль оси). Полосатит: помодульные средние по расстоянию
  шумят, и шум читается как штриховка.
- **Замер вместо догадки:** у автора вдоль хорд «хвостовая вершина - выемка»
  складки нет вообще, там гладкий скат (Arrow t=0.10..0.60: 176-174-170,
  191-196-206-190-184-166, ...). То, что глаз читал как «тупой внутренний
  кончик», у хвостов - не отсутствие складки, а тёмная кромка мастера; она у
  автора тоже тёмная (139-151). Не чинилось.
- **`_match_author_level` покадрово** (сейчас заморожена по циклу). Даёт
  Hand amp 10.75 -> 11.90 и чуть лучше `delta_e` на всех трёх, но
  `temporal_fold` Hand 1.007 -> 1.024 и AppStarting 0.992 -> 1.008, то есть
  пробивает гейт ради 11% размаха на одном курсоре. Не включено.

**Что осталось и почему не тронуто.**

- **Широкое белёсое гало по кромке UpArrow.** Изолировано: видно и с
  выключенной `_match_author_level`, и с выключенной `_tip_relight` - дефект
  мастера (`src/ai`), не рендера. Тот же класс, что крен острия (п.2).
- **`tip_convergence` у правого хвоста 0.9-1.6 у всех шести.** По замеру выше
  у автора в этой зоне складки нет, метрика ищет провал, которого в рисунке не
  существует; лечить её значит рисовать линию, которой автор не рисовал.
  Владелец просил, чтобы внутренний кончик **не доходил** до внешнего - текущее
  состояние это и есть.
- **Складка на сером Hand кажется слабее, чем на цветных.** Замерено после
  композита (перепад поперёк складки, медиана по строкам): наш Hand 42.9 на
  светлом фоне и 63.7 на тёмном против авторских 26.0 и 62.0 - то есть у нас
  контраст не ниже, а выше. Ощущение не подтверждается числом, ручки не
  крутились.

## 12. Лишний слой на кромке - неподключённый ограничитель (2026-08-XX)

Владелец, глядя на рендер, указал: «лишние слои контуры и тени... есть и
внутри и снаружи курсоров». Сравнение с `f1769ec` (коммит, который владелец
называл «почти идеально») подтвердило: на кромке всех шести клиньев есть
вторая яркая полоса, которой в `f1769ec` нет.

**Причина.** `_TIP_RELIGHT_LATERAL`/`_TIP_RELIGHT_LATERAL_FALLOFF` объявлены
и подробно прокомментированы («logical units either side of the chord the
relight replaces at full weight») ещё в момент введения `_tip_relight`
(между `f1769ec` и `HEAD`), но ни разу не использованы в самой функции -
`band = along * mask` заменяла весь срез клина по ширине, а не узкую полосу
у складки. Аналитическая ступенька красила и кромку тоже везде, где
`along>0` (до ~8 логических единиц от вершины на `along=0.35`), поверх
собственного блика AI-мастера.

**Сделано.** Constants подключены: `band = along * lateral * mask`, где
`lateral` - плато до `_TIP_RELIGHT_LATERAL` (2.6 ед.) с растушёвкой на
`_TIP_RELIGHT_LATERAL_FALLOFF` (1.2 ед.). Замерено на Wait: клин имеет
ширину 3.45 ед. на `t=0.14` (где зона полной замены заканчивается после
правки п.2) - меньше, чем 3.8 ед. общего охвата, значит защита от
расслоения не пострадала; на `t=0.30` клин уже 6.5 ед., и внешняя половина
с каждой стороны возвращается к цвету мастера. Сравнение всех шести клиньев
с `f1769ec` на одинаковом кропе - кромка совпадает по характеру.
`delta_e` при этом улучшился (лишняя яркая полоса как раз и уводила цвет от
авторского). `tools/selftest.py` проходит, гейт по `metrics-baseline.json` -
без новых регрессий.

## 13. Кончики UpArrow/Wait/AppStarting в тени (2026-08-XX)

Продолжение п.7/п.2: после замены желоба на ступеньку (см. выше) владелец
заметил, что вершины всё ещё темнее, чем должны - «кончики всё ещё в тени?».

**Измерено, а не на глаз.** Сравнение с `f1769ec` (для этих трёх курсоров -
это цвет AI-мастера без всякой аналитической подсветки, `_tip_relight` тогда
не существовал) показало: у `f1769ec` вершина - почти ровная заливка светом,
без выраженного тёмного участка. Причина в две части:

1. **`width` сходился к полу раньше, чем `diff`.** У ступеньки `width =
   max(edge*taper_frac, 2/L)` упирается в пиксельный пол почти сразу от
   вершины, а `diff` (размах контраста) оставался постоянным 85 на всём
   протяжении. У самой вершины клин уже шире пола (0.6 логической единицы
   полуширины на t=0.05 против пола ~0.125), поэтому обе половины сразу
   насыщались в ±42.5 от опорного уровня - жёсткая грань 50/50 без
   схождения. Починено для всех шести клиньев разом: `diff` теперь тоже
   умножается на `taper_frac`, так что расхождение граней растёт от нуля у
   самой вершины, а не начинается сразу на максимуме.
2. **85 было позаимствовано у Arrow/Hand/Arrow_Down, где под ним есть
   реальная (пусть и слабая) складка** (см. запись про `diff=85` выше в
   `_TROUGH_PARAMS`). У UpArrow/Wait/AppStarting складки в этой зоне нет
   вовсе (см. п.7 выше и замеры сечения в этом же файле) - 85 рисовало
   контраст с нуля, и именно эта половина читалась как тень. Уменьшено до
   `diff=18` только для этой тройки - достаточно, чтобы вершина сходилась
   линией, а не пятном, и заметно ближе по яркости к `f1769ec`.

**Цена, названа вслух.** `tip_contrast` просел у всех шести клиньев
(Arrow 0.215→0.119, Hand 0.220→0.065 и т.д.) - это тот же эффект, что
правило 2 уже описывало: метрика меряет перепад с фоном и падает, когда
вершина перестаёт быть resисточником максимального контраста с первого же
пикселя. Здесь это ожидаемо и желательно - именно от «максимального
контраста с первого пикселя» и был дефект. Крак (п.2) не вернулся - замена
полосы не зависит от величины `diff`, только его яркость. `tools/selftest.py`
проходит; гейт даёт 10 отказов вместо 6, все по `tip_contrast` на клиньях -
ратчет не пересниман, решение о принятии этой цены за владельцем.

## 14. Владелец предпочёл `58a28b72` - изоляция кумулятивного размытия (2026-08-XX)

Владелец указал на конкретный старый коммит (`58a28b72`, 27 июля - ранняя
точка, ещё до `_tip_relight`/`_match_author_level`/новой альфа-подсистемы,
618 строк `hybrid.py`) как эталон и назвал три претензии к текущему рендеру:
паразитные слои вокруг кромки, недосведённые/размытые кончики, кривые хорды.

**Первая проверка (силуэт) - чисто.** Альфа-канал на хвостовом кончике Arrow
идентичен `58a28b72` пиксель-в-пиксель. Значит дело не в геометрии/трассировке,
а целиком в цвете.

**Изоляция, доведённая до конца.** Шесть кандидатов из истории правок между
`58a28b72` и текущим деревом: `_match_author_level`, `_tip_relight`,
`_sat_match`, `_hide_ghost`, `_declutter_hue_outliers`,
`_declutter_engraved_detail`. По одной каждая почти не двигала разницу с
эталоном на кропе хвостового кончика Arrow (12.19 → ~10.9-12.2 уровня при
отключении любой одной). Но при отключении **всех шести разом** разница
упала до 0.11 - практически до нуля. `_master()` (сырой AI-мастер) при этом
замерен побайтово идентичным в обеих версиях. Вывод: ни одна стадия не
сломана - каждая чинит свой отдельно зарепорченный дефект (расслоение,
дрейф цвета, синяя крапинка на острие Wait, съезжающая насыщенность,
цветной шум за кадром анимации), но вместе они **кумулятивно** размывают
кромку и кончики сильнее, чем каждая по отдельности.

**Решение владельца: ослабить все стадии пропорционально**, а не выбирать
победителя. Введён `_LEGACY_TEMPER` - общий рубильник, `_temper(before,
after, k)` подмешивает исходный (домастерный) цвет обратно в результат
правки. Численно подобрано `k=0.5` - половина разницы с эталоном закрыта
(12.19 → 6.15).

**Область сужена по факту замера, не вслепую.** Все шесть на 0.5 сразу
пробили гейт: `ghost_rgb` (цветовое рассогласование невидимых пикселей
между кадрами интерполяции, ради которого и написан `_hide_ghost`) взлетел
до 14-38 уровней при допуске 0.5 - то есть вернулся именно тот дефект,
который эта стадия должна не допускать. `_declutter_hue_outliers`/
`_declutter_engraved_detail` идут в `_master_raw` для **любого** курсора, не
только для шести клиньев - их ослабление вернуло дефекты, не имеющие
отношения к жалобе владельца: у Help `fold_gap` 0.125→0.5 (размылась
гравировка вопросительного знака), у SizeAll `fold_jag` 53.7→119.8. Проверено
отдельно: три оставшихся (`_match_author_level`, `_tip_relight`,
`_sat_match`) в одиночку на 0.5 дают почти тот же выигрыш на кропе Arrow
(5.82 из 6.15) - остальные три вносили меньше процента разницы туда, где
смотрел владелец, и стоили дороже в другом месте. Оставлены на полную силу.

**Итог.** `_LEGACY_TEMPER = 0.5` применён только к `_match_author_level`,
`_tip_relight`, `_sat_match` - именно то, что специфично для шести
клиньев-«самолётиков» (`_WEDGE_TIPS`) и не задевает остальные десять
курсоров через общий код `_master_raw`. Крак на Wait/UpArrow/AppStarting
(п.2 этой сессии) не вернулся - его чинит сама замена полосы `_tip_relight`,
не сила коррекции уровня. `tools/selftest.py` проходит. Гейт: 18 отказов
вместо 6 на начало сессии, почти все по `tip_contrast` (уже известная,
принятая цена - метрика хвалит резкий контраст с фона) и по `fold_jag`/
`fold_luma_step` на клиньях (складка снова чуть менее прямая - плата за
ослабленный `_match_author_level`, который её и выпрямлял). `ghost_rgb` в
допуске у всех.


**Поправка после первой сборки.** Формулировка выше была неточной: `_temper`
проверял только имя стадии, не имя курсора, а `_sat_match` вызывается для
любого насыщенного курсора (`orig_sat >= 0.035`), не только для шести
клиньев. На первой пересборке это дало 17 отказов вместо 16 и утечку на `NO`
(`fold_luma_step`/`fold_jag`/`fold_wander` выросли у курсора, который в
сравнении с `58a28b72` вообще не участвовал). Починено: `_temper(before,
after, name, k=...)` теперь возвращает `after` без изменений, если
`name not in _WEDGE_TIPS`. После фикса `NO` и `Wait` вернулись в список
принятого долга (не FAIL), гейт: 16 отказов, все на шести клиньях.
`temporal_fold` у Hand сдвинулся 1.001->1.023 (порог 1.0) - в пределах шума,
того же рода побочный эффект, что и `fold_jag`/`fold_luma_step` выше, не
отдельный дефект.

---

## 15. `tip_contrast` меряет вершину против отвергнутого эталона (2026-08-12)

**Что оказалось.** Семь отказов гейта по `tip_contrast` читались как «принятая
цена temper'а». Это неверно: temper их не ронял. Метрика берёт **минимум по
остриям**, и связывающим у всех шести клиньев всегда оказывается одно - вершина
(3.1, 3.0). Два остальных острия у нас с большим запасом лучше авторских:

| курсор | вершина, наш | вершина, автор | остриё 29,14 | остриё 14,29 |
|---|---|---|---|---|
| Arrow | 0.126 | 0.084 | 0.322 / 0.174 | 0.347 / 0.205 |
| Arrow_Down | 0.086 | 0.088 | 0.282 / 0.179 | 0.345 / 0.207 |
| Hand | 0.068 | 0.084 | 0.243 / 0.174 | 0.255 / 0.205 |
| UpArrow | 0.044 | 0.085 | 0.229 / 0.178 | 0.182 / 0.204 |

**Почему вершина проседает.** Не альфа - она совпадает с авторской (медиана
0.00/0.01, максимум 0.76/0.77). Только яркость: авторская вершина тёмная, ~98
уровней, наша ~130, то есть ровно уровень серого фона (128), на котором она и
исчезает. У Arrow в диске остались тёмные пиксели (минимум 85), потому он и
проходит; у Hand минимум 120, у UpArrow 113.

**Это регрессия, а не устаревший ратчет.** Проверено подменой рендера: файл
`hybrid.py` с коммита `02363b2` (тот, на котором снята база) воспроизводит
записанные числа один в один - 0.215 / 0.165 / 0.220 / 0.061. Значит вершину
забрал коммит `5a5f363`, и забрал крупно: Hand 0.220 -> 0.062.

Направление правки при этом **не то**, что записано в DEAD_ENDS как отвергнутое.
Там отвергнуто «тянуть вершину к авторским 98 уровням», то есть вниз. Прежние
0.220 были выше авторских 0.084 - речь о возврате своего же значения, не о
воспроизведении авторской темноты.

**Изоляция, доведена до функции.** Подмена целых функций со старого файла при
прочих новых:

| подменено | Arrow | Arrow_Down | Hand | UpArrow |
|---|---|---|---|---|
| ничего (сейчас) | 0.129 | 0.090 | 0.062 | 0.049 |
| `_match_author_level` | 0.129 | 0.090 | 0.062 | 0.049 |
| `_tip_relight` | 0.109 | 0.092 | **0.120** | **0.099** |

`_match_author_level` чист. Виновата переписка `_tip_relight` - но у Hand и
UpArrow причины **разные**, и это не видно, пока меришь их одной метрикой.

**Hand - две правки в теле функции, вклад сложился.** Точечный откат каждой
(строка за строкой, не через константы - обнуление `taper` меняет заодно `width`
и `hw` и меряет не то):

| откачено | Hand | Arrow | Arrow_Down | UpArrow |
|---|---|---|---|---|
| ничего | 0.062 | 0.129 | 0.090 | 0.049 |
| `taper_frac` из строки `shade` | 0.086 | 0.107 | 0.095 | 0.049 |
| якорь `_band_level` -> скаляр | 0.091 | 0.129 | 0.090 | 0.049 |
| всё три | 0.121 | 0.109 | 0.092 | 0.047 |

Сходится с подменой функции целиком (0.120). Обе правки owner-visible:
`taper_frac` введён по жалобе от 2026-08-09 на тупую смещённую «вторую точку»
вместо схождения в угол, полевой якорь - ради живости вершины на анимации (диск
вершины качался 4.6 уровня против авторских 13.8, см. комментарий у самой
строки). Заметь: якорь включается только для `INTERP`, и там же в коде записано,
что на статичных он контраст роняет (Arrow 0.215 -> 0.157 замерено).

**UpArrow - вообще не тело функции.** Ни одна из трёх правок его не двигает
(0.049 / 0.049 / 0.046 / 0.047). Двигает смена параметров: `5a5f363` перевёл
UpArrow, Wait и AppStarting с **жёлоба** (`hw0/hw_grow/depth=32/along_flat`) на
**ступеньку** (`diff=18, edge=0.12`) - это и есть «замена полосы `_tip_relight`»,
которой в п.14 чинили крак. Дело в форме, не в силе: `diff` 18 -> 28 -> 40 -> 55
даёт 0.049 / 0.049 / 0.049 / 0.048, то есть ничего.

**Вывод: бесплатной починки здесь нет.** Каждая часть просадки - цена другой,
одобренной правки: схождение в угол, живость вершины на анимации, крак на трёх
сheen-курсорах. Вернуть контраст = заново открыть одну из этих трёх жалоб.
Решение владельца, не принято. Пока не принято - четыре отказа гейта держатся на
этом, и правильнее завести их в `metrics-known-issues.json` со ссылкой сюда, чем
чинить.

---

## 16. `_deburr` раздувал силуэт на мелких размерах (2026-08-12, починено)

**Как нашли.** `scale_drift` не проходил у пятнадцати курсоров из шестнадцати, а
`density_%` у всех шестнадцати - две трети всего долга, и к ним ни разу не
подходили. Разложение по ступеням лестницы показало, что дрейф почти целиком
даёт одна ступень, 32px. Дальше по конвейеру:

| курсор | маска | маска x up_alpha | после `_deburr` |
|---|---|---|---|
| Help | 0.024 | 0.020 | **0.545** |
| Arrow | 0.020 | 0.016 | **0.141** |
| SizeWE | 0.053 | 0.039 | **0.231** |
| Cross | 0.101 | 0.075 | **0.163** |

Трассировка масштабно устойчива, произведение с альфа-мастером тоже (всё под
порогом 0.10). Весь дрейф вносила одна стадия. `_round_hole` следом не добавляет
ничего.

**Дефект.** `r = max(1, int(round(_DEBURR_LOGICAL * size / V.LOGICAL)))`.
`_DEBURR_LOGICAL` = 0.14 логической единицы, но на мелких размерах округление
даёт ноль, и пол подставляет пиксель - а пиксель это не постоянная доля курсора:
на 32px он равен целой логической единице, в семь раз больше разрешённого
заусенца. Серое замыкание такого радиуса раздувало силуэт. Ровно правило 1 из
списка выведенных из провалов: величина, подогнанная под текущий размер.

**Правка.** Пол убран, при `r < 1` стадия возвращает альфу нетронутой. Затронуты
только ступени ниже ~114px; на 512, где заусенец у талии SizeAll и виден,
`r = 2` и ничего не изменилось.

**Результат.** `scale_drift` ушёл из долга целиком, все пятнадцать: Help
0.545 -> 0.037, Arrow 0.141 -> 0.011, SizeWE 0.231 -> 0.034. Долг 69 -> 53,
регрессий по-прежнему 14, список тот же, `selftest` проходит.

**Побочно починилось дрожание силуэта.** `morph_iou_min` у Handwriting
0.385 -> 0.448, выше авторских 0.437 - это и есть «vobling» из TODO, который
висел в шапке README. У NO 0.349 при авторских 0.344, проходит. Стоит
пересмотреть комментарий у `_EDGE_SHADOW_CURSORS` ([hybrid.py:1814](hybrid.py)):
Handwriting и NO исключили оттуда именно из-за упавшего `morph_iou_min`, и это
основание больше не действует. Не проверялось.

**Осталось от этого блока.** `density_%` не сдвинулся: он меряет `_up_alpha`
напрямую, до `_deburr`, так что это отдельная причина. Худшие - Cross 6.16%,
IBeam 4.98%, SizeAll 3.55% при допуске 2.0.

---

## 17. Складка NO: мерился запрещающий знак, а не стекло (2026-08-12, починено)

**Симптом.** NO держал худшие числа складки во всём наборе: `fold_jag` 193.2,
`fold_luma_step` 68.2, `fold_wander` 2.40 - в разы хуже любого другого курсора.

**Что оказалось.** Трек складки шёл целиком по красному знаку. Замер вдоль
трека: насыщенность 1.00 на всех точках, тогда как у стекла рядом 0.01. Знак
тёмный по яркости, а трекер берёт минимум яркости в окне - вот он и выигрывал.
Худший кадр - 3, последний, чью складку объявляет `CURSOR_TOPOLOGY` (`[True]*4 +
[False]*7`), то есть ровно тот, где знак уже наполз на складку, но она ещё
числится существующей. Разрешалось при этом 8 строк из сорока с лишним.

**Правка в измерении, не в рендере.** `_without_overlay` в `tools/analyze.py`
выводит пиксели наложения из-под `argmin`. Порог относительный, иначе он задел бы
насыщенные курсоры: `sat > max(0.35, 8 x медиана по силуэту)`. Замерено -
у Wait, UpArrow и AppStarting медиана 0.40..0.56, порог уходит выше 1.0 и не
исключается ничего; у NO на кадрах со складкой медиана 0.006..0.015, работает пол
0.35, и его перешагивает только знак; у серых курсоров выше пола нет ни одного
пикселя. Плюс строка отбрасывается, если всё окно оказалось наложением.

**Результат.** NO: `fold_jag` 193.2 -> 14.2, `fold_luma_step` 68.2 -> 1.77,
`fold_wander` 2.40 -> 0.01, разрешённых строк 8 -> 39, худший кадр 3 -> 0.
Регрессий по-прежнему 14, список тот же, на остальные пятнадцать курсоров не
протекло, `selftest` проходит.

**Долг не уменьшился и не должен был.** Пороги складки абсолютные (0.0), так что
NO остаётся в долге - изменились величины, а не факт промаха. Зато база метрик
теперь держит для NO числа, которых рендер больше не даёт: их стоит переснять
вместе с остальным решением по базе.

---

## 18. Полоса на кромке стирала саму складку (2026-08-12, починено)

**Что было.** После `5a5f363` в гейте висели четыре регрессии складки, и все
четыре давал `_edge_shadow_declutter`. Проверено отключением стадии: каждая
возвращается ровно к базовому числу.

| | полоса включена | полоса выключена | база |
|---|---|---|---|
| Wait `fold_gap` | 1.375 | 0.875 | 0.875 |
| Arrow `fold_luma_step` | 1.333 | 1.083 | 1.083 |
| Arrow_Down `fold_luma_step` | 9.850 | 9.650 | 9.650 |
| AppStarting `fold_wander` | 0.210 | 0.204 | 0.207 |

**Механизм.** Полоса чистит кольцо 0.7..1.9 единицы от силуэта максимум-фильтром,
а там, где складка подходит к кромке (выемка хвоста у Wait, вырез у Arrow), она
попадает в это кольцо. Максимум-фильтр не отличает одну тёмную вещь от другой и
поднимает складку вместе с паразитной линией мастера. Складочные метрики читают
именно её провал - отсюда и разрыв линии, и сплющенное сечение.

**Отвергнутые подходы.** Сужение `_EDGE_SHADOW_D_HI` (1.9 -> 1.5 -> 1.2) не
помогает: Wait `fold_gap` держится 1.375, Arrow `fold_luma_step` 1.333, а
остальное ползёт вниз. Вредит не внешний край кольца, а его внутренняя часть,
где складка и лежит. Отключать стадию нельзя - у AppStarting `fold_jag`
взлетает 71 -> 118, у Help `fold_luma_step` 14 -> 22.

**Правка.** `_fold_keepout`: полоса не применяется в 0.8 логической единицы по
обе стороны хорды, с плавным возвратом за 0.3. Паразитная линия идёт параллельно
контуру, а не вдоль хорды, поэтому цена почти нулевая - доля пикселей ниже 70
уровней на 512 по серому меняется 0.50% -> 0.56% у Arrow, 7.79% -> 7.82% у Wait,
у Hand никак. Курсоры без хорды не затронуты вовсе.

**Результат.** Регрессий 14 -> 10. Единственная новая - Wait `fold_jag` 44.750
против базовых 44.417, треть уровня, шум. `selftest` проходит.

---

## 19. Temper: две стадии из трёх платили не за свой дефект (2026-08-12)

Продолжение п.14. `_LEGACY_TEMPER = 0.5` был применён к трём стадиям разом,
потому что их изолировали вместе - не потому, что они стоят одинаково. Померено
по отдельности, на том же кропе Arrow, что и в п.14 (средняя разница в уровнях
при переводе стадии с 0.5 на 1.0):

| стадия | угол 29,14 | угол 14,29 | вершина 3.1,3.0 | что даёт полная сила |
|---|---|---|---|---|
| `level` | 0.06 | 0.07 | 0.25 | складка: `fold_jag` уходит с Arrow/Hand/Arrow_Down |
| `relight` | **0.00** | **0.00** | 4.57 | UpArrow `tip_contrast` закрыт, Hand `temporal_fold` 1.022 -> 1.005, Wait `fold_jag` снят |
| `sat` | 0.00 | 0.00 | 0.00 | ничего: Arrow серый, `_sat_match` к нему не применяется |

Жалоба владельца в п.14 была про **угол хвоста**. Две стадии из трёх в этом
углу не стоят ничего или почти ничего, а темперировались ради него - и платили
за это складкой и контрастом. Обе возвращены на полную силу.

`sat` темперированным остаётся. В углах он бесплатен, но на полной силе
возвращает два отказа складки: Arrow_Down `fold_jag` 84.4 -> 89.1, Wait
44.4 -> 44.8. Это единственная из трёх, чей темпер действительно что-то покупает.

**Цена `relight` на полной силе** - вершина: Arrow_Down `tip_contrast`
0.090 -> 0.069, ниже авторских 0.088. Это та же ось, что в п.15, и то же
нерешённое решение владельца. Обмен по счёту выгоден - UpArrow закрывается
полностью (0.049 был вдвое ниже авторских), Arrow_Down уходит ниже автора.

**Итог: регрессий 10 -> 8.**

---

## 20. Остаток разобран до конца (2026-08-12)

Три оставшиеся регрессии, не связанные с вершиной, диагностированы. Все три
чинятся только ослаблением гейта или отменой одобренной правки.

**`Hand temporal_fold` 1.005 при пороге 1.000.** Источник - та же переписка
`_tip_relight` из `5a5f363`: при темпере 0.5 читается 1.022, на полной силе
1.005, базовые 1.001 сняты с версии до переписки. Ни `_deburr`, ни
`_fold_keepout`, ни полоса на кромке на это число не влияют (проверено, все
дают 1.0051).

Порог трогать нельзя, хотя соблазн есть: по этой шкале **автор даёт 1.195** у
Hand, 1.108 у Wait, 1.162 у AppStarting, то есть все наши три глаже авторских, а
Hand глаже на 16%. Но абсолютный порог для интерполированных выбран осознанно
(силуэт заморожен, значит спешка - наша), и он достижим: Wait 0.973 и
AppStarting 0.983 его берут. Мимо идёт один Hand и на полпроцента.

**`AppStarting fold_luma_step` 6.633 против 5.867.** Расширение `_FOLD_KEEPOUT`
0.8 -> 1.3 чинит её (6.633 -> 5.600), но возвращает `fold_jag` с 76.5 к 117.6
при базовых 117.567 - одна регрессия меняется на другую, и теряется выигрыш в
41 пункт. Оставлено на 0.8.

**`AppStarting jitter_unmeasured`.** Формулировка в `metrics-known-issues.json`
неточна: дело не только в глубине. `inner_jitter` ищет складку в окне 0.5
единицы вокруг **прямой** хорды, а складка AppStarting от хорды отгибается
(п.1: нарастает от вершины к выемке). Hand с прямой складкой даёт 41 строку,
Wait 27-29, AppStarting 1-2 при нужных десяти. Опора профилем по строкам, одна
на цикл, поднимает до 5 - и портит Wait (p95 0.026 -> 0.198). Глубина при этом
3 уровня против порога 6.0, порог перешагивают 5 строк из 37. Опускать
`_FOLD_DEPTH` значит ослабить гейт всем шестнадцати ради одного числа.

---

## 21. Решение по вершине принято по замеру (2026-08-12)

**Поправка к прежней формулировке.** В ходе сессии было сказано, что тёмный
акцент у вершины и обводка на кромке - одно и то же, и решение подавалось как
выбор между ними. Это неверно, проверено напрямую: `relight` на обводку не
влияет вовсе. При 0.5 и при 1.0 доля пикселей ниже 70 уровней держится
одинаковой (Arrow 0.56%, Arrow_Down 0.24%, Hand 0.15%, UpArrow 0.10%), тёмнейший
пиксель тот же (22/34/25/26). Обводка сидит на 0.68 единицы от кромки и вшита в
мастер (п. в DEAD_ENDS), вершина - отдельная величина. Выбора между ними нет.

**Опора решения - автор**, как и для всех точечных метрик в этом файле:

| курсор | автор | relight 0.5 | relight 1.0 | принято |
|---|---|---|---|---|
| Arrow | 0.084 | 0.129 | 0.119 | 1.0, проходит |
| Arrow_Down | 0.088 | 0.091 | 0.069 | **0.5**, проходит |
| Hand | 0.084 | 0.062 | 0.065 | 1.0, ближе |
| UpArrow | 0.085 | 0.049 | 0.074 | 1.0, ближе |

Полная сила выгоднее по сумме недобора (0.049 против 0.058) и закрывает
`Hand temporal_fold` и `Wait fold_jag`. Единственный, кто от неё терял проходной
балл, - Arrow_Down; ему выставлено адресное исключение `_TEMPER_PER_CURSOR`.
Цена исключения 0.3 пункта `fold_jag` (84.1 -> 84.4 при базовых 84.467).

Hand и UpArrow мимо автора при любой силе - это остаток п.15, и он упирается в
`taper_frac` и полевой якорь, обе правки owner-visible. Не трогается.

**Регрессий 8 -> 7.**

---

## 22. База метрик переснята (2026-08-13)

Старая база описывала рендер до этой сессии и активно вводила в заблуждение:
часть «регрессий» была ценой уже принятых правок, а часть - сравнением с
состоянием, которого в дереве больше нет. Переснята на `A.LADDER`, той же
лестнице, что использует `build.check_quality` (правило 3).

Гейт на новой базе: **один отказ**, `AppStarting jitter_unmeasured` - он
единственный, что числится в `metrics-known-issues.json`, и он не несёт числа,
поэтому поднимается на каждом прогоне.

**Что изменилось против старой базы.**

| | было | стало |
|---|---|---|
| `scale_drift`, все шестнадцать | 0.101..0.545 | 0.011..0.065 |
| `tip_contrast` Arrow / Arrow_Down / Hand | 0.215 / 0.165 / 0.220 | 0.129 / 0.091 / 0.062 |
| `tip_convergence` AppStarting / Wait / Arrow_Down | 1.50 / 1.60 / 1.30 | 2.40 / 2.00 / 2.00 |

Первая строка - починка пола радиуса в `_deburr` (п.16). Вторая - цена
переписки `_tip_relight`, разобранная в п.15 и п.19.

Третья требует оговорки. `tip_convergence` вырос у трёх курсоров, и виновата
правка `_EDGE_SHADOW_D_LO` 0.7 -> 0.45, сделанная как раз ради сведения
кончиков. Проверено на зумленных кропах вершин AppStarting и Wait при обеих
настройках: на 0.45 тёмная полоса у кромки **уже**, а светлая грань подходит к
острию **ближе**, то есть по глазу правка делает ровно то, ради чего сделана.
Метрика в собственном комментарии в `gate()` объявлена негодной для вердикта -
обе пробованные формулы засчитывают размытый эталон 2006 года сведённым везде,
и там прямо сказано судить по зумленному рендеру, а число держать для показа
движения между прогонами. Здесь оно и показало движение; вердикт по картинке.

---

## 23. Три претензии владельца от 2026-08-13 (в работу)

Записаны как есть, с первичным замером. Порядок - от простого к сложному.

### 23.1 Левый нижний угол на серых курсорах выглядит сплошным

**Что видно (формулировка владельца).** У серых курсоров левый нижний угол
**не имеет кромки**, которая есть у всех остальных, - он получается сплошным.
На цветных (UpArrow, Wait, AppStarting) хвостовой шип обведён кромкой и внутри
читается как прозрачное стекло с двумя гранями; на серых (Arrow, Hand, Help)
кромки нет и шип выглядит одной сплошной белёсой массой. Отрисовано на кропах
512px по серому фону, шесть курсоров рядом.

**Поправка владельца.** Кромка идёт не по всему контуру: **внутренняя заливка
смещена**, и с одной стороны она перекрывает кромку, отчего та не видна.

**Измерено.** Зазор между контуром и светлой заливкой в строках хвостового шипа
(y от 26 до 30 логических), медиана по строкам, логические единицы:

| курсор | слева | справа | перекос |
|---|---|---|---|
| UpArrow | 1.31 | 0.00 | 1.31 |
| Arrow | 1.22 | 0.19 | 1.03 |
| Hand | 1.22 | 0.22 | 1.00 |
| Wait | 1.06 | 0.31 | 0.75 |
| AppStarting | 1.06 | 0.41 | 0.66 |
| Help | 1.12 | 1.19 | **-0.06** |

Заливка прижата вправо у всех шести клиньев и симметрична у Help. То есть это
не свойство серого стекла: на цветных перекос такой же, просто там кромка
цветная и остаток её слева виден, а на сером - нет.

**Проверено и исключено.** `_edge_shadow_declutter` ни при чём: кроп на чёрном
при `D_LO` 0.45, 0.7 и при выключенной стадии одинаков. Гипотеза «кромка есть,
но серая на сером не видна» тоже отвергнута - на чёрном фоне у UpArrow кромка
читается, у Arrow и Hand её нет.

**У автора перекоса нет.** Численно на его 32px не мерится - в этих строках
меньше четырёх пикселей плотного стекла, а растягивать его кадр нельзя (та же
недействительная сверка, что `morph_iou_vs_lanczos` в DEAD_ENDS). На кропе по
чёрному видно: у него тёмные пиксели по обе стороны шипа со светлым посередине.

**Куда копать.** Смещение цвета относительно векторной маски - того же рода, что
крен в п.2 и расслоение в п.7, и, вероятно, того же происхождения (мастер).
Проверять на `_master()` до маски: совпадает ли центр тяжести цвета в шипе с
центром маски.

### 23.2 Хорда не сводится в кончик и уходит влево у вершины

**Что измерено.** Верхний конец хорды стоит правильно: до любого следа альфы
0.02 логической единицы, то есть точно в трассированном углу. Но до **плотного**
стекла (альфа выше 0.5) - 0.34 единицы на всех шести клиньях. То есть расходится
не хорда, а стекло: у вершины оно настолько прозрачное, что видимый кончик тупее
трассированного угла и смещён относительно него.

**Значит чинить надо альфу у вершины, а не геометрию хорды.** Это не тот
диагноз, с которого начинали смотреть, и он снимает подозрение с `_fold_chord`.

**Сверено с автором (2026-08-13).** Тот же профиль вдоль хорды на его 32px
оригинале (Arrow): альфа 0.31 до t=0.63, скачок к 1.00 на t=0.84, то есть
плотное стекло у него начинается на 0.84 логической единицы от острия. У нас -
на 0.42, вдвое ближе к вершине, чем у автора. Разрыв, который зафиксирован
выше (0.34 единицы между трассированным углом и плотным стеклом), - не
отставание от авторского замысла, а собственный запас: полупрозрачный кончик
стекла есть и у автора, просто у него он ещё шире (0.84 единицы на его
координатной сетке против наших 0.42). Числа не полностью сравнимы (у автора
32px, дискретность в логическую единицу с лишним - переход одним пикселем),
но направление ясное: наш рендер сходится к плотности **ближе** к остроте, чем
исходник, не дальше.

**Вывод.** Правки не требуется - визуальное расхождение хорды, которое видел
владелец, не объясняется недостатком плотности альфы у вершины (это было
рабочей гипотезой, она не подтвердилась при сверке с автором). Снято с
очереди на исправление; причина смещения хорды, если оно всё ещё видно на
глаз, - в другом месте (кандидат - тот же сдвиг цвета относительно контура из
23.1/23.4, поскольку хорда проведена по геометрии, а не по цвету, и то, что
"видно глазом", - это цветная граница, не альфа).

### 23.3 Линия отходит от хорды у хвоста

**Что измерено.** Отклонение найденной линии от прямой хорды по четвертям
хорды, 512px, логические единицы:

| курсор | 3-я четверть | 4-я четверть | максимум |
|---|---|---|---|
| Arrow | 0.35 | 0.41 | 1.19 |
| UpArrow | 0.12 | 0.48 | 1.31 |
| Wait | - | 0.48 | 1.15 |
| Hand | 0.36 | 0.43 | 0.92 |
| AppStarting | - | 0.49 | 1.16 |
| Arrow_Down | 0.28 | 0.32 | 1.01 |

В первых двух четвертях (t < 0.5) линия не находится **вообще** ни на одном
курсоре - складки у вершины там нет, что сходится с п.1 и объясняет, почему она
не доходит до кончика.

Отклонение сосредоточено у хвоста и достигает единицы с лишним. Прежний вывод
п.2 («кривизны в пути нет, отклонение 0.24-0.29») снят: он мерялся до правок
этой сессии и по всей длине сразу, что усредняло хвостовой загиб.

**Уточнено 2026-08-13. Загиб есть, он в последних 5% хорды.** Сначала я
померил t = 0.45..0.88, увидел прямую и заявил, что загиба нет - владелец
возразил, что видит его. Возражение верное: окно замера кончалось раньше
самого загиба. На основном ходу линия правда прямая (отклонение от прямой,
подогнанной на t = 0.40..0.90: Arrow 0.08, Hand 0.07, Arrow_Down 0.08, Wait
0.13, UpArrow 0.12, AppStarting 0.25 логической единицы - это 1-4 экранных
пикселя при 512). Ломается она у выемки.

Отклонение на t = 0.98, рендер против того же замера на 32px автора
(Lanczos до 512, его собственная форма без всякого AI):

| курсор | рендер | автор |
|---|---|---|
| Hand | -1.10 | -1.00 |
| Arrow_Down | 0.72 | -0.74 |
| Arrow | 1.44 | -1.00 |
| Wait | -1.44 | -0.22 |
| AppStarting | -1.62 | -0.22 |
| UpArrow | -1.62 | -0.74 |

(знак у Arrow/Arrow_Down переворачивается: трекер там цепляется за
противоположную сторону выемки, важна величина.)

Hand и Arrow_Down совпадают с автором. **Wait, AppStarting и UpArrow уводит в
шесть-семь раз сильнее автора** - те же три курсора, что и с выдуманной
складкой у вершины.

Откуда: не из рендера. Отключал по одной `_match_author_level`, `_tip_relight`,
`_tip_realign`, `_edge_shadow_declutter`, `_notch_declutter`, `_freeze_lines`, и
все шесть разом - кроп выемки при 512 не меняется ни в одном варианте, и
профиль отклонения на мастере `src/ai512` совпадает с рендером до сотых
(`+0.26 +0.22 ... -0.28 ... -1.62` против `... -1.44`). Загиб приезжает из
апскейла, как и дефект вершины у той же тройки.

Ещё видно на кропе: хорда не кончается там, где кончается складка. Складка
доходит до верха выемки и поворачивает вправо вдоль кромки хвостового шипа, а
хорда идёт дальше вниз-вправо, в тёмный шип. Точка привязки хорды - самое
глубокое отступление контура от своей выпуклой оболочки (`_fold_chord`), и это
не то же самое, что конец видимой складки.

**Замер, которым это ловится** (старый не годился, см. ниже): самый крутой
перепад яркости поперёк хорды, шаг 0.02 единицы, только альфа > 0.5, станции
до t = 0.98 включительно. Точки наносятся зелёным прямо на рендер - видно, на
чём именно держится трекер.

**Почему старая таблица давала загиб везде.** Она искала в полосе поперёк
хорды **самый тёмный** отсчёт. Складка здесь не
канавка, а ступенька (см. длинный комментарий у `_TROUGH_PARAMS`), поэтому
самый тёмный отсчёт лежит не на переходе, а на дальней стороне тёмной грани,
и там, где клин уже полосы поиска, он вообще соскакивает на внешнюю фаску.
Отследил вместо него **самый крутой перепад** яркости поперёк хорды (шаг
0.05 логической единицы, 512px, только пиксели с альфой > 0.5) и нанёс
найденные точки зелёным прямо на рендер: на участке, где складка реально
существует, точки ложатся ровно на видимую границу тёмной и светлой граней.

Профиль отклонения, t = 0.45..0.88 (там, где складка есть):

| курсор | отклонения | среднее | разброс |
|---|---|---|---|
| Arrow | +0.20 +0.15 +0.25 +0.30 +0.30 +0.30 +0.30 +0.30 +0.25 | +0.26 | 0.15 |
| Hand | +0.20 +0.15 +0.25 +0.30 +0.30 +0.30 +0.30 +0.30 +0.35 | +0.27 | 0.20 |
| Arrow_Down | +0.10 +0.15 +0.20 +0.20 +0.20 +0.25 +0.20 +0.20 +0.15 | +0.18 | 0.15 |
| Wait | +0.05 +0.15 +0.20 +0.25 +0.25 +0.20 +0.25 +0.20 +0.15 | +0.19 | 0.20 |
| AppStarting | +0.05 +0.10 +0.10 +0.10 +0.10 +0.20 +0.15 +0.15 +0.05 | +0.11 | 0.15 |
| UpArrow | +0.05 +0.05 +0.05 +0.00 +0.05 +0.05 +0.05 +0.00 -0.10 | +0.02 | 0.15 |

На этом участке линия идёт прямо, с постоянным боковым сдвигом, и эти средние
совпадают с уже замеренными константами `_TIP_REALIGN` (Arrow 0.27, Hand 0.25,
Wait 0.23, Arrow_Down 0.15, UpArrow/AppStarting 0.05). Один и тот же сдвиг
мастера по всей длине складки, исправляется он сейчас только на t = 0.2..0.6.
Расширять `_TIP_REALIGN_END` за 0.6 смысла нет: после 0.6 синтетическая линия
`_tip_relight` не рисуется, конкурировать с собственной складкой мастера
нечему.

При t < 0.42 трекер держится за верхнюю кромку силуэта (клин там уже полосы
поиска) - это ловушка замера, а не дефект стекла. А вот t > 0.94 - настоящий
загиб, разобран выше.

**Починено 2026-08-13: `_notch_from_author`.** Владелец сказал править, правка
сделана.

Ключ, который сдвинул дело: **все авторские рычаги здесь слепы по построению.**
`_match_author_level` сравнивает наш рендер с 32px автора, уронив наш на его
сетку - а на его сетке ошибки нет, она исчезает при даунсемпле. Сеть
перераспределила свет **внутри одного его пикселя**. Значит единственный
эталон мельче его пикселей - обычный ресемпл его же пикселей.

Поперечный срез при t = 0.97 на Wait, 512px:

```
автор  143 131 124 118 109  93  75  62  40  18   3  16  34  50  61  67  74
мастер 181 183 190 190  91  11  17  20  22  23  25  27  26  27  27  30  59
```

У автора одна гладкая долина, у мастера плато, обрыв и чёрный пол, который не
восстанавливается. Та же болезнь, что у вершины UpArrow: пандус превращён в
ступеньку. Глазом это и читается как поворот складки вдоль шипа.

Стадия ставит **потолок на отход от авторской краски** в диске у выемки
(2.0 единицы полностью, 4.0 - до нуля), а не заменяет её: пиксели, которые уже
согласны с автором, не трогаются, подтягиваются только выбросы. Тянет всем
цветом сразу, не яркостью: сдвиг яркости при чужой цветности дал жёлто-зелёную
полосу по границе, умножение - красную на почти чёрном.

Включена только для Wait/AppStarting/UpArrow - у Hand/Arrow_Down/Arrow поворот
совпадает с авторским (таблица выше), и на всех шести правка смывает у Arrow
настоящую авторскую складку выемки.

**Чем заплачено, чтобы не переоткрывать.** Трекер складки берёт самый тёмный
внутренний пиксель в строке, а загиб тёмный - то есть часть того, что он
отслеживал, была дефектом. При 256: Wait разрешался на t = 0.618..0.969, стал
0.618..0.844 (`fold_gap` 0.875 -> 1.875), а AppStarting, у которого **всё**
шестистрочное показание сидело на t = 0.852..0.969, не разрешается больше ни на
одном размере (`fold_unmeasured`, занесено в `metrics-known-issues.json` с
разбором). Взамен два стоявших провала Wait закрыты: `fold_luma_step`
4.667 -> 2.333, `fold_jag` 44.0 -> 8.8; UpArrow 30.3 -> 1.0 и 98.5 -> 12.8.
Сам автор здесь не меряется ничем: `fold_profile` на его кадрах возвращает
None у всех шести клиньев на всех размерах.

Три более узкие формы пробовал до этой, ни одна не работает - записаны в
DEAD_ENDS.md (защитная полоса вдоль хорды, потолок только на пересвет,
низкочастотная поправка).

База метрик переснята, `check_quality` - `clean, 2 accepted, 59 known debt`.

### 23.4 Идея владельца: разбить рендер на шаги против артефактов и смещений

Записана 2026-08-13. Разбор ниже - не отказ, а уточнение, куда она ведёт.

**Конвейер уже пошаговый**: мастер (`_master`), альфа (`_up_alpha`), векторная
маска (`_mask`), затем именованные стадии цвета. Артефакты берутся не из того,
что шаги слиты, а из того, что **два независимых источника формы не совпадают**:
цвет приходит из AI-апскейла, контур - из трассировки, и договорённости между
ними нет ни на одном шаге.

**Что уже проверено и снимает часть подозрений.** Силуэт совпадает: в п.14
альфа сверена с эталоном пиксель в пиксель, контуры маски и мастера сходятся.
Значит смещена не форма, а светотень внутри неё - в хвостовом шипе у автора
тёмное по обе стороны и светлое посередине, у нас светлое прижато к правой
кромке (замер в 23.1).

**Чего замер НЕ доказал.** Центр яркой заливки смещён вправо на 1.3-2.4
логической единицы относительно центра маски у всех шести клиньев, но это не
улика: освещённая грань и должна быть с одной стороны. Нужен замер, который
отделяет светотень от рассовмещения, - например, положение тёмной кромки по обе
стороны шипа против положения контура маски, а не центры тяжести.

**Куда это ведёт по существу.** Полезный шаг, которого сейчас нет, - явное
**совмещение цвета с трассированным контуром** перед композитом: измерить
смещение освещённой грани относительно маски по каждой стороне и скорректировать
его, а не полагаться на то, что сеть попала в контур. Это ровно то, что в п.7
записано как «вести разделительную линию как объект трассировки», только шире -
привязывать к геометрии не одну линию, а всю светотень у кромок.

**Цена честно.** Это новая стадия, а не настройка, и по опыту DEAD_ENDS правки
такого рода на клиньях отвергались владельцем шесть раз подряд. Прежде чем
писать, стоит сделать замер из абзаца выше и убедиться, что смещение
действительно есть, а не читается таковым из-за светотени.

**Замер сделан (2026-08-13).** Не центр тяжести, а положение тёмной кромки
относительно контура маски по обе стороны хвостового шипа (строки y 26-30
логических, `_master_rgb` против `_mask`, 256px, отрицательное = кромка отступает
внутрь от контура):

| курсор | слева | справа | перекос (слева минус справа) |
|---|---|---|---|
| Arrow | -0.71 | -0.22 | 0.49 |
| Arrow_Down | -0.69 | -0.20 | 0.49 |
| Hand | -0.71 | -0.07 | 0.64 |
| UpArrow | -0.68 | -0.18 | 0.50 |
| Wait | -0.47 | +0.02 | 0.49 |
| AppStarting | -0.46 | +0.11 | 0.57 |

Кромка отступает от контура с обеих сторон (мастер не долетает до векторной
границы вообще нигде - ожидаемо, апскейл размывает), но слева отступ вдвое-втрое
больше, чем справа, и одинаково у всех шести клиньев. Это уже не светотень:
светотень меняла бы яркость кромки, а не то, на каком расстоянии от контура она
стоит. **Рассовмещение цвета относительно контура подтверждено**, величина - около
половины логической единицы вправо на 256px (то есть около 1 единицы на 512px).

**Итог.** Гипотеза 23.4 подтверждена измерением, которое её и должно было
подтвердить или опровергнуть.

**Ресайз исключён (2026-08-13).** Тот же замер на нативных 512px, где
`_master_rgb` отдаёт PNG-мастер без единого вызова `_resize` (`size == anchor`,
код на строке 2119 попадает в `return m_rgb` до ресайза):

| курсор | слева | справа |
|---|---|---|
| Arrow | -0.30 | -0.09 |
| Arrow_Down | -0.29 | -0.09 |
| Hand | -0.14 | +0.11 |
| UpArrow | -0.28 | -0.07 |
| Wait | -0.25 | +0.03 |
| AppStarting | -0.20 | +0.07 |

Перекос уже здесь, в масштабе пропорционален замеру на 256px. Значит дело не в
`_resize`'овском Lanczos-ядре хабитата этой сессии - сдвиг зашит в сам PNG в
`src/ai512/`, то есть в выход сети (Real-ESRGAN/anime_6B апскейл) при первичном
пересчёте, до всего кода в hybrid.py. Однострочной правки сэмплинга здесь нет:
дешёвого источника не нашлось, оба простых кандидата (ресайз, сэмплинг) исключены
измерением.

**Значит остаётся дорогой путь** - явная стадия совмещения цвета с контуром
из абзаца выше - или принять как долг. Учитывая шесть прежних отказов на клиньях
и цену новой стадии, решение по нему за владельцем; пока перекос записан в долг,
не в работу.

### 23.5 `_tip_realign`: складка мастера сведена на хорду (2026-08-13, сделано)

Владелец увидел вживую тот же дефект на кропе с наложенной хордой: складка не
сходится в трассированную вершину, а сворачивает и вливается в левый край
контура примерно на четверти пути. Причина - `_tip_relight` (синтетическая
прямая складка по хорде) подмешивается к цвету AI-мастера всего на 50%
(`_TEMPER_K["relight"]`), а собственная складка мастера лежит не на хорде, а
рядом с ней (тот же сдвиг, что в 23.1/23.4) - на 50%-й смеси она проступает
как самостоятельная кривая линия поверх прямой.

**Перепробовано и отвергнуто как компромисс** (владелец: "никаких
компромиссов"): полная сила смешивания (1.0) убирает паразитную линию, но
разбор синтетической модели `_TROUGH_PARAMS`/`taper` показал, что она
физически не может дать видимую грань такой ширины, как у мастера, - по
геометрии клин слишком узкий у вершины, переход остаётся тонкой линией
впритык к кромке силуэта, неотличимой от антиалиасинга (проверено числами по
пикселям и глазами на чёрном фоне, `diff` до 150 не помогает).

**Настоящая причина и фикс.** Дело не в силе смешивания, а в том, что
собственная складка мастера просто не там, где её ждёт хорда. Замерено
(t=0.35/0.45/0.55 по хорде, поиск самого крутого перепада яркости по
сечению): Arrow 0.25-0.30, Hand 0.25, Wait 0.20-0.25, Arrow_Down 0.10-0.20,
UpArrow/AppStarting <0.1 логической единицы вправо от хорды. Новая функция
`_tip_realign` (hybrid.py) сдвигает пиксели мастера в сторону хорды на эту
величину, в полосе вдоль хорды (нарастает t=0.2→0.35, держится до 0.6),
**до** того как `_tip_relight`/`_temper` их читают - обе стадии получают уже
выровненный материал, поэтому расхождение исчезает при любой силе смешивания,
не только при 1.0.

Первая версия сдвига (ramp от t=0) задела диск `tip_contrast` (1.5 лог.
единицы у вершины) и просадила его вдвое - метрика поймала реальную потерю
резкости в точке. Отодвинул начало нарастания до t=0.2 (замер вообще не
покрывал ближе), и метрика вернулась к прежним значениям: `tip_contrast` не
сдвинулся у Arrow/Arrow_Down/Wait/AppStarting (не в списке изменений против
базы), у Hand/UpArrow остался тем же долгом, что был до правки. Гейт на шести
клиньях: было 14 отказов, стало 3 совсем небольших (`Hand temporal_fold`
+0.001, `Wait fold_luma_step`/`fold_jag` +0.08/+0.27) плюс уже принятый
`AppStarting jitter_unmeasured`.

Проверено визуально на 512px (кропы с хордой) и на 128px (реалистичный
показываемый размер, где раньше излом был хорошо виден) - складка сходится в
вершину, петля у UpArrow заметно меньше.

---

### 23.6 Проверка 23.1 глазами и разбор UpArrow до самого низа (2026-08-13)

**Задача 1 из плана (проверить пять курсоров) - выполнена, дефектов нет.**
Отрендерил Arrow, Hand, Arrow_Down, Wait, AppStarting при 128/256/512, кропы
вершины на светлом и тёмном фоне. На всех пяти остриё сходится в одну точку,
складка одна, параллельной тени рядом нет, смещения влево/вправо нет ни на
одном размере. На 128 точка ступенчатая по пикселям, но это антиалиасинг, а не
раздвоение.

**Задача 2, UpArrow. Дефект локализован числами, а не на глаз.** Профиль по
хорде: максимум яркости в сечении, станции 0.25..6.0 логических единиц от
вершины, финальный рендер 512. Здоровые клинья идут рампой:

```
Arrow       134 137 140 150 159 190 210 213 211 211 ...
Hand        141 141 141 141 148 171 186 189 192 199 ...
Arrow_Down  123 125 127 130 133 142 178 182 195 198 ...
```

UpArrow держит плоскость две единицы и потом прыгает на 60 уровней за одну
станцию - плита с обрывом на конце, что и читается как тупая вершина со
вторым остриём внутри:

```
UpArrow     142 143 145 145 146 145 144 203 204 207 ...
```

**Рампа есть в том, что скормили сети.** Тот же профиль на 128px базе
`_base128` (вход апскейлера) у UpArrow: `115 116 120 136 125 149 160 229`, у
Arrow_Down: `110 111 113 116 114 150 153 210` - форма почти одна. На выходе
сети Arrow_Down её сохранил (`107 108 109 113 140 190 216`), UpArrow потерял
(`112 114 115 117 118 120 121 235`). Сеть одна, вход один по форме, результат
противоположный; отличается только цвет.

**Вариант A (параметры апскейла) проверен и закрыт.** Прогнал 128→512 заново
на трёх разных заполнениях прозрачной зоны: штатный TELEA inpaint, зажим по
ближайшему видимому пикселю, вообще без заполнения. Все три дают ту же плоскую
плиту с точностью до двух уровней (см. DEAD_ENDS.md). Дело не в том, что
окружает вершину, а в том, как сеть читает сам клин. Перегенерация `src/ai512`
с другими параметрами обрезки/паддинга - не тот рычаг, каким казалась.

**Пробовал сдвинуть ступеньку (`_tip_advance`) - отвергнуто владельцем на
глаз, откачено.** Клин - это конус с вершиной в трассированной точке, поэтому
масштабирование мастера относительно этой точки отображает каждую грань саму в
себя: двигается только то, что лежит вдоль оси, то есть ровно та фаска, что
стоит не на месте. Коэффициент 1.35 ставит ступеньку на 1.67 единицы, туда же,
где её начинают здоровые клинья. Профиль стал правильной формы, остальные
курсоры не изменились ни на один уровень.

И вместе с фаской приезжает тёмный кант мастера, который её обрамляет.
Тангенциально он сжимается тем же коэффициентом, поэтому приходит к вершине
уже, плотнее и темнее, и садится на обе грани лита как жёсткая тень, которой
там раньше не было. Владелец увидел её сразу на первом же кропе. Разностная
карта (красный - потемнело): красная линия обнимает синюю область по обеим
сторонам, максимум потемнения 43 уровня. Это тот же обмен, что уже был у
`_draw_tip` и `_match_author_at_tips`: убрали одно, нарисовали другое.

Побочно метрика тоже возражала: `tip_contrast` 0.049 → 0.032 при пороге рачета
0.046. Механизм разобран по пикселям (решающий фон серый, хаки у самой точки
стало на 6 уровней светлее, 111 → 117 при альфе 0.73, то есть ближе к середине
серого), понижение коэффициента не спасает: 1.15/1.20/1.25 дают
0.041/0.040/0.045. Но решило не это, а тень.

**Что остаётся.** Пять рычагов со стороны рендера промахнулись по одной и той
же причине: форма у вершины неправильная в самом мастере, а любое её
перемещение или сглаживание переносит дефект, а не убирает. Апскейл 128→512
как параметрический рычаг закрыт (три заполнения - один результат). Не
пробовано: другая модель весов на одном UpArrow, обучающий/ведущий вход из
вектора, ручная правка `src/ai512/cur__UpArrow__0.png`.

### 23.7 Трещина по кромке у Handwriting и NO (2026-08-13, починено)

Нашлось при обычном просмотре всех шестнадцати курсоров при 128/512 на светлом
и тёмном. У Handwriting и NO по верхней и левой кромке идёт **чёрный волосок**
во всю длину - читается как трещина в стекле, а не как фаска. Рядом Hand, у
которого авторский кадр 0 - тот же рисунок пиксель в пиксель, - гладкий.

Это ровно тот дефект, ради которого написан `_edge_shadow_declutter`, и эти два
курсора были единственными, кто нёс его в полную силу: их выкинули из
`_EDGE_SHADOW_CURSORS` после первого прогона гейта по двум показаниям.
Перемерил оба.

**`morph_iou_min` больше не воспроизводится.** 0.448 со стадией и 0.448 без,
до третьего знака, на каждой паре кадров. Та регрессия принадлежала коду того
времени, не стадии.

**`fold_luma_step` воспроизводится - и это трекер, читающий сам дефект.**
`_fold_track` берёт самый тёмный внутренний пиксель в строке, ложный волосок и
есть самое тёмное в его окне, а `_fold_keepout` защищает полосу лишь 0.8
единицы от хорды. Замер при 256: уцелевший трек на кадре 2 Handwriting лежит на
+0.95..+1.58 единицы от хорды - в самой полосе фильтра, далеко от складки. Этот
кадр и даёт всю регрессию (`fold_luma_step` 0.0 -> 15.5, худшее по набору
2.100 -> 15.500), остальные кадры двигаются на десятые. Кадр 0 у обоих платит
`jag` 15 -> 42 и 14 -> 41 - там, где Arrow со включённой стадией уже стоит (34).

Маскирование замыкания силуэтом (max по заполнению нулём, min по заполнению
255, чтобы фильтр не тянул через границу альфы) пробовал - ни один показатель
не сдвинулся ни на сотую, значит через кромку он и не тянул. Оставил простую
форму.

База переснята, `check_quality` - `clean, 2 accepted, 59 known debt`.

### 23.8 Кадры 3-5 Handwriting - каша (2026-08-13, найдено; починено 2026-08-21)

Там же при просмотре покадрово. Кадры 0-2 - чёткое стекло, кадры **3, 4, 5** -
серые мутные пятна без единой грани, кадры 6-8 частично возвращают стекло.
Авторские кадры 3-5 при этом имеют внятный силуэт. Это следующая работа.

**Разобрано 2026-08-21.** Маски тут ни при чём, и это первое, что надо было
проверить: IoU силуэта против автора на 32 по всем девяти кадрам 0.916..0.991,
а на общем внутреннем множестве альфа и яркость расходятся с автором на 2-4
уровня. Дефект целиком в цвете, и на 32 его не видно - мутность начинается со
128, потому что мельче все выдумки мастера усредняются.

Источников оказалось два, а не один. Мастер `src/ai512` для кадров 3-6 - рваная
бумага: сеть, получив полупрозрачный кроссфейд автора, увеличивать в нём ничего
не смогла и дорисовала пластины с трещинами. Именно поэтому кадры 3-5 когда-то
и завели в `_BROKEN_COLOUR`, где их цвет подменялся авторским. Но авторский цвет
приходит с 32 пикселей, и на 128 и выше он и есть те самые «мутные пятна» - то
есть вторая половина дефекта была не сетью, а лекарством от неё.

**Что стоит теперь (`_MATERIAL_BASIS`).** Низкие частоты - авторские, они правы
про складку, уровень и вес кадра; высокие - от кадров 2 (стрелка) и 8 (перо),
привязанных к силуэту этого кадра аффинным преобразованием по вторым моментам
(`_moment_map`, IoU 0.963 / 0.912 / 0.857). Раздел по `_MATERIAL_SPLIT` = 1.0
логической единицы. Вокруг своей хорды складки полоса шириной 2 единицы
детали не принимает вовсе - иначе донорский излом печатается вторым.

Цена и потолок: `delta_e` кадра 4 идёт 3.55 -> 4.49 при пороге 5.0, и это же
задаёт `_MATERIAL_GAIN` = 1.2 (при 1.6 было бы 5.40 и гейт бы встал). Складочные
метрики Handwriting не двигаются вовсе: gap 0.50, step 3.33, wander 0.20, jag
43.3 - ровно база. Кадры 7 и 8 не трогали, их мастер сеть нарисовала верно.

Три отвергнутых способа положить ту же поправку - в `DEAD_ENDS.md` за эту дату.

### 23.9 Handwriting и NO вне клиньев: пробовал включить, откатил (2026-08-13)

Продолжение 23.7. Кроме трещины у этих двух курсоров **складка не доходит до
острия**: у Hand линия идёт от кончика к выемке, у них расплывается на трети
пути в широкое пятно. Причина найдена сразу - Handwriting и NO не входят ни в
`_WEDGE_TIPS`, ни в `_TROUGH_PARAMS`, ни в `_TIP_REALIGN`, хотя кадр 0 у обоих -
авторский кадр Hand пиксель в пиксель, а `_fold_chord` разрешается на
Handwriting 0-3 и 5 и на NO 0-3.

Морфинг помехой не был: каждая стадия сперва спрашивает `_fold_chord` и берёт
ответ конкретного кадра, так что перо и запрещающий знак проходят нетронутыми
(замер: кадры без хорды сдвигаются на 4-7 уровней, и то от `_temper` и
`_match_author_at_tips`, которые ключуются по имени).

**Включил с замеренными параметрами** (`_TIP_REALIGN` Handwriting 0.18, NO 0.25
- собственный замер по станциям t = 0.45..0.88; `_TROUGH_PARAMS` - числа Hand,
раз рисунок тот же). Складка действительно становится линией, виднее всего на
кадре 2 Handwriting.

**Откатил по `tip_contrast`: 0.242 -> 0.145 и 0.244 -> 0.155.** Сорок процентов
остроты кончика - ровно тот класс скрытой регрессии, ради которого рачет для
этой метрики и написан (п.15). И это объяснимо: `_WEDGE_TIPS` существует
потому, что апскейл **портит свет у острия**; у этих двух мастер острие рисует
правильно (0.24 против 0.062 у обработанного Hand), так что релайт воюет с
краской, которая и так была верной.

Что пробовать дальше: не вся стадия на полной силе, а `_TEMPER_PER_CURSOR` по
ключу `relight` для этих двух, либо `diff` меньше 85. Замеры сохранены в
комментариях у `_WEDGE_TIPS` и `_TIP_REALIGN`.

### 23.10 Порез у выемки на Handwriting и NO (2026-08-13, починено)

У обоих в выемке стоит **сплошная тёмная полоса** примерно в единицу толщиной и
четыре длиной, там где у автора только мягкий серый. Хуже, чем было у любого из
шести клиньев. Причина та же, что в 23.9: оба вне `_WEDGE_TIPS`, а `_WEDGE_TIPS`
до сих пор гейтил и `_notch_declutter` заодно.

**`_notch_declutter` не помогает - развязал и снова связал.** Его опора -
сглаженная окрестность, `_NOTCH_BLUR` 1.4 единицы, а полоса толщиной около
единицы. Окрестность проседает вместе с дефектом, порог опускается за ним, кроп
не двигается. Это ровно тот провал, который его собственный докстринг
приписывает подходу «локальный уровень» вообще.

**Помогает `_notch_from_author`.** Он опирается на авторскую краску, а не на
собственную окрестность, поэтому не проседает. Добавил Handwriting и NO в
`_NOTCH_AUTHOR_CURSORS`. Полоса из чёрной царапины с яркой каймой становится
мягкой серой тенью, на всех трёх размерах.

NO по метрикам заметно лучше: `fold_luma_step` 2.500 -> 1.433, `fold_jag`
40.833 -> 7.967. Handwriting не двигается - его показание складки лежит в другом
месте. Новых регрессий нет, `tip_contrast` не тронут (стадия к острию не
подходит). База переснята.

**Замечено попутно, не чинено:** самые крупные долги по складке сейчас у Help
(`fold_luma_step` 20.5, `fold_jag` 54.0) и Arrow_Down (`fold_wander` 0.460,
`fold_luma_step` 7.517, `fold_jag` 82.5). Их никто не смотрел глазами.

### 23.11 Боковая кромка грязная (2026-08-13, починено)

Замечание владельца: боковые кромки тоже кривые. Смотрел при 32 и 64, где эта
кромка и есть весь край.

**Что там.** У автора затенённая правая верхняя сторона - одна мягкая тёмная
линия во всю длину клина. У нас та же линия, но разбита на крапины: тёмно-бурый,
почти чёрный, серый, красный пиксель - по одной на пиксель. Текстура мастера
доживает в детали шириной около пикселя, и каждый пиксель берёт свой отсчёт.
Читается как грязь, а не как стекло.

**Это не ошибка уровня.** Замер поперёк стороны при 128 на Wait, композит на
сером, станции от кромки внутрь:

```
рендер 128  96  84  95 137 182 182 177 175
автор  127  96  93 101 119 142 163 178 185
```

Темнота самой линии почти авторская (84 против 93). Ошибка - хромовая кайма
рядом: 182 против 142, и приходит она ступенькой там, где у автора пандус в
полторы единицы. Но глазом видно не каёмку, а именно то, что линия не одного
цвета два пикселя подряд.

**Что не сработало:**
- `_EDGE_SHADOW_D_LO` 0.45 -> 0.30 -> 0.20: профиль двигается на 3-4 уровня
  (84 -> 87 -> 88), а на 0.20 у Hand ломается складка (`fold_luma_step`
  2.75 -> 8.50, `fold_jag` 34 -> 66, `fold_wander` 0.015 -> 0.475) - тот же
  провал, что записан против 0.25. Не рычаг.
- Потолок на отход от автора в полосе у кромки (обобщение `_notch_from_author`
  на весь контур): каёмку опускает на 20 уровней, крапины оставляет - они не
  отход от автора. Плюс без выреза у углов сжигает остриё (Hand `tip_contrast`
  0.062 -> 0.038).

**Что сработало - `_edge_comb`.** Сглаживание **вдоль** контура, не поперёк.
Касательная перпендикулярна grad(`_edge_distance`), поэтому все отсчёты остаются
на одной глубине и профиль поперёк кромки не меняется - силуэт остаётся острым.
Линия становится ровной на всех размерах.

Два выреза, оба обязательны: у трассированных углов (там касательная
бессмысленна, и сглаживание идёт поперёк острия - Arrow 0.129 -> 0.038, Hand
0.062 -> 0.038, Arrow_Down 0.092 -> 0.036) и `_fold_keepout` там, где складка
подходит к контуру у хвостового выреза (без него UpArrow `fold_gap`
1.250 -> 2.250).

Параметры: reach 0.5 (0.75 чистит чуть дальше и стоит `jag` у NO 8.0 -> 21.8),
глубина 0.7 (0.9 стоит Arrow_Down `fold_luma_step` 7.5 -> 8.1). Единственный
сдвиг в базе - `delta_e` у NO 6.757 -> 6.763.

`selftest` - PASS, `check_quality` - `clean, 2 accepted, 59 known debt`.

### 23.12 На 32px курсоры бледнее авторских (2026-08-13, починено)

**Как нашлось.** Композит на светлом столе (240) и 99-й процентиль
|яркость - 240| по трассированной маске. На 64 наш рендер сходится с автором в
пару уровней, на 32 - недобирает 20-30%, а 32 это размер по умолчанию в системе:

```
             рендер  автор
Arrow          81.3  101.3
UpArrow        75.9  102.4
Hand           79.1  101.3
NO             80.4  113.0
Handwriting    80.5  113.3
Wait          144.9  167.1
AppStarting   126.8  162.0
```

**Ложный след - линейный свет.** `_resize` усредняет в линейном свете, что
физически верно и перцептивно бледно, а автор рисовал 32px глазом в гамме.
Проверено: усреднение готового 512-кадра в гамме даёт Arrow 94.2, в линейном -
92.0, при отгрузочных 81.3. То есть **цветовое пространство стоит 2-4 уровня из
16-20**. Гамма-даунсемпл именно в `_master_rgb` (единственный ресайз мастера)
дал вообще +3. Не тот рычаг. Заодно проверено, что премультипликация ни при чём
(гамма с ней и без неё совпадает до 0.6 уровня), то есть выигрыш не от подмеса
прозрачных полей.

**Настоящая причина.** Прогнал цепочку `frame_image` стадия за стадией
одновременно на 32 и на 512, приводя 512-й промежуточный результат к 32 и
сравнивая. До `_edge_shadow_declutter` колонки идут вместе (расхождение 1-3
уровня), на ней расходятся разом:

```
стадия              нативно 32   из 512   разрыв
Arrow   relight            89.8     93.2      3.4
Arrow   edge_shadow        81.3     92.4     11.1
UpArrow relight            91.3     94.5      3.2
UpArrow edge_shadow        75.2     94.2     18.9
```

В самой стадии:

```python
r = max(1, int(round(_EDGE_SHADOW_REACH * size / V.LOGICAL)))
```

`_EDGE_SHADOW_REACH` = 0.35 логической единицы. На 512 это 5.6 пикселя и
округляется в 6 - тот радиус, который замеряли. На 32 это 0.35 пикселя,
округляется в ноль, а `max(1, ...)` выдаёт целый пиксель - **радиус 1.0
логической единицы, втрое шире замеренного, на курсоре, у которого весь тёмный
контур шириной в один пиксель**. Max-фильтр съедал контур целиком. На 48 радиус
выходил 0.67 единицы, на 64 - 0.50.

**Фикс.** Минимальный радиус max-фильтра - один пиксель; там, где пиксель шире
замеренного вылета, стадия честно не запускается (ниже 96px). `max(1, ...)`
убран, чтобы округление не подменяло отказ. На 256 и 512 радиус не тронут.

**Смотрел глазами** на 32, 48, 64, 96 и 128, оба фона, рядом с авторской
графикой. До 64 включительно полосы, ради которой стадия написана, просто нет -
она уже уже пикселя, стадия только осветляет настоящую кромку, и авторская
кромка темнее обеих версий. На 96 вторая линия видна отчётливо (двойной контур
вдоль верхне-правой стороны у Arrow, UpArrow, AppStarting, Hand, Help) и стадия
её снимает. 96 - ровно тот размер, где пиксель впервые помещается в вылет.

Стало на 32: Arrow 89.8, UpArrow 90.6, Hand 87.7, Wait 159.0, AppStarting 162.5
(у автора 162.0 - в точку). Handwriting 82.6 и NO 83.2 против 113 у автора
подтянулись мало - у них остаток другой природы, отдельный долг.

Регрессий против базы ноль, `selftest` - PASS, `check_quality` - `clean,
2 accepted, 59 known debt`.

**Остаток: тёмный контур (2026-08-13, починено).** После правки выше на 32
недобор был уже 5-12%, а не 20-30, но недобор остался, и это не стадия. Прогон
цепочки по стадиям показывает разрыв уже на первой (мастер + `_freeze_lines`):
у Handwriting 81.7 против 113.3 у автора, дальше ни одна стадия не виновата.

Дело в **ширине**. У автора тёмная кайма вокруг силуэта - целая логическая
единица; у нас из `src/ai512` приходит примерно пятая часть единицы, и на пути
вниз она усредняется со светлым стеклом. Самый тёмный пиксель по маске, композит
на 240: у нас 145/141/141 против 106/109/106 у автора (Arrow/Handwriting/NO).
Доля маски темнее 150: у автора 3.7-4.1%, у нас 0.4-1.1%.

Стадия, которая сравнивает с его 32px, уже есть - `_match_author_level`. Но у
неё два ограничителя: `_LEVEL_CAP` = 12 уровней и `_LEVEL_SMOOTH` = 4.5
логической единицы. Оба существуют ради одного - поправка меряется на его
пикселях и растягивается на наши, и без них его сетка отпечатывается на рендере
в несколько раз мельче. **На 32 растяжения нет**: его пиксель - наш пиксель,
«мельче его пикселей» физически не бывает, отпечатываться нечему. А 12 уровней
не перекрывают разрыв в 40, и размытие в 4.5 единицы не восстанавливает кайму
шириной в единицу.

Оба ограничителя теперь отпускаются по одной рампе `up = clip((size-32)/32,0,1)`:
на 64 и выше всё как было, на 48 половина, на 32 поправка идёт в его
разрешении с потолком `_LEVEL_CAP_NATIVE` = 40. Смотрел на 32 на обоих фонах
рядом с его графикой, восемь клиньев: кайма встаёт там, где у него, и больше
нигде, трафарета не появилось.

```
             было  стало  автор
Arrow        89.8   98.9  101.3
Arrow_Down   90.6  103.0  106.0
UpArrow      90.6  100.3  102.4
Hand         87.7  101.5  101.3
Wait        159.0  166.5  167.1
AppStarting 162.5  160.9  162.0
```

Потолок 60 даёт ещё уровень у Arrow и перелёт у Wait, потолок 20 - половину
каймы. Регрессий против базы ноль, `selftest` - PASS, `check_quality` - `clean,
2 accepted, 59 known debt`.

**Help, Handwriting и NO (2026-08-13, починено).** Они не в `_WEDGE_TIPS`,
поэтому стадия их не касалась вообще, и после правки выше остались единственными
бледными на 32 (88.3/82.6/83.2 против 106.9/113.3/113.0 у автора).

Пустить их в стадию на всех размерах нельзя - шесть регрессий по складке, все
на 128/256: Help `fold_luma_step` 20.500 -> 22.867, `fold_wander` 0.274 -> 0.275,
`fold_jag` 54.000 -> 54.733, Handwriting `fold_wander` 0.225 -> 0.287, NO
0.014 -> 0.020 и `fold_luma_step` 1.433 -> 1.667. Это ровно тот трафарет, ради
которого размытие и стоит, на трёх курсорах, под которые стадию не настраивали.

У стадии две работы, и теперь два сторожа. Выпрямление границы светлого листа -
работа по вершине клина, идёт на всех размерах на `_WEDGE_TIPS`, как и было.
Возврат тёмной каймы - работа малых размеров, и её получают все клиновидные
(`_EDGE_SHADOW_CURSORS`), но только там, где поправка ещё рядом с его сеткой
(`up < 1`, то есть ниже 64). Метрики складки читаются на 128 и 256 и этой
области не касаются вовсе.

Стало на 32: Help 101.4 (автор 106.9), Handwriting 106.8 (113.3), NO 106.7
(113.0). На 48 смотрел глазами - кайма встала, трафарета нет. Ни одного курсора
ниже 75% от автора больше не осталось. Регрессий против базы ноль.

**Мельканий не добавилось.** Поправка на 32 капается 40 уровнями и не мылится,
а у Handwriting и NO она ещё и покадровая (они не в `INTERP`) - ровно та форма,
которая может замигать, и ни одна метрика гейта этого не поймала бы: `jitter` и
`temporal_fold` читаются на 256, где поправка вернулась к 12 уровням и 4.5
единицам. Замерил там, где она теперь живёт - шаг между соседними кадрами на 32,
композит на 240, среднее и p99 по объединению масок, рядом с его собственной
последовательностью:

```
             кадров  наш сред  наш p99  его сред  его p99
AppStarting       9      3.54    24.31      5.60    29.32
Hand              9      3.06    18.78      5.35    24.16
Handwriting       9     19.60    94.60     18.25    91.32
NO               11     31.54   148.88     32.53   155.10
Wait              9      2.47    16.53      3.87    18.00
```

Везде на уровне автора или тише; у Handwriting выше на 7%, при том что его
собственная графика и есть эталон того, что анимация должна делать.

### 23.13 `density_%` - один размен, а не шестнадцать дефектов (2026-08-13, разобрано)

`density_%` мимо цели 2.0 у всех шестнадцати (2.29..6.16) и носится долгом с тех
пор, как гейт подключили, ни разу не расписанный по ступеням. Расписал:

```
курсор        drift%       32      48      64      96     128     256     384
Arrow           2.50    +1.83   +0.45   -0.06   -0.42   -0.52   -0.62   -0.66
Cross           6.16    +4.66   +0.21   -0.14   -0.76   -1.07   -1.39   -1.50
IBeam           4.98    +3.57   +0.55   +0.09   -0.61   -0.94   -1.25   -1.41
SizeAll         3.55    +2.83   -0.24   -0.35   -0.36   -0.50   -0.66   -0.72
```

Это не шестнадцать дефектов, а один: **32 - выброс** (+1.7..+4.7% от собственного
среднего курсора), всё от 64 и выше - плавный спад около процента. Убрать 32 -
и Cross падает 6.16 -> 1.71, Arrow 2.50 -> 1.11, IBeam 4.98 -> 1.96.

Причина в нормализаторе уровня внутри `_up_alpha`. Он держит равным по лестнице
**средневзвешенное по маске**, а мягкая кромка маски шириной в один аппаратный
пиксель - это целая логическая единица на 32 и одна шестнадцатая на 512. На 32
кромка забирает большую долю веса, карта там ниже, и скаляр вынужден задирать
внутренность. `density` меряет именно внутренность.

Отключение нормализатора работает - и проваливает вторую половину того же
размена. `density_%` схлопывается (Arrow 2.50 -> 0.10, Help 2.29 -> 0.09, Cross
6.16 -> 1.47, IBeam 4.98 -> 0.66, двенадцать из шестнадцати под целью), а
`scale_drift` уходит с 0.011..0.068 на 0.124..0.172 при пороге 0.10 - падают все
шестнадцать. Это докстринг нормализатора, замеренный с другого конца: он меняет
0.15 логической единицы дрейфа покрытия на 2-4% уровня внутренности. Дрейф
покрытия - это курсор, незаметно меняющий размер вместе с размером отрисовки, и
это глаз видит. 2.5% непрозрачности на эродированной внутренности - нет.

Взвешивание только по сплошным пикселям тоже пробовал, оно ломает тонкие
(SizeAll 3.55 -> 18.07, SizeNS 2.51 -> 10.32). Оба тупика записаны в DEAD_ENDS.

**Вывод.** Цель 2.0 по `density_%` недостижима, пока `scale_drift` держится под
0.10, и текущий выбор верный. Шестнадцать записей в долге - один осознанный
размен. Перевести ли их из `debt` в `accepted` - бухгалтерия, решение за
владельцем; рендер здесь править нечего.

---

## Перенос формы кромки с аналитической поверхности

Добавлено 2026-08-14. Механизм работает и проверен глазами, но упирается в
геометрию клина. Код не в дереве - воспроизводится по описанию ниже за час.

**Замер, из-за которого за это стоило браться.** Профиль поперёк контура на
Arrow_Down, 512px, станции каждые 0.125 единицы, глубина до 2.5:

| источник | станций с внутренним провалом |
|---|---|
| мастер (то, что рисуется сейчас) | **97%** |
| `_bevel_shading` по тем же лучам | **0%** |

Мастер на станции 0 идёт 131, 136, 115, 172, 173, 167, 113 - два обрыва по 57 и
54 уровня там, где у автора один мягкий спад. Аналитика: 1.4, 8.9, 14.7, 15.3,
15.3, 15.6 - гладко.

**Что отвергнуто сразу.** Отдать клиньям всю заливку (`_SYNTH_BEVEL |=
{"Arrow_Down"}`, одно слово) - `rim_layers` падает 78% -> 29% при авторских 12%,
и курсор превращается в кашу: этот путь берёт цвет из `_resize(orig, size)`, а
на 512 это авторские 32px, растянутые в шестнадцать раз. Число право, картинка
нет.

**Что построено вместо.** Переносится не заливка, а форма радиального профиля.
Для каждой станции `corr(u) = (b(u) - b(e)) - (m(u) - m(e))`, где `e` - конец
луча внутри стекла. Мастер сохраняет свой уровень, цвет и все частоты вдоль
дуги; переписывается только то, как он поднимается от кромки. Поправка
сглаживается **вдоль дуги** (иначе печатается гребёнка лучей: она была ясно
видна на верхней кромке Arrow) и разбрасывается обратно по тем же лучам
билинейно.

**Результат без ограничений** (`rim_layers`, доля станций):

| курсор | было | стало | автор |
|---|---|---|---|
| Arrow | 76% | **61%** | 5% |
| Hand | 67% | **50%** | 5% |
| Help | 45% | **26%** | 5% |
| UpArrow | 84% | **74%** | 6% |
| Handwriting | 75% | **66%** | 15% |

Глазами на 2x по кромке: у Arrow вдоль верхнего ребра шли две линии - хромовый
блик и тёмная полоса параллельно ему изнутри; после переноса тёмная полоса
исчезает, остаётся один спад. У Wait грязная бурая полоса становится тонкой и
чистой. Это ровно то, что просит владелец.

**Во что упёрлось.** Поправка ломает складку: UpArrow `fold_luma_step` 1.000 ->
5.733, `fold_jag` 12.8 -> 31.7, `fold_gap` 1.25 -> 2.00; NO `fold_jag` 8.0 ->
21.8. Посылка «аналитический гребень и есть складка автора, значит keep-out не
нужен» неверна: гребень - медиальная ось силуэта, а нарисованный излом рядом с
ней, и это «рядом» шириной с саму измеряемую деталь.

Перепробовано:

- `_fold_keepout` (0.8 единицы) - спасает UpArrow, не спасает NO и Arrow_Down: у
  них нет хорды, и вес возвращается единицей.
- Потолок поправки 22 -> 15 уровней - `fold_gap` у UpArrow не сдвинулся вообще
  (2.125 в обоих случаях). Ломает не величина, а само присутствие поправки.
- Keep-out 1.6 единицы + потолок 15 + только клинья и Help - гейт почти зелёный
  (остаются `fold_wander` 0.008 -> 0.009 на UpArrow и подпороговое движение на
  Wait), но **выигрыш съеден**: Arrow 76% -> 74%, Help 45% -> 44%, а UpArrow и
  Arrow_Down на процент хуже.

Причина геометрическая: на тонком клине полоса кромки и окрестность складки -
одни и те же пиксели. Keep-out шириной 1.6 единицы вокруг хорды накрывает как
раз те участки, что несут дефект. Различитель не может быть пространственным по
хорде (совпадают) и не может быть по величине (проверено).

- Keep-out вокруг **отслеженного пути излома** вместо хорды: `_fold_offsets`
  уже отдаёт `ts, offs` - смещение излома от хорды по всей длине. Радиус 0.5
  единицы от пути, потолок 22. Проверено: **тоже не освобождает полосу**.
  Arrow 76% -> 75%, Help 45% -> 38%, Arrow_Down и UpArrow на процент хуже - то
  есть немногим лучше keep-out'а 1.6 по прямой и далеко от 61% и 26% без
  ограничений.

Три попытки примирить перенос со складкой (keep-out по хорде 0.8 и 1.6, потолок
поправки, keep-out по пути 0.5) - каждая либо ломает излом, либо стирает
выигрыш. Механизм доказан и глазами, и числами; примирение в этой сессии не
сошлось.

**Куда смотреть дальше.** Развилка, а не готовый шаг:

1. Не защищать складку, а **чинить её тем же способом**: строить эталон профиля
   *вдоль* излома так же, как здесь построен поперёк кромки, и переносить обе
   формы за один проход. Тогда поправке нечего ломать - складка будет частью
   цели, а не препятствием.
2. Пустить перенос **раньше по цепочке**, на `src/ai512`, до того как складку
   начнут измерять и подкручивать шесть стадий подряд. Это выходит за рендер, но
   дефект родом оттуда: 97% станций несут провал уже в мастере.
3. Признать, что на клине полоса кромки и окрестность складки неразделимы в
   пространстве, и различать их **по направлению**: полоса параллельна контуру,
   излом идёт под углом. Ни одна из попыток этого не использовала - все делили
   по расстоянию.

### Разделение по направлению: полоса горизонтальна, излом диагонален

Добавлено 2026-08-14, тот же заход, другой механизм. Пункт 3 развилки выше
построен и проверен. Механизм разделения работает, но **метрику не двигает**, и
почему - осталось невыясненным. Код не в дереве.

**Замысел.** Разложить кромку в координатах (дуга, глубина). Полоса идёт
параллельно контуру, значит это **горизонталь на постоянной глубине**. Излом
контур пересекает, значит это **крутая диагональ**: его глубина уходит примерно
на единицу за единицу дуги, то есть на пять отсчётов за станцию при шаге 0.25.
Эрозия карты провалов вдоль дуги **на фиксированной глубине** оставляет
горизонталь и стирает диагональ. Поправка тогда равна эродированной карте, и у
излома она ноль **по построению** - ни keep-out, ни хорды, ни потолка, ни
подстройки по курсорам.

**Разделение действительно работает.** Провал глубже 6 уровней на Arrow (512,
280 станций):

| эрозия | доля станций | средняя глубина |
|---|---|---|
| без эрозии | 86% | 22.8 |
| ±0.5 единицы дуги | 72% | 13.7 |
| ±1.0 | 50% | 8.7 |
| ±1.5 | 32% | 5.6 |
| ±2.5 | 10% | 2.1 |

**Чего не хватило.** `rim_layers` не сдвинулся: Arrow 82% -> 80%, Help 53% ->
53%, Hand 80% -> 80%. Пробовано и так, и так:

- поправка = эродированная карта: Arrow 82% -> 81%. Эрозия это минимум по
  соседям, то есть заниженная оценка глубины; заполнялось меньше, чем нужно,
  чтобы перешагнуть порог в 6 уровней.
- эродированная карта как **вентиль**, а заполняется реальный измеренный провал:
  Arrow 82% -> 80%. Тоже почти ничего.

Второй вариант и есть загадка. Провал в сыром RGB настоящий: вдоль луча на
станции 40 профиль идёт 198, 199, 203, **166**, 224 - обрыв в 37 уровней. В
композите он же 153, 184, 187, **159**, 203 - 28 уровней, то есть виден. Альфа
стекла 0.66 медианой и гуляет по лучу от 0.35 у контура до 0.80 на глубине 1.0.
Арифметика говорит, что поднятие сырого 166 до ~210 должно убрать провал и в
композите (159 -> 194 при соседях 203 и 187). Стадия меняет 2100 пикселей.
Число не двигается.

**Замер сделан, и он закрывает вопрос.** Поправка доходит до финального кадра:
3803 пикселя изменены, максимум 62 уровня, альфа не тронута. Поздние стадии её
не съедают. Профиль меняется на 96 станциях из 280 - а дип исчезает **на одной**.

Причина видна на станции 1: провал 111.5 при впадине глубиной 33 уровня поднят
до 116.3, то есть вентиль там открылся на 0.15. Эродированная карта дала 4.6
уровня там, где локальная впадина 33.5.

Проверено, что это не жёсткость структурного элемента: если разрешить признаку
дрейфовать по глубине на 1, 2, 3 или 4 отсчёта за станцию (наклонный элемент -
дилатация по глубине пропорционально удалению по дуге, потом эрозия по дуге),
число не двигается ни на процент: Arrow 82% -> 80% при любом наклоне.

**Вывод, и он сильнее исходного вопроса.** Когерентного признака вдоль контура в
карте провалов нет. Дипы стоят **по станциям**, а не тянутся полосой - ни на
постоянной глубине, ни на медленно плывущей. Два независимых метода дали один
ответ: медиана по дуге (DEAD_ENDS, «полосы нет») и эрозия с допуском на наклон
(здесь).

Значит мертво всё семейство «отделить полосу от складки по когерентности вдоль
дуги», а это ровно центральная посылка находки 6 исходного плана. И это же
объясняет, почему перенос формы выше сработал там, где всё остальное нет: он
единственный **ничего не предполагает о когерентности** - каждое сечение
исправляется против своего эталона.

**Живой маршрут остался один:** перенос формы, и единственная его нерешённая
задача - защита складки. Не тратить попытки на когерентность по дуге.

## 24. A/B против опубликованного набора 58a28b7 (2026-08-15)

Все 16 курсоров, оба дерева развёрнуты рядом (`git show 58a28b7:<файл>` плюс его
собственные `src/` и `traced.json`), рендер в отдельном процессе на своё дерево,
замеры - одной копией кода на возвращённых растрах.

**Верность автору.** Наш кадр приведён к его 32px сетке: IoU силуэта и средняя
CIE76 dE по пикселям, которые оба считают телом.

| | опубликовано | HEAD |
|---|---|---|
| IoU к автору, среднее по 16 | 0.8488 | **0.8714** |
| dE к автору, среднее по 16 | 4.50 | **2.74** |

Лучше на 13 курсорах из 16. Хуже по IoU двое, оба на 0.014: Help (0.8583 ->
0.8445, но dE 3.42 -> 1.07) и SizeNESW (0.8889 -> 0.8750, dE 3.56 -> 3.32).

**Лишний слой на кромке**, доля станций, 512:

| курсор | опубликовано | HEAD | | курсор | опубликовано | HEAD |
|---|---|---|---|---|---|---|
| Cross | 83% | **16%** | | Arrow | 87% | 83% |
| SizeAll | 67% | **14%** | | UpArrow | 93% | 88% |
| IBeam | 55% | **14%** | | Hand | 91% | 82% |
| SizeNWSE | 76% | **14%** | | Handwriting | 90% | 86% |
| SizeWE | 51% | **18%** | | NO | 90% | 85% |
| SizeNS | 49% | **24%** | | Arrow_Down | 99% | 94% |
| SizeNESW | 82% | **26%** | | Help | 68% | 56% |
| | | | | AppStarting | 100% | 99% |
| | | | | Wait | 100% | 100% |

Лучше или так же на всех 16. Ступенька там, где тень рисуется из геометрии.

**Зеркальная асимметрия**, 512, шум замера снят контролями (заведомо
несимметричные Help и UpArrow плывут на +1.6 и +1.9, значит всё внутри ±1.9 -
шум): Cross lr **-5.34**, IBeam lr **-6.13**, IBeam ud **-3.66** - настоящее.
SizeAll t +1.58 и SizeWE ud +2.56 - на границе шума.

### Wait и AppStarting: решение принято

Единственное, что выросло - слоёв на станцию: Wait 2.17 -> 2.67, AppStarting
2.07 -> 2.29. Разобрано и **принято осознанно**:

1. Это авторская черта, а не наша. Замер `rim_layers_orig` на его 32px:
   Wait **0.81**, AppStarting **0.65** - против 0.03-0.16 у всех остальных
   четырнадцати. Второй тёмный слой на кромке рисует он сам, и только на этих
   двух.
2. К автору при этом стало **ближе**, а не дальше: Wait IoU 0.8566 -> 0.8694,
   dE **8.33 -> 5.12**; AppStarting dE **9.32 -> 5.66**.
3. Глазами на 512 рядом с его 32px: цвет насыщеннее и ближе к оригиналу, тёмная
   кромка уже и определённее, а не шире.

Метрика считает провалы в профиле, и резкая кромка рядом с ярким телом даёт их
больше, чем размытый градиент. На этих двух курсорах она штрафует за то, что
кромка стала похожа на авторскую. Заносить в `metrics-known-issues.json`
нечего - это не регрессия гейта, гейт зелёный.

## 25. Перенос формы кромки: собран, включён на четырёх (2026-08-19)

Механизм из раздела «Перенос формы кромки с аналитической поверхности» написан
заново и лежит в дереве: `_rim_transfer` в `hybrid.py`, стадия идёт сразу после
`_notch_from_author`. Включён на четырёх курсорах,
`_RIM_XFER = {"Arrow", "Help", "NO", "AppStarting"}`, гейт зелёный на всех
шестнадцати.

**Полная лестница против `metrics-baseline.json`:**

| курсор | `rim_layers` | слоёв на станцию | что ещё сдвинулось |
|---|---|---|---|
| Help | 0.440 -> **0.259** | 0.534 -> 0.427 | `fold_gap` 0.312 -> **0.000** (уходит из долга), `luma_step` 27.53 -> 24.47, `jag` 70.9 -> 60.2, `wander` 0.261 -> 0.243, `delta_e` 3.49 -> 3.71 |
| Arrow | 0.719 -> **0.655** | 0.960 -> 0.932 | складка не шелохнулась ни на одном размере; `delta_e` 2.71 -> 3.46 |
| NO | 0.755 -> **0.652** | 1.516 -> 1.267 | `delta_e[7]` 6.76 -> **6.57**, `cadence` 1.912 -> 1.902 |
| AppStarting | 0.978 -> **0.921** | 2.360 -> 2.500 | `tip_wobble` 0.264 -> 0.253, `temporal_fold` 1.002 -> 1.001 |

Потолок поправки 60 уровней композита, не 30: на 30 у NO оставалось 0.718, на
60 - 0.652, у Arrow 0.662 -> 0.655, и ни одна складка от этого не двинулась.
Глазами на 512 и на остриях 6x разницы между 30 и 60 не видно.

Долг 59 -> 58: `fold_gap` у Help закрыт. Авторские значения `rim_layers` для
сравнения: 0.045-0.053 у первых трёх, 0.65 у AppStarting - это его собственная
черта (раздел 24).

**Глубина окна - 1.0 логической единицы, не 2.5.** На 2.5 окно уходит за
кромку в тело и в окрестность складки, и оттуда приносит её обратно: у Arrow
`fold_jag` 53.9 -> 55.6 и `luma_step` 4.20 -> 4.33, у Help остриё держалось
только на самой большой глубине. На 1.0 окно - это сама кромка: складка у Arrow
не двигается вообще, а `rim_layers` у Help идёт дальше вниз, 0.333 -> 0.252
(при одном и том же потолке 30).
Одно число на все курсоры, по курсорам ничего не подбирается.

Пять расхождений с описанием прошлой сессии - каждое стоило числа:

1. **Читается и правится в композите на 128**, а не в сыром RGB. Альфа стекла
   идёт от 0.35 у контура до 0.80 на глубине единицы, поправка в сырых уровнях
   приходит к глазу умноженной на неё, а метрика меряет композит.
2. **Секция кончается на гребне аналитики**, а не на 2.5 единицах. На тонкой
   лапе медиальная ось попадает внутрь окна, за ней аналитический профиль идёт
   вниз, и перенос печатает гребень в кромочную полосу отдельным слоем: именно
   это портило Arrow_Down 0.745 -> 0.78 и Handwriting 0.733 -> 0.79.
3. **Аналитический подъём масштабируется под подъём мастера** по станции.
   Размах аналитики - `_BEVEL_DIFF`, число про клинья; напечатанный на мастере
   с другим подъёмом, он даёт ступеньку там, где они расходятся.
4. **Обратно поправка кладётся полем**, а не рассыпается по своим же лучам:
   пиксель берёт глубину из `_edge_distance_at`, а секцию - смешанную по
   станциям вокруг себя с весом по смещению вдоль дуги (`_RIM_XFER_BLEND`).
   Взять секцию **ближайшей** станции - это разбиение стекла на ячейки Вороного,
   каждая печатает свою секцию, и поправка ложится плоскими фасетками с прямыми
   швами. У кончика, где ячейки расходятся веером, это видно сразу: на Help при
   512 и увеличении 4x - блоки с прямыми границами по остриям и у крючка «?».
   Смешивание их убирает и стоит 0.314 -> 0.333 по `rim_layers`; цена честная,
   фасетки - артефакт, а не выигрыш.
5. **Поправка не доходит до самих остриёв** (`_RIM_XFER_CORNER`, 1.5 единицы
   плюс единица разгона). Секция - это чтение вдоль одной нормали, а в точке
   нормали нет: лучи станций по обе стороны пересекаются, их секции расходятся,
   и смесь двух расходящихся секций печатает в острие тёмный клин. Там, где
   геометрия не определяет поправку, поправка не применяется.

**Кого включить не вышло.** Arrow_Down и Handwriting на любой глубине идут
вверх по `rim_layers`; Hand и Handwriting дают `fold_jag` на кадрах, которых
нет в кадре 0 (Hand 34.0 -> 36.8, Handwriting 134.0 -> 136.5) - смотреть надо
всю анимацию, замер по нулевому кадру их пропускает; UpArrow и Wait ломают
складку (UpArrow `jag` 13.9 -> 50.3). Всем им перенос либо не нужен, либо
упирается в ту же геометрию клина.

**Развилка 1 (чинить складку тем же способом) построена и не сходится.**
`_FOLD_XFER`/`_fold_transfer` в дереве, выключен. Эталон строится *вдоль*
излома: сечения берутся по отслеженному пути (`_fold_offsets`, не по хорде),
высокочастотная часть их яркости вдоль длины переписывается на аналитическую,
медленный градиент остаётся. Числа:

| потолок | Arrow step 512 | Arrow jag 512 | UpArrow gap 128/256/384 |
|---|---|---|---|
| выключено | 4.20 | 53.9 | 2.75 / 2.00 / 2.92 (после переноса кромки) |
| 18 | 15.9 | 86.3 | **0.50 / 0.38 / 0.67** |
| 6 | 5.8 | 48 | 2.25 / 2.75 / 2.75 |
| 3 | 4.5 | 56 | 2.25 / 2.75 / 2.92 |

Сильный перенос действительно **чинит разрыв складки у UpArrow** - лучше, чем
он был до всякого переноса кромки, - и ровно тем же ходом ломает яркость вдоль
линии у Arrow и Hand втрое. Слабый не чинит ничего. Одного потолка на оба
эффекта нет: аналитика вдоль складки меняется своей амплитудой, и там, где
мастер уже прав, эта амплитуда - чистый вред.

Остаётся развилка 2 (перенос выше по цепочке, на `src/ai512`) - и она же
единственная, где UpArrow не приходится мирить с Arrow.

## 26. `density_%`: причина найдена и доказана, починка упирается в храповик (2026-08-19)

Задача 1 из списка. Причина названа точно, три варианта починки замерены, в
дереве не оставлено ничего: гейт ни один из них не пропускает.

**Причина - весовая функция якоря уровня, а не рендер стекла.** `_up_alpha`
держит уровень карты прозрачности скаляром: `cur` считается как среднее по
маске, `target` - то же на 128. Кромка маски шириной в один аппаратный пиксель:
на 32 это целая логическая единица, на 512 - шестнадцатая. Карта на этой кромке
падает, и её доля в среднем зависит от размера. Arrow, доля сглаженной кромки в
покрытых пикселях и что читает якорь:

| размер | доля кромки | `cur` | множитель |
|---|---|---|---|
| 32 | **41%** | 158.02 | **1.023** |
| 48 | 31% | 160.00 | 1.010 |
| 64 | 25% | 160.82 | 1.005 |
| 128 | 14% | 161.59 | 1.000 |
| 512 | 4% | 161.91 | 0.998 |

Множитель 1.023 на 32 - это и есть `density_%` 2.46. Десять курсоров читают
одинаковые 2.54 потому, что делят одну геометрию, а не один дефект стекла.
Порог 2.0 и `_DENSITY_SAMPLE` тут ни при чём, метрика права.

**Вариант 1: якорь по внутренности** (пиксели глубже 1.0 единицы, область
выбирается один раз на 128 и пересэмплируется). Уровень после этого совпадает на
всех рунгах до сотых. `density_%`: Arrow 2.46 -> **0.24**, Help 2.38 -> **0.19**,
NO 2.34 -> **0.20**, Cross 6.76 -> **1.62**, SizeAll 3.84 -> **1.90**. Но тот же
скаляр держал и полное покрытие: `scale_drift` уходит 0.013 -> **0.159** на всех
шестнадцати при пороге 0.1. Один скаляр не держит две величины.

**Вариант 2: + возврат покрытия на кромке.** Уровень держит внутренность,
покрытие - отдельная поправка, применяемая только к пикселям, которые маска
сглаживает (множители 1.072 на 32 и 0.963 на 512 у Arrow - маленькие, потому что
считаются против массы самой кромки). `scale_drift` возвращается в 0.008-0.062.
Остаётся 8 провалов: `density_%` у SizeNS 2.53 -> 3.55 и SizeWE 2.65 -> 4.11
(на тонкой планке шириной шесть единиц частичные пиксели достают до середины,
внутренности нет), плюс мелочь по складкам и `delta_e` на Arrow_Down,
Handwriting, NO - альфа входит в цвет через `_sat_match`.

**Вариант 3: + бокс вместо Lanczos при уменьшении карты, уровень читается на
опорной сетке.** Тонкие планки чинятся тоже: Arrow 0.55, Cross 0.35, SizeNS
1.32, SizeWE 1.56, SizeAll 0.55, Help 0.40, NO 0.46, IBeam 0.60 - **вся
шестнадцатка ниже порога, ни одной регрессии по `density_%`**. Цена - 20
провалов гейта, все со стороны цвета: складки шевелятся на девяти курсорах
(Arrow `fold_gap` 0.625 -> 0.875, Hand `jag` 34.0 -> 37.2, Wait `jag`
8.8 -> 10.3), у AppStarting складка впервые становится измеримой и сразу не
проходит (было `fold_unmeasured` в принятых), IBeam `delta_e` 5.43 -> 5.46.

**Где это стоит.** Все пороги `fold_*` равны 0.0, то есть недостижимы, и все
шестнадцать курсоров уже сидят в долге по ним; храповик при этом запрещает
любое движение вверх. Так что вариант 3 меняет 16 позиций долга по `density_%`
на 20 подпороговых шевелений там, где цель всё равно не берётся. Это решение
владельца, а не разработчика: либо принять шевеление складок, либо искать
компенсацию со стороны цвета (альфа входит в `_sat_match` и в композит).

Кода в дереве нет - откачено. Всё, что нужно для повтора, в этом разделе: якорь
по внутренности, поправка покрытия на кромке, бокс при уменьшении.

## 27. `tip_contrast`: полевой якорь разложен на две части (2026-08-19)

Задача 3 из списка, непроверенный подозреваемый - якорь `_band_level`. Проверен.
Разбор в разделе 15 верен, но неполон: якорь делает **две вещи сразу**, и они
разделяются.

Якорь ставит уровень заплатки `_tip_relight` не скаляром по полосе, а полем
(размытием по полосе), и включён только на `INTERP`. Поле несёт:

1. **движение** - как блик проходит по вершине за цикл, ради чего его и вводили;
2. **стоячее смещение** - у самой точки уровень мастера выше среднего по полосе,
   заплатка ложится светлее полосы, и контраст вершины на фоне падает.

Второе можно вычесть, не трогая первое: смещение - это то, что не меняется за
цикл, то есть среднее по кадрам отклонения поля от среднего по полосе
(`_band_bias`, считается по цветовым мастерам, чтобы не завязываться на порядок
стадий).

| вариант | Hand `tip` | Hand `amp` | Wait `tip` / `amp` | AppStarting `tip` / `amp` |
|---|---|---|---|---|
| поле (сейчас) | 0.062 | 10.91 | 0.165 / 9.16 | 0.111 / 12.91 |
| **поле минус смещение** | **0.074** | **10.91** | 0.170 / 9.18 | 0.111 / 12.92 |
| скаляр (как на статичных) | 0.094 | 8.29 | 0.170 / 7.06 | 0.111 / 10.03 |

То есть треть просадки Hand снимается **бесплатно**: живость (`tip_sheen.amp`)
не меняется вообще. Остаток - цена самого движения, и он честно масштабируется:
доля поля `k`, оставленная над плоским средним, даёт Hand
`tip` 0.074 / 0.079 / 0.083 / 0.086 / 0.094 при `amp` 10.91 / 10.37 / 9.85 /
9.33 / 8.29 для k = 1.0 / 0.8 / 0.6 / 0.4 / 0.0. Цель 0.084 берётся на k = 0.4
ценой 14% живости.

**Почему не в дереве.** Даже бесплатный вариант (k = 1.0) шевелит две
подпороговые величины: AppStarting `temporal_fold` 1.002 -> 1.003, Wait
`fold_wander` 0.025 -> 0.026 (при этом Wait `fold_jag` 8.83 -> 8.60). Храповик
запрещает любое движение вверх, а выбирать k по тому, где метрика перестаёт
падать, - это подгонка под метрику. Решение владельца: либо принять два
шевеления в четвёртом знаке ради 0.062 -> 0.074 без цены, либо назначить k
осознанно, как цену живости.

Код откачен, воспроизводится по описанию: `_band_bias(name, size, band)` =
среднее по кадрам от `_band_level(lum_i) - среднее(lum_i по полосе)`, и
`lvl = flat + k * (_band_level(lum) - _band_bias - flat)`.

## 28. Мелочи из задачи 6: разобраны, обе - не то, чем назывались (2026-08-19)

**IoU у Help и SizeNESW - это порог бинаризации, а не силуэт.** Замер
воспроизведён (наш кадр на его 32px сетке, тело = альфа >= 128; Help даёт
0.8445 - в точности число из раздела 24). Пиксели, которых «не хватает»:

| курсор | автор есть, у нас нет | медианный разрыв | из них у порога (|автор-128| <= 10) |
|---|---|---|---|
| Help | 33 | **3 уровня** | 31 |
| SizeNESW | 11 | 25 уровней | 9 |

У Help это 33 пикселя со значениями 124-126 против авторских 128-135: стекло на
три уровня прозрачнее, и метрика режет их по 128. Силуэт при этом совпадает -
проверено отдельно: маска, построенная по **опубликованным** трассам, даёт Help
0.8410, то есть трассы не при чём (и они изменились: 13 вершин из 48, максимум
0.25 единицы). Гнаться за этими тремя уровнями = двигать уровень стекла ради
порога; dE там при этом упал 3.42 -> 1.07.

**`delta_e` у NO[7] и IBeam[0] - покрытие силуэта, а не цвет.** Худшие пиксели
(на белом и на чёрном фоне соответственно):

| | автор | у нас | маска | `_up_alpha` |
|---|---|---|---|---|
| NO[7] (23,15) | a 255 | a 98 | **107** | 233.7 |
| NO[7] (15,23) | a 235 | a 107 | **123** | 221.1 |
| IBeam[0] (28,18) | a 118 | a 34 | **63** | 137.6 |
| IBeam[0] (8,16) | a 187 | a 170 | 253 | 171.8 |

У NO на седьмом кадре запрещающее кольцо у нас вдвое прозрачнее авторского, и
это целиком маска: `_up_alpha` там 233 из 255. У IBeam то же на нижней засечке
(строки 27-28), плюс маленькая разница по уровню стекла на верхней. То есть обе
претензии - трассировка тонких деталей, а не оттенок, и правка живёт в
`trace.py` + `traced.json` одним коммитом, а не в рендере. Бокс вместо Lanczos
при уменьшении карты (см. раздел 26) здесь не помогает: NO 6.57 -> 6.56,
IBeam 5.43 -> 5.49.

**`NEXT_SESSION_PROMPT.md` и `SESSION_SUMMARY_2026-08-13.md`** по-прежнему
описывали состояние на 13 августа. Решение владельца - удалить; сделано
2026-08-20 вместе с `PLAN_NEXT.md`, живые маршруты из которого перенесены в
`STATUS.md`.

### 28.1 Откуда берётся недостача маски (2026-08-19)

Продолжение: у NO[7] и IBeam[0] виновата маска, значит вопрос к трассировке.
Замер - доля пикселей, которые есть в **собственном источнике трассировки**
(`src/ai`, порог `max(30, min(0.45*peak, 55))`), но которых нет в маске, делённая
на периметр источника, в логических единицах:

| кадр | вдавливание | потеряно px из периметра |
|---|---|---|
| NO[7] | **0.215** | 340 из 395 |
| IBeam[0] | **0.244** | 217 из 222 |
| NO[0] | 0.159 | 181 из 284 |
| Help[0] | 0.152 | 198 из 325 |
| Arrow[0] | 0.142 | 161 из 283 |

Контур везде вдавлен внутрь на 0.14-0.24 логической единицы относительно своего
же источника - систематически, по всему периметру. Половина этого объяснима
построением: `boundary_chain` берёт **внутренние** пиксели границы и кладёт
вершину в индекс пикселя, а не в его центр, то есть теряет полпикселя на 128 =
0.125 единицы. Остальное - порог 55 по мягкому краю.

На толстых курсорах это незаметно, на тонком штрихе - нет: у NO кольцо шириной
две единицы теряет четверть с каждой стороны, и на 32px его альфа падает с
авторских 255 до 98. Отсюда и `delta_e` 6.57.

Спрямление рёбер тут ни при чём - проверено: `straighten_runs`, отключённый
целиком, не меняет ни одной вершины на этом кадре.

**Что делать.** Сдвинуть контур наружу на полпикселя (вершина в центр пикселя, а
не в индекс) - правка в одну строку `boundary_chain`, но она двигает силуэт всех
шестнадцати, поэтому это отдельная задача с полным гейтом и обязательной парой
`trace.py` + `traced.json` в одном коммите. Ожидаемый выигрыш: `delta_e` у NO[7]
и IBeam[0], возможно IoU у Help (33 пикселя у порога).

### 28.2. Вынос контура наружу: замерено, не отгружено

Гипотеза из 28.1 проверена не правкой `trace.py`, а эквивалентом: вершины
`traced.json` сдвинуты наружу по локальной нормали на постоянную `d` прямо в
памяти, кэши сброшены, метрики пересчитаны. Смещение по нормали, а не сдвиг
всей фигуры: сдвиг чинит две стороны и портит две другие.

Четыре курсора, ради которых всё затевалось, ведут себя ровно как ожидалось -
`delta_e` монотонно падает с ростом `d`:

| d | NO | IBeam | Arrow | Help |
|---|---|---|---|---|
| 0.00 | 6.57 / 0.9298 | 5.43 / 0.9153 | 3.45 / 0.8455 | 3.71 / 0.8445 |
| 0.05 | 6.26 / 0.9345 | 5.00 / 0.9492 | 3.30 / 0.8545 | 3.49 / 0.8500 |
| 0.125 | 5.81 / 0.9079 | 4.51 / 0.9667 | 3.12 / 0.8507 | 3.39 / 0.8347 |
| 0.15 | 5.66 / 0.8816 | 4.44 / 0.9667 | 3.10 / 0.8447 | 3.39 / 0.8313 |
| 0.20 | 5.58 / 0.8640 | 4.23 / 0.9500 | 2.99 / 0.8447 | 3.40 / 0.8313 |

(`delta_e` / IoU). На полпикселя, то есть на предсказанных 0.125, IoU уже
проседает у NO и Help - половина выигрыша `delta_e` оплачена силуэтом. А `d=0.05`
на этой четвёрке выглядел бесплатным: обе метрики лучше у всех четырёх.

Прогон по всем шестнадцати выигрыш не подтвердил. При `d=0.05`:

- `delta_e` лучше у тринадцати (среднее -0.13, максимум -0.36 у IBeam),
  у трёх - шум +0.007 (Arrow_Down, UpArrow, Hand);
- IoU лучше у пяти (SizeNESW +0.020, SizeWE +0.016, IBeam +0.017),
  без движения у восьми, **хуже у четырёх**: NO -0.022, Cross -0.019,
  SizeNS -0.011, Handwriting -0.009.

Четыре просадки IoU - это четыре отказа храповика. Причём выборка из четырёх
курсоров показывала обратное: обе метрики росли. Ещё один случай, когда
подвыборка соврала.

Вывод по существу: вдавливание контура реально и измерено, но оно **не
равномерное**. У NO, Cross, SizeNS и Handwriting силуэт относительно автора уже
не тонкий, и общий вынос делает их толще нужного. Единая константа поэтому не
проходит, а константа на курсор - подгонка под метрику, а не геометрия.

Живой остаток: чинить не смещением контура, а порогом-по-краю в самой
трассировке (сейчас `max(30, min(0.45*peak, 55))` - жёсткая ступень по мягкому
краю), либо трассировать по полупрозрачности (marching squares по уровню 0.5),
где вершина сама встаёт в геометрически верное место и вдавливание исчезает
без константы. Это уже не правка в строку, а замена `boundary_chain`.

### 28.3. Трассировка по уровню альфы: проверено, не годится как замена

Продолжение 28.2. Вместо константы - поставить каждую точку сырой цепочки на
уровень `alpha == thresh`: идти от неё по своей нормали и остановиться там, где
исходная альфа пересекает порог. Реализовано копией `trace.py` в песочнице
(вставка после `boundary_chain`, ±2 пикселя, бисекция по билинейной выборке),
прогнано на всех шестнадцати, результат скормлен рендеру подменой `cursors.TRACED`.

Смещение получилось ровно такое, как предсказывала 28.1: у Arrow среднее 0.48
пикселя наружу, максимум 0.98, сдвинуты все 283 точки цепочки. Лестница исчезает,
и `simplify` после этого оставляет от контура вчетверо меньше вершин
(Arrow 51 -> 6, Help 57 -> 25, Cross 53 -> 23).

И вот тут всё ломается. Углы классифицируются `corner_curvature` **по этой же
цепочке**, а порог `CORNER_KEEP_DEG` настроен на лестницу: на ней настоящий угол
даёт резкий поворот внутри окна. Уровень мягкой альфы угол скругляет (в
источнике он размыт), поворот размазывается по окну, флаг не ставится, и
`straighten_runs` сливает через бывший угол два прогона в один. На однокомпонентных
курсорах это почти не видно, на многолучевых - катастрофа:

| курсор | delta_e | IoU |
|---|---|---|
| Cross | 4.03 -> 15.05 | 0.918 -> 0.480 |
| SizeNESW | 4.94 -> 15.79 | 0.857 -> 0.465 |
| IBeam | 5.43 -> 11.33 | 0.915 -> 0.746 |
| SizeAll | 3.83 -> 7.07 | 0.804 -> 0.651 |
| NO | 6.57 -> 10.20 | 0.930 -> 0.864 |

Рендер это подтверждает: у Cross теряется луч, у NO по кольцу идёт тёмный шов.

**Но побочный замер важнее самой попытки.** На шести однокомпонентных курсорах
силуэт практически не двинулся (IoU -0.009, `delta_e` не хуже), а `rim_layers`
рухнул:

| курсор | rim_layers | delta_e |
|---|---|---|
| Arrow_Down | 0.745 -> 0.322 | 3.22 -> 3.16 |
| UpArrow | 0.755 -> 0.377 | 3.80 -> 3.40 |
| Hand | 0.640 -> 0.380 | 2.89 -> 2.88 |
| Arrow | 0.655 -> 0.493 | 3.46 -> 3.16 |

То есть **половина главного открытого дефекта приходит из лестницы
трассированного контура, а не из затенения**. Ступенька в полпикселя вдоль
контура сама по себе даёт лишнюю впадину в поперечном профиле - `rim_layers`
честно её считает, а весь прошлый сезон правок искал её в шейдинге.

Отгружать нельзя, и не только из-за многолучевых. Прогон с ограничением снапа
шестёркой однокомпонентных отрендерен и рассмотрен: остриё Arrow становится
**темнее** - на апексе появляется заметная тёмная шапка, которой в базовой версии
нет. `rim_layers` при этом улучшился вдвое. Ещё один случай, когда метрика
хвалит то, что глаз бракует.

**Углы отдельно от гладких участков - проверено тут же, тоже мимо.** Очевидная
починка: классифицировать углы **до** снапа (по лестнице, как сейчас), сносить на
уровень только точки неугловых прогонов, вокруг каждого флага держать зону
покоя (6 точек) и рампу (ещё 6). Силуэты после этого выживают: у Cross снова
четыре луча, `delta_e` 4.24 вместо 15.05, Help и IBeam целы. Но гейт всё равно не
проходит:

- IoU падает у одиннадцати из шестнадцати, причём у всей однокомпонентной
  семьи ровно на -0.023 (0.8455 -> 0.8222) - контур целиком уехал наружу на
  полпикселя по гладким прогонам, и 32px-бинаризация это видит;
- Handwriting -0.110, NO -0.107 по IoU;
- `rim_layers` теперь разнонаправлен: IBeam 0.241 -> 0.148 и UpArrow 0.755 ->
  0.596 лучше, зато Help 0.259 -> 0.344 и SizeNESW 0.081 -> 0.161 хуже;
- `delta_e` тоже вразнобой: NO -0.862 и Arrow -0.537 против SizeNS +0.270 и
  Hand +0.152.

То есть выигрыш по `rim_layers` из предыдущего замера держался в основном на том,
что снап **скруглял углы** - как только углы удержали, половина выигрыша ушла.
Сама лестница даёт меньше, чем показалось.

**Что осталось.** Направление трассировки закрыто до отдельной сессии с полным
гейтом; три варианта (константа, снап целиком, снап с удержанием углов) замерены
и записаны в DEAD_ENDS.md. Если к нему возвращаться, то не ради `rim_layers`, а
ради `delta_e` тонких штрихов, и начинать с порога `max(30, min(0.45*peak, 55))`,
а не с положения вершин.

## 29. Один мастер и одно освещение: эксперимент по циклическим анимациям

Постановка владельца: для AppStarting/Hand/Wait единица обработки выбрана
неправильно. Сейчас обрабатывается кадр - девять независимых интерпретаций
Real-ESRGAN, которые потом пытаются согласовать между собой временным ядром
`_SHEEN_SMOOTH`, заморозкой острия `_tip_still` и кроссфейдом. Сеть в каждом
кадре заново решает, где идёт складка и как выгнут белый участок, поэтому
усреднение двух кадров даёт не промежуточное положение складки, а две
полупрозрачные складки. Вместо этого: один канонический мастер на весь цикл,
геометрия неподвижна, из оригинальных 32px берётся только изменение освещения,
интерполируется как периодический сигнал и накладывается на канонический кадр.

Собрано в `lightanim.py` (отдельный модуль, в `build.py` не подключён,
шипящийся конвейер не тронут). `anim_frames_lighting(name, size)` отдаёт все 27
кадров.

**Что подтвердилось сразу.** Альфа автора побитово одинакова во всех девяти
кадрах всех трёх курсоров (максимальная разница 0.0), меняется только цвет - до
231 уровня у Wait. Замораживать силуэт не приближение, а его собственное
поведение. Все три идут ровным темпом 3 джиффи на кадр, значит фазы равномерны и
тригонометрическая интерполяция - это в точности ДПФ.

**Что пришлось поменять в плане по дороге.**

1. *k=4, а не k=2.* Девять отсчётов несут ровно четыре гармоники, то есть k=4 -
   полное преобразование, проходящее через каждый кадр автора. Обрезка задумана
   была против несогласованности сети, но сети в этом тракте больше нет, и
   обрезка теряет только его собственную подсветку: k=2 воспроизводит его кадры
   с ошибкой 4.3/3.2/3.4 уровня, k=4 - 0.34/0.01/0.35.
2. *Свет поканальный, а не по яркости.* Держать хроматику канонического кадра и
   двигать только яркость - идея аккуратнее, но она не про эти рисунки: цвет у
   автора ходит на 231 уровень, и яркостная модель воспроизводит его кадры с
   ошибкой 9.5 уровня против 0.35 у поканальной.
3. *Свет складывается, а не умножается.* Логарифмическое отношение - правильная
   модель затенения, но подсветка тут зеркальная и уходит в белое, а белое на
   насыщенном жёлтом стекле - это добавленный свет, а не умноженный жёлтый.
   Умножение поднимает почти нулевой синий мастера вчетверо и кладёт вдоль
   верхней кромки серо-голубую дымку. Видно на рендере сразу.
4. *Свет берётся из мастеров, а не из оригиналов автора.* Вот это главное
   расхождение с планом, и оно вынужденное. Оригиналы автора дают чистое
   освещение без структурного шума (его геометрия статична), но лежит оно на
   **его** геометрии: его освещённый лист кончается на пару логических единиц не
   там, где наш. Разность его кадров, положенная на наш мастер, даёт пурпурное
   пятно там, где его подсветка пересекает нашу тёмную кромку - воспроизводится
   на любом кадре, не лечится ни заливкой края, ни ограничением по гамме.
   Низкочастотная часть наших же мастеров лежит на нашей геометрии; всё, из-за
   чего мастера были непригодны (перерисованная складка, ползающая кромка), -
   высокая частота, и размытие на 2 логические единицы её убирает, оставляя
   ровно искомое: где свет.

**Числа.** Дрожание высокочастотной структуры за цикл (СКО по времени
высокочастотной составляющей внутри силуэта, 128px) и полный путь цвета за цикл:

| курсор | дрожание old -> new (автор) | путь old -> new (автор) |
|---|---|---|
| AppStarting | 6.55 -> **2.65** (1.48) | 13.9 -> 10.9 (15.9) |
| Hand | 9.10 -> **2.09** (1.39) | 13.2 -> 9.4 (13.0) |
| Wait | 5.57 -> **2.53** (1.06) | 11.0 -> 9.0 (11.8) |

Дрожание падает в 2.5-4.4 раза и подходит к авторскому вдвое ближе, чем всё, что
делал старый тракт. Плата - размах подсветки на треть меньше авторского.
Это одна кривая обмена, и по ней можно ехать в обе стороны:

| размытие света | путь (AppS/Hand/Wait) | дрожание |
|---|---|---|
| 1.0 | 15.6 / 13.3 / 12.4 | 4.41 / 4.64 / 3.92 |
| 1.5 | 12.8 / 10.9 / 10.3 | 3.20 / 2.72 / 2.98 |
| 2.0 | 10.9 / 9.4 / 9.0 | 2.65 / 2.09 / 2.53 |

Усиление гладкого поля (`_LIGHT_GAIN`) едет по той же кривой: 1.45 при размытии
2.0 даёт путь 15.2/13.6/12.6 при дрожании 3.92/2.97/3.52.

Отдельно: `tip_sheen.amp` без `_tip_still` идёт 12.9/10.9/9.2 -> 17.1/15.3/13.6
при авторских 14.7/13.6/10.7. Старый тракт занижал размах на остриях, новый
завышает; заморозки острия в нём нет вовсе, и дрожание острия при этом ниже
авторского (0.37/0.31/0.29 против 0.39/0.34/0.34).

### 29.1. Живее, чем у автора

Решение владельца: раз геометрия заморожена, анимация должна быть **заметнее**,
а не осторожнее. Размах подсветки увеличен усилением гладкого поля
(`_LIGHT_GAIN`), и на этом вылезла последняя поломка модели.

При усилении 1.45 и выше на изломе под верхней кромкой появлялось зелёно-бирюзовое
пятно. Замер по пикселю объяснил, почему: канонический жёлтый там (0.665, 0.376,
0.018) в линейном свете, а уход подсветки - это (-0.391, -0.199, +0.002). У
жёлтого стекла синего нет, терять ему нечего, поэтому вычитание уносит красный,
оставляет зелёный, и получается зелень. Ограничение по гамме тут не спасает: оно
не даёт вылезти за диапазон, но не мешает жёлтому стать зелёным по дороге.

Два обходных пути были собраны и отброшены (оба записаны в комментарии модуля):
гашение поля у кромки (эти курсоры - тонкие клинья, поводок шириной 1.5 единицы
достаёт до половины тела, размах на остриях падает 24.0 -> 8.1) и масштабирование
поля по освещённости места (излом всё равно зеленел - дело не в количестве света,
а в том, что вычитание красного делает с жёлтым).

Работает разделение по знаку: **уходящий свет умножает, приходящий складывается**.
Поверхность, с которой свет ушёл, темнеет пропорционально - тон сохраняется;
подсветка, которая пришла, добавляется поканально, как и положено бликам. Пятно
исчезает на всех усилениях.

Итог при усилении 2.0 (128px, старое -> новое, в скобках автор):

| курсор | путь за цикл | дрожание ВЧ | остриё | клип | ровность |
|---|---|---|---|---|---|
| AppStarting | 13.9 -> **18.2** (15.9) | 6.55 -> **5.14** (1.48) | 12.9 -> 28.4 (14.7) | 4.6% -> 11.0% (2.7%) | 1.00 -> 0.99 (1.16) |
| Hand | 13.2 -> **18.7** (13.0) | 9.10 -> **4.11** (1.39) | 10.9 -> 30.7 (13.6) | 0.0% -> 2.9% (0.0%) | 1.01 -> 1.09 (1.19) |
| Wait | 11.0 -> **14.7** (11.7) | 5.57 -> **4.80** (1.06) | 9.2 -> 22.5 (10.7) | 4.8% -> 9.9% (2.7%) | 0.97 -> 0.96 (1.11) |

То есть подсветка идёт на 15-45% размашистее авторской, на остриях - вдвое, и при
этом высокочастотное дрожание всё равно ниже старого тракта, а геометрия
неподвижна по построению: альфа во всех 27 кадрах побитово одна и та же, мастер
один на цикл, `_tip_still` и `_SHEEN_SMOOTH` в этом тракте не участвуют вовсе.
Остаточные 4-5 единиц дрожания - это само световое поле, а не структура.

**Что не сделано.** Модуль не подключён к `build.py`, гейт по нему не гонялся,
метрики базовой линии не трогались.

## 30. Структурные грани: остриё как пересечение двух поверхностей

Постановка владельца: «кончики не сходятся», «серая нижняя грань исчезает» и
«хорда съехала» - это один архитектурный дефект. Внешняя геометрия стрелки
строится трассировкой (апекс вообще реконструируется пересечением двух сторон и
может выноситься наружу до `TIP_MAX_EXT = 0.75`), а внутренняя грань стекла -
это пиксели мастера, нарисованные там, где их положила сеть. Два независимых
мира перемножаются и должны совпасть.

**Замерено, прежде чем что-то менять.**

Хорда A→J у Arrow: A=(3.08, 2.95), J=(19.5, 19.0) - оба конца уже есть в
обводке (`_fold_chord`), плюс B и C как два оставшихся выпуклых угла. Ничего
руками задавать не пришлось.

Смещение реальной AI-складки от этой линии по 25 станциям: у острия -0.02,
-0.14, **-0.44, -0.70**, -0.61, -0.21, дальше по телу устойчиво +0.4.

Альфа вдоль хорды от вершины (512): в самой A - **9**, на 0.25 - 37, на 0.5
уже 182. Вектор острый, стекло начинается на полединицы позже: `alpha =
_mask * up_a`, и второй множитель отменяет первый.

Контраст двух поверхностей (медианы линейной яркости по разные стороны хорды,
внутренность маски, 512):

| t от острия | было | стало |
|---|---|---|
| 1-2 | **1.06** | 2.58 |
| 2-3 | 2.78 | 2.71 |
| 3-5 | 3.26 | 2.95 |
| 8-12 | 2.12 | 2.12 |
| 16-22 | 2.10 | 2.10 |

У автора по телу 1.91-2.07 - ровно как у нас. Дефект **чисто локальный**: в
первых двух единицах от вершины две поверхности перестают различаться. Первый
замер (через `alpha > 200`) показывал дефицит контраста по всему курсору - это
была ошибка выборки, порог отсекал полупрозрачное стекло; правильная маска её
исправила.

**Что сделано.** `_landmarks` (A, B, J, C из обводки), `_facet_frame` (знаковое
расстояние до хорды и вдоль неё), `_facet_split` и `_tip_glass` в `hybrid.py`,
включено только для Arrow (`_FACET_CURSORS`).

Первая версия - поправка контраста поверх мастера - гейт завалила: `fold_jag`
53.9 -> 57.8. Причина по делу: свой перепад на хорде накладывается, а AI-складка
в 0.7 единицы рядом никуда не девается, и в сечении оказывается два края вместо
одного. Поэтому в tip-zone мастер не поправляется, а **заменяется**: цвет каждой
поверхности линейно подгоняется по полосе 3-9 единиц (где мастеру можно верить)
и продолжается внутрь до вершины, две поверхности разделяются по знаку
расстояния до хорды, а ширина перехода **открывается от нуля в A** до 0.4
единицы в теле. Линия между ними не рисуется: у линии есть своя ширина, и она бы
разделила остриё надвое.

Альфа: `_tip_glass` берёт прозрачность стекла в 1.5 единицах от вершины и
протягивает к A. Векторная маска по-прежнему рисует край, поэтому остриё
остаётся ровно таким острым, как оно оттрассировано - в нём просто появляется
стекло. Альфа в A: 9 -> 92 (92 - это собственное сглаживание полигона: 0.475
маски на 183 стекла), на 0.25 единицы: 37 -> 183.

**Результат.** Гейт PASS. `tip_contrast` у Arrow 0.129 -> 0.199 (у автора 0.084),
`rim_layers` 0.655 -> 0.626, `fold_jag` не сдвинулся. На рендере 512 остриё стало
клином из двух поверхностей, сходящихся в вершину, вместо растворяющегося
лепестка.

### 30.1. Три версии перестройки и что их развело

Первая версия (поправка контраста поверх мастера) гейт завалила: `fold_jag`
53.9 -> 57.8, потому что свой перепад накладывается на хорду, а AI-складка в 0.7
единицы рядом остаётся, и в сечении оказывается два края.

Вторая (заменить зону линейной подгонкой цвета каждой грани по полосе 3-9)
гейт прошла и на вид была хороша, но её сгубил собственный диагностический
профиль репозитория - максимум яркости в сечении по станциям 0.25-2.5:

```
было      Arrow  133 136 139 141 152 161 195 217 229 238
подгонка  Arrow  198 199 199 199 199 199 199 200 200 200
```

Разгон яркости к острию заменился ровным плато - ровно та болезнь UpArrow,
из-за которой в этом файле уже есть раздел, только нарисованная светлее.
Наклон, снятый с полосы 3-9, впятеро меньше настоящего у самого острия.

Третья, отгруженная: **поперёк хорды перестраивается, вдоль - нет**. В каждом
сечении зоны берутся две грани по перцентилям яркости (тёмная и светлая), их
профили вдоль хорды сохраняют собственный разгон мастера, а поперёк ставится
одна чистая граница с шириной, открывающейся от нуля в A. Профиль после этого
`131 131 131 131 143 162 178 190 193 196` - разгон на месте.

Замер автора на тех же станциях: `0 0 0 98 101 103 104 107 113 117` (первые три
нуля - у него там альфа ниже половины). У него разгон 20 уровней, у нас было 105,
стало 65. То есть по этому чтению мы двигаемся к нему, а не от него.

### 30.2. Где упёрлись в храповик

Перцентиль, по которому берётся уровень грани, - это выбор между «типичным»
и «крайним» значением сечения, и он же оказался границей проходимости гейта:

| перцентиль | ratio 1-2 (тело 2.12) | tip_contrast (порог 0.123, автор 0.084) | rim |
|---|---|---|---|
| 25 | 2.11 | 0.083 | 0.629 |
| 10 | 2.63 | **0.125** | 0.629 |
| 4 | 2.90 | 0.131 | 0.647 |

Принципиально правильный - 25: он ставит остриё ровно на собственный контраст
тела (2.11 против 2.12) и ровно на авторский `tip_contrast` (0.083 против его
0.084). И именно он не проходит: базовая линия этой сборки 0.129 **выше
авторской**, а храповик ходит только вверх, поэтому «стать как автор» считается
регрессией. Отгружен перцентиль 10 - он держит крайние значения сечения, даёт
контраст граней даже выше телесного и проходит.

Это третий случай в этом файле, когда храповик блокирует движение к автору
(см. 26, 27). Решение владельца: пересобрать базовую линию. Что из этого
получилось - в 30.3; таблица выше при пересборке частично не подтвердилась.

Для AppStarting/Hand/Wait это тем более уместно: там силуэт и так статичен на
весь цикл, структурная грань A→J одна на все кадры, а двигаться должен только
свет (см. раздел 29).

### 30.3. Пересборка базовой линии и что она показала

Базовая линия пересобрана (`tools/analyze.py --baseline metrics-baseline.json`),
гейт зелёный. Но сама пересборка сняла с таблицы 30.2 два неверных числа, и оба
были в мою пользу.

**Первое.** Перцентиль 25 не «ровно авторский», а на процент ниже: 0.0827 против
его 0.0838. Разница в том, что таблица 30.2 мерила остриё не на той ступени
лестницы, а гейт мерит на 256. То есть пересборка базовой линии этот вариант всё
равно не спасала бы - он падал не только на храповике, но и на пороге по автору,
а порог по автору пересборкой не двигается и двигаться не должен.

**Второе, и это оказалось главным.** Прежние замеры Arrow_Down (0.0915) и UpArrow
(0.0438) в 30.2 - это значения **с выключенной** перестройкой: сравнивались
кэшированные кадры, `analyze._frame_cache` - обычный dict, и `cache_clear()` по
модулю его не трогает. С включённой перестройкой на тех же ступенях:

| курсор | off | pct 25 | pct 20 | pct 15 | автор |
|---|---|---|---|---|---|
| Arrow | 0.1292 | 0.0827 | 0.1213 | - | 0.0838 |
| Arrow_Down | 0.0915 | 0.0597 | 0.0756 | 0.0845 | 0.0879 |
| UpArrow | 0.0479 | 0.0269 | 0.0388 | 0.0408 | 0.0848 |

Перестройка граней **тратит** контраст острия, а не только контраст храповика.

**Почему.** Статистика граней бралась по внутренности (`ed > 0.35`), а
накладывалась на всю зону острия - включая тёмный ободок по кромке. У острия
клин узкий, ободок занимает почти всю его ширину, и перестройка его затирала
внутренним стеклом. А контраст острия против рабочего стола несёт именно ободок.
Ободок - другая структура, чем две поверхности внутри него, и трогать его этой
стадии нечего. Отсюда `_FACET_KEEP_RIM`: доля кромки, которую держит мастер, с
растушёвкой ещё на такую же ширину.

| keep (pct 25) | tip_contrast | ratio 1-2 |
|---|---|---|
| 0.00 | 0.0827 | 2.11 |
| 0.25 | 0.0827 | 1.96 |
| 0.40 | 0.1263 | 1.23 |
| 0.60 | 0.1292 | 1.05 |

Отгружено: Arrow, перцентиль 20, keep 0.25. Остриё читается двумя поверхностями с
контрастом 2.12 при телесном 1.98, `tip_contrast` 0.1195 - в 1.4 раза выше
авторского. Базовая линия по Arrow сдвинулась 0.1292 -> 0.1195 (это и есть
пересборка: часть запаса над автором потрачена на структуру острия),
`rim_layers` 0.719 -> 0.630, `delta_e` от самой перестройки +0.6% (3.455 ->
3.477).

**Arrow_Down и UpArrow выключены.** Ни один из девяти замеренных вариантов
(перцентиль 25/20/15 на keep 0/0.25/0.4) не окупается: у Arrow_Down лучший
контраст граней 1.72 при телесном 2.72 и остриё 0.0845 против авторских 0.0879;
у UpArrow грани идут 0.93 -> 1.22 при телесном 1.33, а остриё падает 0.0479 ->
0.0408 при том, что оно и так вдвое ниже авторского. Тратить остриё на
пол-единицы контраста граней там нечем. Это не закрывает сам подход - это
говорит, что у этих двух дефект острия не в разделении поверхностей, а в самом
ободке, и лечить надо его.

**`_tip_glass` оставлен.** На `tip_contrast` он не влияет вообще (метрика берёт
максимум по диску в 1.5 единицы, там правит ободок), но альфа у самого апекса,
в четверти единицы от A, поднимается 82/99/37 (32/128/512 px) до 128/165/182 -
то есть остриё перестаёт растворяться раньше вершины и перестаёт зависеть от
размера так сильно. Цена: `scale_drift` 0.0126 -> 0.0238 при пороге 0.10, потому
что опорное значение берётся из `up_a` своей ступени.

В пересобранную линию попал и дрейф уже сделанной, но ещё не закоммиченной
работы по ободку и сгибу: `delta_e` Arrow 2.71 -> 3.45, AppStarting 4.05 -> 4.57,
Help 3.49 -> 3.71 (порог 5.0), `rim_layers` Help 0.44 -> 0.26, NO 0.75 -> 0.65
(не гейтится, только отчёт). Все они замерены до перестройки граней и к ней
отношения не имеют.

---

## 31. Гейт: три класса вместо одной таблицы порогов (2026-08-19, сделано)

Постановка владельца: `fold_wander = 0`, `fold_luma_step = 0`, `fold_jag = 0` -
это не модель качества. Нулевая цель по `luma_step` означает, что яркость вдоль
складки не имеет права меняться, по `jag` - что профиль поперёк складки одинаков
в каждой строке. Ни то, ни другое не является свойством освещённого стекла. И 35
позиций долга из 58 говорили ровно это.

Разделено на три класса.

**Инварианты.** Силуэт, `scale_drift`, `density`, `fold_gap`. Абсолютные пороги
остаются: они достижимы и значат что-то сами по себе. Складка либо идёт по всей
длине, либо разорвана - это структура, а не картинка.

**Качество относительно автора.** `tip_contrast`, `delta_e`, `tip_sheen`,
`morph_iou`. Как доля от его собственного кадра. Его рисунок - стандарт, свой мы
не изобретаем.

**Диагностика изображения.** `fold_wander`, `fold_luma_step`, `fold_jag` -
цели нет вообще. Калибровать не по чему: `fold_profile` на авторских 32px не
возвращает ничего ни на одном размере ни для одного из шести клиньев. Поэтому
судятся по принятому глазом рендеру - по базовой линии - и только уход от неё
дальше `_DIAG_SLACK` считается отказом. Допуск 20%: движение меньше этого глаз
на складке не ловит, а на метрике оно есть всегда.

**`unmeasured` - отдельный ответ.** Не проход и не дефект рендера: трекер не
увидел складку. Возвращается третьим списком и печатается своей секцией. Но
потеря чтения, которое было в базовой линии, остаётся отказом - именно так
измерение складки просидело сломанным 36 попыток.

**Побочно вылечены «чистые нули».** `_flat()` писал `fold_gap/wander/luma_step/
jag` равными 0.0, когда не разрешился ни один размер. То есть в файле, с которым
гейт всё сравнивает, неизмеренная складка выглядела идеальной. Теперь там `null`.

**Что стало.**

| | было | стало |
|---|---|---|
| долг | 58 | 31 |
| принято без чисел | 2 | 0 |
| не измерено | - | 2 (обе AppStarting) |
| отказы | 0 | 0 |

`metrics-known-issues.json` опустошён: обе записи там существовали только чтобы
глушить `unmeasured`, у которого теперь есть свой класс. Заметки в файле
оставлены - они говорят, что значит починить каждую.

**Проверено, что класс работает.** Гейт против подделанной базовой линии: сдвиг
на 43% (`jag` 53.87 против 45.25) отказ, сдвиг на 8.7% проход. `selftest.py`
обновлён под новый контракт и проверяет обе стороны: неизмеренная складка
приходит в свой класс и не в отказ, а потеря чтения относительно базы - отказ.

**Дальше по этому маршруту:** повторить вариант 3 по плотности (раздел 26).
Раньше он менял 16 позиций долга по `density_%` на 20 подпороговых шевелений
складок при недостижимой цели; теперь блокировать его может только реальный уход
диагностики за 20%, и решение принимается глазами, а не таблицей.

---

## 32. Анимация света подключена к сборке (2026-08-20, сделано)

`hybrid.anim_frames` для AppStarting/Hand/Wait уходит в `lightanim`
(`H.LIGHT_ANIM = True`, переключатель на месте). Кросс-фейд полных RGBA
ключевых кадров сплайном Catmull-Rom больше не используется на этих трёх - ни
для Windows (27 кадров), ни для Xcursor (9 кадров, авторские длительности).

**Что пришлось починить перед подключением.** Проверка A/B нашла в
экспериментальном файле два дефекта, которых не было видно, пока он не шёл в
сборку.

*Призрак.* Поле света меняло цвет и там, где альфа нулевая: `ghost_rgb`
0.000 -> 4.92 при пороге 0.5. Ничего из этого не видно на экране, но метрика
существует ровно чтобы невидимая зона не таскала мусор. Остаток теперь
умножается на альфу с рампой до `_GHOST_ALPHA` = 10. Стало 0.02-0.04.

*Темп.* Реконструкция бралась в равных фазах, и авторская неравномерность
проходила насквозь: `cadence` (пик к среднему по видимому шагу) 1.39-1.48 при
пороге 1.15. Поле - непрерывная функция фазы, поэтому кадры теперь ставятся не
по равному времени, а по равному изменению картинки: `periodic_at` считает
фит в произвольной фазе, `_paced_phases` строит кривую по 216 фазам и режет её
на равные доли. То же самое делает старый `anim_frames`, но на уровень выше -
по готовым кадрам, а не по свету.

Две ловушки внутри этого, обе стоили по прогону:

- темп нельзя мерить по самому полю. Модель света множительная с одной стороны
  и аддитивная с другой, плюс sRGB сверху: равное изменение поля - не равное
  изменение картинки. Темп по полю дал `cadence` 1.57, то есть хуже, чем без
  темпа вообще. Считается по отрисованному цвету на прореженном размере;
- `V.linear_to_srgb` возвращает uint8. Шаг в минус три уровня читался как 253,
  кривая темпа превращалась в шум, а выравнивание - в ничто (1.392 против
  1.392 без него). После приведения к float - 1.025.

И маска: темп считается по «стеклу как таковому», `alpha > 0.85 * пика` - той
же доле, что и у старого тракта и у самой метрики. По `alpha > 128` выходило
1.21: выравнивался ободок, а не блик.

**Что стало.**

| | старый тракт | новый |
|---|---|---|
| AppStarting cadence | 1.016 | 1.068 |
| Hand cadence | 1.025 | 1.033 |
| Wait cadence | 1.027 | 1.072 |
| AppStarting движение за цикл | 138.5 | 184.1 |
| Hand движение за цикл | 130.1 | 187.1 |
| Wait движение за цикл | 104.4 | 144.0 |
| ghost_rgb | 0.000 | 0.02-0.04 (порог 0.5) |

Гейт зелёный, долг не вырос (31), базовая линия переснята - в ней двигались
ровно `cadence` и `ghost_rgb` этих трёх.

**Дыра, которую это открыло.** Почти все метрики анимации меряют авторские
ключевые кадры через `frame_image`, а не то, что уходит в `.ani`: `inner_jitter`,
`tip_sheen`, `delta_e`, `temporal_*` после подключения не сдвинулись **вообще**,
до последнего знака. По циклу, который реально отгружается, дрожание складки у
Hand p95 0.0122 -> 0.0106 и максимум шага 0.090 -> 0.021 - вчетверо; у Wait
трекер на новом тракте разрешает 6 строк вместо 20, то есть чтение теряется.
Метрика, ради которой всё это делалось, сейчас смотрит не туда. Это следующий
кандидат в работу: перевести `inner_jitter` на кадры цикла и разобраться,
почему у Wait их разрешается меньше.

---

## 33. Временные метрики переведены на отгружаемые кадры (2026-08-20, сделано)

После подключения `LIGHT_ANIM` временные метрики не сдвинулись до последнего
знака (раздел 32): они мерили покадровые мастера, которых в продукте больше нет.
Разделены три понятия кадра, и больше не смешиваются:

- `author_frames` - девять кадров 2006 года, эталон;
- `frame()` / `frame_image` - состояние рендера в авторской фазе. Годится для
  цвета в нарисованных фазах, геометрии, поиска регрессии в конкретном
  состоянии, но это промежуточное представление, а не продукт;
- `product_frames` - ровно то, что уходит в `.ani`, WebP и `.cape`.

На третье переведены `inner_jitter`, `temporal_fold/body`, `tip_sheen/wobble`.
`cadence` и `ghost_rgb` там были и раньше. `delta_e` намеренно оставлен на
авторских фазах: 27 авторских кадров не существует, и интерполяция эталона под
каждый кадр цикла превратила бы метрику в оценку собственной интерполяции.

**`inner_jitter` пересчитан попарно.** Прежнее правило требовало, чтобы строка
разрешалась трекером во **всех** кадрах цикла. При девяти кадрах это переживалось,
при двадцати семи - нет: строка, читаемая в девяти случаях из десяти, проходит
первый тест в 63% случаев и второй в 25%. Утроение числа кадров само по себе
срезало Wait с двадцати строк до шести, и это выглядело дефектом нового рендера.
Правильный вопрос - насколько складка сдвинулась между **двумя соседними**
кадрами, которые её оба разрешили; один непрочитанный кадр теряет одно
наблюдение, а не всю строку. Шов последний-первый входит в пары: цикл замкнут.

Рядом с числом теперь стоит покрытие, потому что обратная ошибка так же дёшева:
трекер нашёл складку в трёх удобных местах и отрапортовал идеально неподвижную
анимацию. `rows`, `pair_coverage`, `frame_coverage_min`; чтение считается
состоявшимся при `_JITTER_ROWS` = 10 строках и `_JITTER_COVER` = 0.60 покрытия.
Оба порога - про достаточность данных, не про величину дрожания.

**Что показали отгружаемые кадры.**

| | было (мастера) | стало (продукт) | автор |
|---|---|---|---|
| Hand `inner_jitter` p95 | 0.031 | 0.010 | - |
| Hand строк | 40 | 41 (покрытие 1.00) | - |
| Wait `inner_jitter` p95 | 0.016 | 0.019 | - |
| Wait строк | 20 | 28 (покрытие 0.62) | - |
| AppStarting | не измерено | не измерено, 1 строка | - |
| `tip_sheen` AppStarting | 12.92 | 28.73 | 14.66 |
| `tip_sheen` Hand | 10.91 | 30.59 | 13.60 |
| `tip_sheen` Wait | 9.16 | 22.13 | 10.65 |
| `temporal_fold` AppStarting | 1.001 | 1.114 | 1.162 |
| `temporal_fold` Hand | 1.006 | 1.115 | 1.195 |
| `temporal_fold` Wait | 0.974 | 1.043 | 1.108 |

Шести строк у Wait не было - было двадцать восемь. Диагноз про пересечение по
всем кадрам подтвердился полностью.

**`temporal_*` у трёх анимаций теперь судится по автору.** Плоский порог 1.0
стоял на том, что силуэт заморожен и любая неровность - наша. Это было верно,
пока тень раскладывали мы; теперь это его девять сэмплов, восстановленные по
фазе, и его собственный цикл читает 1.11-1.19. Мы читаем 1.04-1.12, то есть
ровнее исходника. Держать remaster на 1.0 там, где эталон не проходит, - это
изобретать стандарт. Две позиции долга ушли (31 -> 29).

**Что видно про Wait.** Покрытие 0.62 при пороге 0.60 - впритык, и минимум по
кадрам 0.21: в худшей фазе трекер разрешает пятую часть строк. Число строк по
кадрам цикла идёт `1 1 2 2 2 3 3 3 3 ... 5 5 3 2 1`. То есть свет в части цикла
действительно уплощает складку у Wait. Это уже не артефакт статистики, а
предмет для следующего разбора - на кимографе, а не по одному числу.

**Дальше:** A/B `_tip_still` (пункт 4в владельца). Теперь `tip_sheen` смотрит на
тот цикл, который получает пользователь, и эксперимент наконец честный.

---

## 34. `_tip_still` и `_SHEEN_SMOOTH` сняты (2026-08-20, сделано)

A/B, ради которого делался раздел 33, оказался с нулевым результатом по
построению: `lightanim` рендерил канонический кадр и поля света внутри
`_plain_masters()`, то есть **с обоими корректорами уже отключёнными**. Проверено
прямо: отгружаемый цикл побитно одинаков с `_tip_still` и без него.

То есть с момента подключения `LIGHT_ANIM` оба корректора применялись только к
двум вещам: к метрикам, которые мерили покадровые мастера, и к плитке в
`preview.png`. К продукту - ни к чему.

Оба сняты. `_master` теперь возвращает то, что нарисовала сеть. Отгружаемые
кадры побитно те же: sha256 цикла на 128px до и после совпадает у всех трёх
(проверено запуском старого `hybrid.py` из гита на том же дереве).

**Что изменилось - только числа и превью.**

| | было | стало | автор |
|---|---|---|---|
| AppStarting `delta_e` | 4.571 | 4.242 | - |
| Hand `delta_e` | 2.891 | 2.788 | - |
| Wait `delta_e` | 3.776 | 3.519 | - |
| AppStarting `tip_contrast` | 0.111 | 0.127 | 0.069 |
| Hand `tip_contrast` | 0.062 | 0.178 | 0.084 |
| Wait `tip_contrast` | 0.165 | 0.120 | 0.066 |
| Hand `fold_jag` | 34.0 | 59.0 | - |

Долг 29 -> 27: строка «Hand tip_contrast 0.062 < 0.084» описывала кадр, которого
никто никогда не видел. Смешение трёх ключевых кадров съедало контраст острия у
Hand втрое, и это уходило в отчёт как дефект рендера.

Два отказа гейта разобраны и приняты пересъёмкой базовой линии, потому что
рендер не менялся вообще - изменилось только, на что смотрит метрика:

- `Wait tip_contrast` 0.165 -> 0.120 при храповом полу 0.157. Остаётся в 1.8
  раза выше авторского 0.066;
- `Hand fold_jag` 34.0 -> 59.0. Это резкость, которую смешение размывало.
  Смотрел мастера рядом при 512: сглаженный слева читается мягче и мутнее,
  сырой справа держит складку, ободок и остриё. Отгружался всё это время
  именно правый.

`preview.png` для этих трёх теперь показывает канонический рендер - тот самый
кадр, от которого освещается весь цикл. Раньше показывал скорректированный
мастер, то есть картинку, которой нет ни в одном отгружаемом файле.

В `tools/loop.py` подсказки `reduce_sheen` и `check_still_tip` указывали на
снятые ручки; заменены на `check_light_field` и `check_facets`.

**Побочная находка: ассеты в гите не воспроизводятся из своего же кода.**
`assets/*.webp` пересобрались, хотя рендер побитно тот же - и это не следствие
снятия корректоров. Проверка на Hand, всё в предумноженном RGB после одинакового
кодирования и декодирования: новый ассет совпадает с рендером ровно, `max 0.0`;
ассет из HEAD не совпадает ни с рендером при снятых корректорах (`max 68`,
`mean 0.28`), ни с рендером при включённых (`max 49`, `mean 0.27`). То есть
закоммиченные ассеты сделаны из промежуточного состояния тракта, которого сейчас
нет ни в одном варианте кода - собраны раньше, чем доводка темпа и призрачного
света была дописана. Разница мизерная (порядка 0.3 уровня в среднем, глазами
ничего), но урок в другом: пересборка ассетов и коммит кода делались в разные
моменты, и провенанс потерялся. Сейчас дерево самосогласовано - ассеты
воспроизводятся из своего кода побитно.

## 35. Плотность: двойной счёт покрытия на кромке, и трекер складки (2026-08-20, сделано)

Вариант 3 из раздела 26 повторён не буквально. Бокс при уменьшении карты там был
эмпирическим подбором; под ним лежит причина, которую он обходил.

**Двойной счёт.** Карта прозрачности живёт в 512 и уменьшается на каждую ступень
лестницы. Любое усреднение при уменьшении затягивает то, что лежит снаружи
силуэта, поэтому кромочные пиксели карты выходят взвешенными покрытием. А
векторная маска взвешена покрытием по построению и домножает его второй раз:
кромка несёт `cov²` там, где должна нести `cov`, и курсор теряет массу на каждом
шаге вниз. Старый скаляр по всей маске это компенсировал глобально - поднимал всю
карту, включая внутренность, которой поднимать было не нужно. Отсюда и брались
`density_%` 2.4-6.8: не стекло густело, а прибор мерил собственный ободок
лестницы (доля сглаженных пикселей в покрытии - 41% на 32 против 4% на 512).

Три части: усреднение при уменьшении взвешено маской (`_up_alpha_raw`), уровень
читается по внутренности глубже одной логической единицы на опорной сетке
(`_level_region`, `_interior_level`), покрытие возвращается отдельно и только на
пикселях, которые маска сглаживает (`_hold_coverage`). Один скаляр не держит две
величины - это и был разбор варианта 1 в разделе 26.

| | было | стало |
|---|---|---|
| `density_%`, все 16 | 2.34 - 6.76 | 0.08 - 1.51 |
| `scale_drift` | лучше на 10 из 11 (Cross 0.074 -> 0.013, IBeam 0.039 -> 0.008, SizeWE 0.033 -> 0.010), хуже только SizeAll 0.068 -> 0.080 |
| `delta_e` | чуть лучше на всех 16 |
| `mirror_asym` | Cross 30.2 -> 26.6, SizeNS 33.6 -> 28.4, SizeWE 23.8 -> 19.2, SizeAll 33.0 -> 28.9, IBeam 22.2 -> 20.7 |
| `morph_iou` | Handwriting 0.451 -> 0.630, NO 0.349 -> 0.360 |

Проверка без `_hold_coverage`: `density_%` чуть лучше (максимум 0.90 против
1.51), `scale_drift` заметно хуже (Cross 0.078, IBeam 0.040 - то есть уровень
базовой линии). Отказы гейта в обоих вариантах совпадают до строки, значит
возврат покрытия ничего не ломает, а сносу дрожания размера помогает. Оставлен.

Глазами на лестнице 32/48/128 на светлом и тёмном фоне: неотличимо, разница
только на ободке, максимум 19 уровней на пиксель у Arrow при среднем 0.9.

**Семь отказов по складке оказались прибором.** `fold_gap` у Arrow 0.625 -> 0.875
на 256. Разобрано до конца: выпадают семь подряд строк 106-112, у которых складка
на месте и читается с prominence 14-18 при пороге 6. Трекер до них не доходит,
потому что окно строки строилось от `on.min()`/`on.max()`, где `on` - колонки
ярче 0.85 от пика альфы кадра. На этих строках `on` двумодален: слева от складки
остаётся два пикселя у самого края, и когда они тускнеют на пол-уровня,
`on.min()` прыгает с 58 на 190, окно схлопывается, строка выпадает. Максимальная
разница яркости в области складки между старым и новым рендером - **один
уровень**. Чтение зависело от одного пикселя на другом конце строки.

**Что сделано с трекером** (`_fold_track`, `fold_profile`):

- сколько у строки стекла по-прежнему читается по картинке, но **счётчиком**, а
  не парой экстремумов: счётчик двигают те пиксели, которые изменились;
- окно берётся от участка глубже `_FOLD_INSET` = 0.75 логической единицы, в
  котором стоит сама хорда, а не от внешнего размаха строки. Глубина - из
  геометрии обвода, она не шевелится, когда стекло тускнеет на уровень;
- куски трека короче `_FOLD_ROWS` выбрасываются до всех измерений и считаются
  отдельно (`specks`). Две строки на 66 при складке 98..146 давали Hand «разрыв»
  3.75 логических единиц - разрыв, которого на картинке нет;
- ниже 128 складка не читается вовсе (`_FOLD_MIN_SIZE`). Старое окно туда просто
  не доходило, новое доходит и возвращает растр: одна пропущенная строка на 64 -
  это пол-единицы «разрыва».

Что изменилось в показаниях (рендер новый в обоих столбцах, разница - прибор):

| курсор | `fold_gap` | `fold_jag` |
|---|---|---|
| Handwriting | 2.375 -> 0.500 | 134.0 -> 43.3 |
| UpArrow | 1.250 -> 0.125 | 13.0 -> 4.0 |
| Wait | 2.625 -> 1.000 | 9.5 -> 6.9 |
| Arrow | 0.625 -> 0.250 | 53.9 -> 47.3 |
| Arrow_Down | 0.625 -> 0.250 | 65.4 -> 55.8 |
| Hand | 0.625 -> 2.500 | 59.0 -> 57.8 |
| NO | 0.250 -> 1.250 | 42.6 -> 46.0 |

Hand и NO стали хуже потому, что трекер теперь доходит туда, куда раньше не
доходил. Смотрел профили Hand на 128, кадр 5, строки 39-48: складка там не тёмная
линия, а гладкая ступень 198 -> 225 без провала. Прибор честно говорит «здесь
линии нет»; раньше он этих строк просто не касался. Это чтение рендера, а не
дефект прибора, и уходит в долг, как и остальные `fold_gap` при недостижимой
цели 0.0.

`inner_jitter` от того же окна: Hand 41 -> 54 строки, Wait 28 -> 33 при покрытии
0.631. У AppStarting складка так и не набирает 10 строк - остаётся `unmeasured`.

Негативный контроль в `tools/selftest.py` снова с запасом: подсаженный дефект
0.25 -> 1.50 логических единиц (перед починкой было 0.875 -> 1.25 при требовании
чистое + 0.5, то есть контроль падал).

Итог сборки: `quality : clean, 13 known debt, 1 unmeasured` (было 28 и 2).

## 36. Остриё UpArrow: одолжено у Arrow_Down (2026-08-20, сделано)

Пять рычагов со стороны рендера и перегенерация мастера по этому дефекту уже
записаны в тупики. Все они пытались **сдвинуть или сгладить** то, что есть.
Замер поперёк клина в двух логических единицах от острия (мастер 512, значения
яркости от -0.9 до +0.6 единицы поперёк хорды):

    Arrow       118  37  27 224 238 144  90
    Arrow_Down  123  88  72  96 225 160  59
    UpArrow     115  95 104  97  82 109 121

Складка есть у всех трёх. Освещённой грани рядом с ней у UpArrow **нет вообще** -
не тусклая, а отсутствует. Поэтому ничто мультипликативное её не вернёт: умножать
нечего. Это и объясняет, почему все прошлые попытки промахнулись.

**Что сделано.** Arrow_Down нарисован по тому же обводу, что и UpArrow, - значит
его затенение переносится без всякой привязки, пиксель в пиксель. В зоне
`_APEX_R0` = 2.0 логических единиц от острия (гаснет к `_APEX_R1` = 3.5) яркость
берётся донорская, но:

- широкий уровень донора делится обратно и подставляется свой (`_mblur` по
  `_APEX_LEVEL` = 1.0 единицы), так что зона не может выйти светлее или темнее,
  чем была. Одалживается форма затенения, а не экспозиция;
- цвет берётся из собственного направления цветности UpArrow, сглаженного по
  тому же радиусу. Первая версия масштабировала каждый пиксель отношением - и
  там, где у UpArrow складка (яркость 5), а у донора грань (200), два канала
  упирались в 255 раньше третьего: вдоль складки шла **пурпурная линия**. Видно
  на глаз, поймано на первом же рендере;
- вес - 1 внутри `_APEX_R0` и `_smoothstep` до нуля к `_APEX_R1`, ступеньки на
  границе нет.

| | было | стало | автор |
|---|---|---|---|
| `tip_contrast` | 0.048 | **0.071** | 0.085 |
| `tip_convergence` | 2.10 | 2.00 | - |
| `fold_jag` | 3.97 | 3.78 | - |
| `fold_wander` | 0.012 | 0.013 | - |
| `delta_e` | 3.769 | 3.784 | - |

Перебор доноров и радиусов:

| вариант | `tipcon` | `tipconv` | `wander` | `jag` | `dE` |
|---|---|---|---|---|---|
| база | 0.048 | 2.10 | 0.012 | 3.97 | 3.769 |
| Arrow_Down 2.0/3.5 | 0.071 | 2.00 | 0.013 | 3.78 | 3.784 |
| Arrow_Down 2.5/4.0 | 0.072 | 2.00 | 0.016 | 4.00 | 3.789 |
| Arrow_Down 3.0/5.0 | 0.073 | 2.00 | 0.016 | 4.00 | 3.811 |
| Arrow 2.5/4.0 | **0.150** | **5.00** | 0.016 | 4.00 | 3.788 |
| Arrow 3.0/5.0 | 0.150 | 5.00 | 0.015 | 4.00 | 3.803 |

Arrow как донор удваивает контраст острия против авторского - и отодвигает
начало самой складки на 5 логических единиц от вершины, то есть возвращает тот
же дефект с другой стороны. Взят Arrow_Down с узкой зоной: он единственный, кто
улучшает обе величины сразу и не выходит за диагностический допуск по кривизне.

Смотрел до чисел: 512 крупно на светлом и тёмном, потом весь курсор на 32/48/128
парами. Жёлтая грань доходит до вершины и сходит на конус, вместо плоской плиты с
обрывом. На 32 и 48 разница мелкая, но в ту же сторону.

Гейт: PASS, ничего не хуже базовой линии. Тупик «дальше только выше по цепочке,
вне `src/ai512`» этим закрыт - оказалось, что чинить нужно не свой мастер, а
взять чужой, у которого тот же обвод.

---

## 37. Уровень стекла: якорь держал не ту величину (2026-08-20, сделано)

Пункт 7 плана владельца: серое семейство (Cross, SizeAll, Size\*, IBeam) -
«самое видимое слабое место», разбирать как одну семью.

**Сначала замер, потом ручки.** Разложил недостачу массы по глубине от
обведённого края, наша доля от авторской на его же 32px:

| курсор | 0-0.5 ед. | 0.5-1 | 1-1.5 | 1.5-2.5 |
|---|---|---|---|---|
| Cross | 0.85 | 0.92 | 0.91 | 0.88 |
| IBeam | 0.79 | 0.92 | 0.90 | 0.89 |
| SizeNESW | 0.82 | 0.93 | 0.91 | 0.90 |
| Arrow | 1.01 | 0.95 | 0.90 | 0.98 |
| Help | 0.99 | 0.98 | 0.93 | 0.99 |

Серые недобирали ровно везде, Arrow и Help по краю были в норме. Разница между
ними одна: на 32px у Cross видимая зона это 148 антиалиасных пикселей из 148, у
IBeam 88 из 88, сплошных нет вообще, а у Help сплошных десятая часть.

**Промах.** `_up_alpha_native` держал нашу медиану на сетке 128 против авторской
медианы на 32. На 128 медиана считает стекло, на его 32 у тонкой формы -
антиалиасинг. Совпадали они идеально (Cross 0.541 против 0.543) и стоили
курсору плотности: на авторском разрешении **все 58 отгружаемых кадров** сидели
ниже него, от 1.4% (NO[8]) до 10.7% (IBeam). `build.check_metrics` молчал,
потому что делал ровно то же несовпадающее сравнение и показывал +0.0..+2.4%.

Недобор не был краевым артефактом: масса автора, лежащая выше потолка нашей
обводки, это 1.3% у Cross и 1.6% у SizeNS - остальные 10.8 пикселя из 11.9 у
Cross были достижимым стеклом внутри силуэта.

**Правка.** Обе стороны читаются на авторском разрешении, среднее вместо
медианы (медиана целиком краевой зоны это медиана антиалиасинга), и только там,
где силуэт реально несёт - `_LEVEL_REACH`. Последнее нашлось через сторож: без
него `Handwriting[7]` уходил на +8.1%, потому что пятнадцать его пикселей из
ста восьмидесяти четырёх лежат под покрытием 0.21 при авторской альфе 0.52, и
стекло доплачивало за то, чего не достаёт обводка. Недостающий вылет - дефект
трассировки, и чинить его надо в трассировке.

`build.check_metrics` переведён на ту же пару: наш кадр на его размере против
его кадра. Это не смягчение порога, а прекращение сравнения стекла с
антиалиасингом; порог остался 8%.

**Что получилось.** Якорь садится: на авторских 32 отклонение -0.0% (Help,
UpArrow, Arrow) до -3.5% (SizeAll) вместо прежних -8.3 и -10.7. Сторож: 0
предупреждений на 58 кадрах. `delta_e` лучше у **всех шестнадцати** (IBeam
5.389 -> 4.268 и уходит с долговой отметки 5.0, SizeNWSE 4.173 -> 2.750,
Handwriting 4.484 -> 3.553, Arrow 3.445 -> 2.925), `tip_contrast` лучше у всех
шестнадцати, `rim_layers` вырос везде. `density` и `scale_drift` не двинулись -
скаляр перед ресемплингом не трогает согласованность лестницы. Долг 13 -> 11.

**Чем заплачено.** `mirror_asym` у серых подрос на 1-2.5 (Cross 26.6 -> 28.8,
IBeam 20.7 -> 23.2): асимметрия меряется в уровнях, уровень поднялся, гейт
держит это в допуске относительно авторской собственной асимметрии.

**Складка AppStarting перестала читаться.** Она и раньше читалась только на 512
и только на кадрах 6 и 7, при `jag` 107.35 - трекер набирал там ровно 6 строк
при минимуме `_FOLD_ROWS = 6`, после подъёма набирает 5. Сама впадина не
изменилась: выраженность, замеренная напрямую мимо правил допуска трекера, до и
после равна 58.22 на кадре 6 и 58.87 на кадре 7, до второго знака. То есть
потеряно чтение, а не качество. Решение владельца - принять и переснять базовую
линию; складка ушла в список `unmeasured`, где она видна.

**Что осталось.** Недостача не плоская по глубине: наружная полуединица серых
всё ещё около 0.85 авторской. По правилу 7 это попиксельная правка в манере
`thin`, а не четвёртый якорь. И у IBeam 6.2% авторской массы лежит выше потолка
обводки (у Handwriting[7] столько же) - это трассировка, стеклом не берётся.

---

## 38. `_tip_glass`: пункт 8 замерен и закрыт без правки (2026-08-20)

Пункт 8 плана владельца - перенести `_tip_glass` на канонический уровень альфы
до лестницы размеров, по той же логике, по которой строился `_up_alpha_native`.

Замерял то, ради чего пункт написан: альфу отгружаемого кадра у самого острия
Arrow, по хорде A-J, в логических единицах, разброс по восьми ступеням от 32 до
512. Как отгружается сейчас - 50.9% от среднего в 0.15 единицы от вершины,
31.2% в 0.5, 17.4% в 1.0, 5.7% в 1.5.

Три прочтения, все хуже: пол, положенный один раз в нативном и растянутый
(59.5%), канонический опорный уровень при пер-размерном поле (52.9%), и перенос
стадии внутрь `_up_alpha` до удержания покрытия (50.6% при удвоенном
`scale_drift`, 0.0034 -> 0.0072). Числа и причины - в DEAD_ENDS.md.

Вывод, который стоит держать: пол, заполняющий резкую маску, обязан ложиться на
ту же сетку, на которой маска рисуется. `_tip_glass` уже стоит правильно.
Остаточный разброс у острия - это покрытие, одна логическая единица формы под
краем в один аппаратный пиксель, и альфой оно не берётся. Правки нет; изменён
только комментарий у раннего возврата в `_up_alpha`, о который спотыкается любая
стадия, дописанная в хвост.

---

## 39. NO и серое семейство: слой не тот (2026-08-21)

Тот же разбор, что закрыл Handwriting (маска против цвета), прогнан на NO и
IBeam. Ответ отрицательный: перенос материала им не поможет, потому что дефект
не в материале.

**NO.** IoU силуэта против автора 0.93..0.98 по всем одиннадцати кадрам, а на
общем внутреннем множестве альфа и яркость расходятся на уровень. Ошибка
`delta_e` живёт в двух местах, и оба не про высокие частоты:

| зона | delta_e (белый / серый / чёрный фон) |
|---|---|
| кромка (138 px из 207) | 7.44 / 5.74 / 7.18 |
| внутренность (69 px) | 4.69 / 4.76 / 5.31 |

У 25 худших пикселей наша альфа 103.9 против авторских 168.2 - это и есть
недостача маски на запрещающем кольце из раздела 28.1.

Внутренность разложена по осям Lab (серый фон, кадры 5-10): `dL` от +0.48 до
+1.35, `dH` 1.64..2.38, а `dC` **+3.69..+6.71**. То есть мы пересыщаем красное
кольцо, и это единственная заметная ось. Прямой подозреваемый - множитель 1.05
в якоре насыщенности (`frame_image`, `_sat_anchor(name, idx) * 1.05`). Замер:
NO[7] идёт 6.56 -> 6.14 при 1.02 и -> 5.93 при 1.00, то есть множитель даёт
примерно две трети превышения над порогом 5.0, а остаток - та же кромка.
Трогать его нельзя походя: константа глобальная, её видят все цветные курсоры,
нужен полный гейт.

**Серое семейство.** Внутренность у всех на авторском уровне, недостача сидит
ровно во внешней половине единицы. Отношение нашей альфы к авторской в этой
полосе (авторская опора - Lanczos, кадр 0):

| курсор | 32 px | 48 px |
|---|---|---|
| IBeam | 0.924 | 0.826 |
| Cross | 0.952 | 0.904 |
| SizeNS | 0.938 | 0.896 |
| SizeWE | 0.962 | 0.912 |
| SizeAll | 0.951 | 0.944 |
| Arrow (контроль) | 1.013 | 1.064 |

Внутренность у тех же курсоров 0.99..1.01. У Arrow, большой поверхности,
дефицита нет вовсе - значит это не общая стадия, а поведение тонкого элемента
на краю. Ровно адрес пункта 2.2 плана, и он про покрытие, а не про цвет.

**Вывод для архитектуры.** Разделение «яркость от AI, цвет от автора» - ответ
класса Handwriting. У NO ошибка цветовая, но не переносная: её делают якорь
насыщенности и тонкое кольцо. Отдельный chroma-базис не нужен.

---

## 40. Тонкие элементы: стекло, а не контур (2026-08-21)

Продолжение раздела 39. Недостача массы у серого семейства разложена и починена
со стороны альфы; `traced.json` не тронут.

**Разложение.** На 256, IBeam: уровень стекла внутри 0.988 авторского, площадь
0.913. То есть тонок не уровень, а покрытие. Но чинить покрытие покрытием не
выходит - замерены оба способа, оба только по альфе, ни один не двигает вершины:

| IBeam, сила 0.30 | масса | IoU на 32 |
|---|---|---|
| как есть | 0.902 | 0.885 |
| подъём стекла | 0.928 | 0.910 |
| подъём покрытия маски | 0.909 | 0.924 |

Поперёк стержня маска и так близка к единице, поднимать в ней нечего.

**Место решил замер, а не рассуждение.** Три постановки:

- после `_hold_coverage` - работает, но `scale_drift` Arrow 0.004 -> 0.044,
  Cross 0.013 -> 0.083: добавленную массу удержание не видит, и курсор начинает
  менять вес вместе с размером;
- перед `_hold_coverage` - удержание съедает подъём на мелких ступенях целиком,
  масса на 32 не двигается вовсе;
- **в нативной карте `_up_alpha_native`, один раз** - дальше только ресемплинг.
  Масса плоская по лестнице, `scale_drift` почти не шевелится (Arrow 0.004 ->
  0.006, Cross 0.013 -> 0.015).

Поле ширины (`_thin_weight`) тоже меряется один раз, на `_THIN_REF` = 256, и
ресемплится. Выведенное на своей ступени, оно на 32 считает тонким пол-курсора:
IBeam 0.948 против 0.928, Cross уходит за автора (1.023).

**Итог по массе** (ворота 1.6-3.2 логической единицы, `_THIN_LIFT` 0.30):

| курсор | 32 | 48 | 64 | 128 | 256 | было |
|---|---|---|---|---|---|---|
| IBeam | 0.928 | 0.927 | 0.928 | 0.930 | 0.929 | 0.901 |
| Cross | 0.990 | 0.990 | 0.990 | 0.993 | 0.992 | 0.954 |
| SizeNS | 0.996 | 0.996 | 0.996 | 0.999 | 0.997 | 0.967 |
| SizeWE | 0.997 | 0.997 | 0.997 | 1.001 | 0.999 | 0.969 |
| SizeAll | 0.971 | 0.974 | 0.977 | 0.986 | 0.986 | 0.957 |
| Arrow | 1.007 | 1.007 | 1.007 | 1.008 | 1.007 | 1.003 |
| Help | 1.004 | 1.004 | 1.004 | 1.008 | 1.005 | 0.998 |

Ворота по толщине отработали: Arrow и Help сдвинулись на 0.004-0.006.

**Гейт: долг 11 -> 10, `PASS`.** Ушла строка `SizeNS tip_contrast` - та, ради
которой планировалась правка бевела на семи курсорах. `tip_contrast` вырос
везде: SizeNS 0.039 -> 0.045 при цели 0.042, IBeam 0.075 -> 0.084, Cross 0.063
-> 0.075, SizeAll 0.050 -> 0.058, Arrow 0.124 -> 0.129, UpArrow 0.074 -> 0.077.
`Wait fold_gap` 1.000 -> 0.875. Подорожало `density_%` (SizeWE 1.51 -> 1.91,
SizeNS 0.94 -> 1.26, Cross 1.37 -> 1.52) - в допуске, но это цена, и следить за
ней надо.

Осталось: IBeam на 0.928, то есть семь процентов массы всё ещё нет. Сила 0.30
выбрана как та, при которой Arrow не двигается; поднимать её - отдельное
решение с полным гейтом.

*Поправка от 2026-08-21 (пункт 42): 0.928 мерилось против авторской суммы с
дымкой alpha <= 2, которой у IBeam 480 пикселей из 605. Честная масса 0.962, и
остаток геометрический, а не альфовый - поднимать силу незачем.*

## 41. `tip_contrast` мерил не то, что назывался (2026-08-21)

Аудит определения, без правки рендера. Метрика честно считала свою формулу, но
формула отвечает не на тот вопрос, который стоит в её имени.

**Что она считает.** `alpha × |lum - bg| / 255` в **одном** пикселе-экстремуме
внутри диска 1.5 логической единицы вокруг угла, худший фон, минимум по углам.
Переименована в `tip_extreme_contrast`, гейт за ней сохранён.

**Две поломки определения.**

Первая - экстремум решает один пиксель. У автора ровно один полностью
непрозрачный пиксель на кадр, координата (4,4), остриё, и так у всех трёх
стрелок (1 из 309, 99-й процентиль 237). У Cross и IBeam таких нет вообще (max
217 и 222), то есть для них метрика меряет что-то третье.

Вторая, важнее: произведение alpha на темноту нельзя разложить обратно, и
слишком тёмное остриё зарабатывает столько же, сколько верно непрозрачное.
Разложение на пределе (фон grey, эталон - `orig_frame`, то есть авторский кадр
через Lanczos, как его и видит гейт):

| курсор | src | score | alpha | \|dL\|/255 |
|---|---|---|---|---|
| UpArrow | наш | 0.077 | 0.820 | 0.094 |
| UpArrow | автор | 0.085 | 0.773 | 0.110 |
| Arrow | наш | 0.129 | 0.812 | 0.159 |
| Arrow | автор | 0.084 | 0.773 | 0.108 |
| Arrow_Down | наш | 0.099 | 0.820 | 0.120 |
| Arrow_Down | автор | 0.088 | 0.773 | 0.114 |

Alpha у нас **выше** авторской у всех трёх. Разводит их только светимость, и
Arrow проходит с запасом 1.54 потому, что его остриё на 47% темнее авторского.
Оптимизация под этот счёт означает "сделай остриё темнее", что неверно.

**`_tip_glass` тут ни при чём.** UpArrow не в `_FACET_CURSORS`, стадия двигает
на этом кадре 0 пикселей. Замерено, не выведено.

**Новая метрика: `tip_profile`.** `tip_rings` берёт тот же композитный контраст
кольцами по 0.25 единицы, `tip_profile` делит наши кольца на авторские и
возвращает худшее отношение по худшему углу. Кольцо сравнивается с кольцом на
том же расстоянии, поэтому тёмное тело больше не платит за бледное остриё.
Полоса 0.75-1.5: ближе - у эталона не рисунок, а звон ресемплера, дальше - диск
уходит с острия на тело.

Первые числа: UpArrow 0.42, Arrow 0.50, SizeAll 0.69, AppStarting 0.83,
SizeNWSE 0.89, остальные 0.92-1.53.

**И вот это разворачивает вывод по UpArrow.** Устойчивая метрика его не
оправдывает, а обвиняет сильнее одиночного пикселя, и дефект переживает три
проверки: против эталона NEAREST вместо LANCZOS отношение 0.50 вместо 0.42, то
есть размытие объясняет 0.08 из 0.58; кольцевые профили alpha и светимости
расходятся не в alpha (наша 0.81-0.83 против авторских 0.31-0.43 на u=0.25-1.25),
а в светимости (наша 104-124 против авторских 94-97); и на кропе 5x по серому
это видно глазом - у автора остриё закрыто тёмной шапкой антиалиаса шириной в
2-3 пикселя, у нас цвет доходит до самой точки, а контур остаётся волосяным.

То есть настоящий дефект называется иначе, чем всё, что пробовалось раньше:
**на острие контур не съедает поперечник целиком.** Там, где фигура тоньше
удвоенной ширины кромки, у автора кромка занимает всё сечение, а наш рендер
держит кромку постоянной ширины и заливает остаток телом. Это то же семейство,
что и подъём стекла на тонких элементах (пункт 40), но по контуру, а не по
альфе.

**Почему `tip_profile` не в гейте.** Он открыл бы сразу три отказа, а решение,
что из этого дефект, а что - честное расхождение резкого ремастера с размытым
оригиналом, относится к рисунку, а не к инструменту. Метрика включена в
ратчет: хуже станет - скажет, новых отказов не создаёт. Гейт остался на
`tip_extreme_contrast`; долг 10, `bad` 0, ровно как до правки.

## 42. IBeam: геометрия, и это талия (2026-08-21)

Вопрос стоял механически: где именно исчезают недостающие проценты массы.
Инструмент теперь в дереве - `tools/visual_audit.py --parts`.

**Сначала пришлось убрать помеху в самом эталоне.** У IBeam 480 из 605
засвеченных пикселей имеют alpha <= 2. Это 3.4% того, что наивная сумма считает
его массой, и почти вся его талия. Ни у кого больше такого нет: Handwriting и NO
дают 0.58%, остальные под 0.05%. Поэтому цифра 0.928 из пункта 40 - "семь
процентов массы нет" - неверна: она мерилась против суммы с этой дымкой.
Честная - **0.962**, порог `_HAZE = 2.0` в `visual_audit`.

**Разложение массы на площадь и уровень.** Суммарная альфа не различает нехватку
силуэта и нехватку стекла - они перемножаются. Силуэт меряется против авторского
покрытия (его альфа, делённая на его же внутренний уровень, с обрезкой), стекло
против его уровня.

| курсор | area | level | mass |
|---|---|---|---|
| IBeam | 1.235 | 0.796 | 0.962 |
| Cross | 1.326 | 0.755 | 0.992 |
| SizeNS | 1.373 | 0.707 | 0.998 |
| Arrow | 1.405 | 0.743 | 1.007 |

У IBeam самая низкая площадь в семье и самый **высокий** уровень стекла. То есть
подъём стекла из пункта 40 своё уже отдал, и со стороны альфы взять больше
нечего.

**Где именно.** По полосам (талия - строки 11-20):

| полоса | area | mass | missing | excess |
|---|---|---|---|---|
| верх | 1.284 | 0.990 | 0.041 | 0.027 |
| талия | 0.910 | 0.791 | 0.206 | 0.026 |
| низ | 1.246 | 0.965 | 0.069 | 0.031 |

Знаковая ошибка и решает вопрос. В талии недобор к перебору 8:1 - систематическая
худоба. Для сравнения талия SizeNS: 0.175 против 0.127, то есть 1.4:1 - там форма
смещена, а не тонка. Это ровно тот разделитель, который был нужен, и он работает.

**Ширины по строкам, 256, дымка убрана** (логические единицы поперечника):

| строка | автор | наш силуэт | разница |
|---|---|---|---|
| 11 | 1.541 | 1.689 | +0.148 |
| 12 | 0.944 | 0.722 | -0.221 |
| 13 | 0.222 | 0.030 | -0.192 |
| 14-17 | 0.000 | 0.000 | 0.000 |
| 18 | 0.176 | 0.030 | -0.146 |
| 19 | 0.916 | 0.706 | -0.210 |
| 20 | 1.518 | 1.662 | +0.145 |

Перемычки нет ни у нас, ни у автора: строки 14-17 пусты у обоих, засечки не
соединены. Дефект - четыре строки, 12-13 и 18-19, где наш контур на 0.15-0.22
единицы уже авторского покрытия. Во всех остальных строках он шире на
0.15-1.08.

**Вывод по дереву решений: geometry deficit.** Величина 0.15-0.22 логической
единицы - это в точности задокументированный завал обводки внутрь (0.14-0.24,
пункт 28.1), просто там, где фигура настолько узка, что он весит пропорционально
много. Рендер оправдан замером: уровень стекла у IBeam выше всех, площадь
силуэта задаёт `traced.json`, а он под запретом. Принято как локализованный
долг: четыре строки, названы поимённо.

Заодно карта площадей выделяет SizeAll: area 1.934 при семейных 1.24-1.46. Это
не дефект массы (mass 0.987), но обводка там раздута вдвое против остальных, и
если завал внутрь когда-нибудь будут править, начинать надо не с IBeam.

## 43. Клин: гипотеза о поперечнике проверена и не подтвердилась (2026-08-21)

Проверялась модель "тело исчезает там, где сечение уже удвоенной кромки":
`q = w/2r`, гашение тела на `q` от 1.0 до порога. Ничего не коммичено, стадия
вставлялась после `_facet_split`, последнего, кто трогает RGB, так что силуэт и
альфа не менялись по построению.

**Сначала пришлось починить свою же метрику.** `tip_profile` возвращал 0.00 на
48 и 64 у всех курсаров: кольцо шириной 0.25 единицы там меньше пикселя, а
пустое кольцо засчитывалось нулём контраста. Разрешение прибора выдавалось за
провал рендера. Теперь кольцо меньше `_TIP_RING_MIN = 8` пикселей не читается, и
угол молчит, если не разрешилась хотя бы половина полосы. `tip_profile`
говорит только на 128 и 256 - на 32 и 48, где смотрят глазами, у него нет
разрешения, и это надо помнить.

**Закон уже соблюдается обеими сторонами.** Сечения по нормали к хорде, 256:

| | автор | наш |
|---|---|---|
| ширина кромки r на широкой части (t = 0.25-0.45 хорды) | 1.32-1.34 у всех трёх | UpArrow 1.0, Arrow_Down 1.03, **Arrow 0.25** |
| сечение однородно (тела нет) до | w = 2.30 | w = 1.62 |
| тело появляется при | w = 3.20 | w = 2.12 |

То есть у обоих тело появляется примерно при `w = 2r` со своим r. Механизм не
нужно вводить - он уже есть. Отличается параметр: наша кромка уже авторской, а у
Arrow её почти нет (0.25 против 1.34) - её съедает `_facet_split`.

**A/B/C/D всё равно прогнан**, пороги 2.25r / 2.5r / 3r, r брался авторский:

| вариант | UpArrow tipprof | Arrow | Arrow_Down |
|---|---|---|---|
| A текущий | 0.42 | 0.50 | 0.95 |
| B 2.25r | 0.30 | 0.24 | 0.41 |
| C 2.5r | 0.29 | 0.24 | 0.42 |
| D 3r | 0.30 | 0.26 | 0.42 |

`tip_extreme_contrast` падает у всех: 0.077 -> 0.039, 0.129 -> 0.087, 0.099 ->
0.039. Глазами на 32 и 48 остриё тупеет и бледнеет. Причина видна в сечениях:
цвет, на который гасится тело, - наша же кромка, а она около острия имеет
светимость 121-128 при сером фоне 128. Гасить тело на неё значит стирать
остриё. Тупик, записан.

**Настоящий параметр - уровень, а не занятость.** Зона острия (диск 1.5
единицы), альфа-взвешенная светимость:

| курсор | 32 | 48 | 256 | автор |
|---|---|---|---|---|
| UpArrow | 120 | 127 | 128 | 100 |
| Arrow | 123 | 129 | 125 | 100 |
| Arrow_Down | 115 | 121 | 121 | 99 |

Разрыв 18-30 уровней на всех ступенях. Альфа при этом совпадает: на 32 наша
0.55-0.57 против авторских 0.54-0.55. Наше остриё стоит ровно на сером фоне и
на нём исчезает.

**Вариант E: якорь уровня.** Светимость полосы вдоль хорды (3 единицы, спад
1.5) сдвигается на разницу средних с авторской, только luma, хрома не трогается,
альфа и силуэт не трогаются. Прототип в `scratchpad/level.py`, не в дереве.

| курсор | tipextr A -> E | tipprof A -> E | dE | drift |
|---|---|---|---|---|
| UpArrow | 0.077 -> **0.149** | 0.42 -> 0.93 | 3.32 -> 3.25 | без изменений |
| Arrow | 0.129 -> 0.212 | 0.50 -> 1.71 | 2.96 -> 2.92 | без изменений |
| Arrow_Down | 0.099 -> 0.156 | 0.95 -> 1.66 | 2.79 -> 2.75 | без изменений |
| Hand | 0.196 -> 0.244 | 1.53 -> 1.71 | 2.42 -> 2.41 | без изменений |

Перелёта нет: E приводит зону острия к 100-120 при авторских 98-102. Глазами на
32 остриё становится определённым тёмным концом вместо растворяющегося в фоне,
форма не меняется.

**E ломает два анимированных клина** и потому ограничен четырьмя: AppStarting
`tip_extreme_contrast` 0.138 -> 0.066 при авторских 0.069, то есть новый отказ;
Wait 0.130 -> 0.083. Это ровно та тёмная кромка, которую запрещено трогать, так
что ограничение совпадает с запретом. У Hand якорь берётся по его собственному
кадру, а не по нулевому: `src=0` даёт тот же tipextr 0.244, но dE 2.87 против
2.41.

**Полный гейт с E на четырёх: `bad 0, debt 9`.** Ушла строка `UpArrow
tip_extreme_contrast` - последняя позиция долга, не считая складок и delta_e у
NO. Не коммичено: это правка рендера по механизму, которого владелец ещё не
видел.

## 44. Клин, вариант G: низкочастотный перенос света у острия (2026-08-21)

E сдвигал полосу одним числом. G берёт у автора только размытую светимость,
наша детализация едет сверху: `delta = blur(author_luma) - blur(our_luma)`,
взвешенное альфой размытие (иначе пустой фон тянет край вниз), `result +=
delta * tip_mask`. Альфа, хрома и силуэт не трогаются, как и в E. Только четыре
статических клина, AppStarting и Wait вне механизма. Прототип
`scratchpad/lowfreq.py`, не в дереве.

**`tip_profile` не различает E и G.** У всех трёх сигм и у E он даёт ровно
одно и то же: UpArrow 0.93, Arrow 1.71, Arrow_Down 1.66, Hand 1.71. Это
максимум по кольцу: как только хоть один пиксель кольца обошёл автора,
отношение насыщается. Критерий "Arrow_Down должен остаться около 1" этой
метрикой проверить нельзя, и 1.71 у Arrow - не перетемнение, а насыщение.

**Разделяет их пространственная ошибка** - светимость сечения вдоль хорды
против авторской, средняя со знаком по t = 0.5..7:

| курсор | A | E | G1 (s=1.0) | G3 (s=2.5) |
|---|---|---|---|---|
| UpArrow | +14.2 | **+4.1** | +4.3 | +7.5 |
| Arrow | +20.6 | +8.8 | **+8.5** | +10.8 |
| Arrow_Down | +11.9 | +3.9 | **+3.7** | +6.1 |
| Hand | +11.9 | +5.2 | **+4.8** | +5.9 |

**Перетемнения нет ни у E, ни у G.** Ошибка остаётся положительной на всех t у
всех четырёх: обе поправки не доводят до автора, а не переходят его. Максимум
|ошибки| у E и G1 совпадает до уровня (10 / 21 / 10 / 13). Опасение, ради
которого G строился, замером не подтвердилось.

**И E ничего не уплощает.** Высокочастотная энергия в полосе (luma минус свой
низкочастотный, RMS по альфе): A 20.2/24.2/23.9/26.6, E 20.8/24.5/24.4/27.7,
G1 21.0/21.1/24.2/27.4, автор 13.0/13.3/12.2/13.3. E - постоянное смещение
внутри полосы, оно по построению не может тронуть детализацию; единственное
заметное движение - у G1 на Arrow, 24.2 -> 21.1, то есть G слегка сглаживает
там, где E не трогает.

**Итог.** G1 выигрывает у E на 0.2-0.4 уровня на трёх курсарах из четырёх и
проигрывает 0.2 на UpArrow. Механизмы эквивалентны в пределах замера; G
принципиальнее (автор задаёт только низкую частоту), E проще. Ни один не
коммичен. Разница между ними меньше, чем остаток ошибки (+4..+9 уровней),
который не закрывает ни тот, ни другой - вот куда стоит смотреть дальше, а не
в выбор между E и G.

Глазами на 32: G1 даёт определённое тёмное остриё без затупления, форма та же;
G3 заметно бледнее G1, как и по числам.

## 45. Амплитуда поправки уровня: H и J, и почему остаётся E (2026-08-21)

Лестница из трёх ступеней, прогнана целиком. Ничего не коммичено, все прототипы
в `scratchpad/`.

**H - E с решённым ослаблением маски.** E добавляет `(target - ours) * w`, а
взвешенное среднее этого равно не `(target - ours)`, а этому же, умноженному на
`sum(w*w)/sum(w)`. H решает `mean_w(rgb + d*w) = target` относительно d:
`d = (target - ours) * sum(w)/sum(w*w)`. Не подобранный коэффициент, а обратная
величина известного ослабления.

Работает как обещано: средняя ошибка по полосе +4.4/+6.6/+4.1/+3.5 (E) ->
+0.8/+1.8/+1.0/+0.2 (H). И одновременно подтверждает второй сценарий: среднее
ушло в ноль, а профиль - нет. Arrow под H даёт буквально `-8 / +3 / +10` по t.
Минимум ошибки у H -5.8/-8.4/-3.6/-6.4 против E -2.5/-2.1/-0.7/-1.6. Одного
постоянного сдвига не хватает - доказано, а не предположено.

**J - поправка `ΔL(t)` вдоль хорды**, по станции на 0.25 единицы, сглаженная по
t, с решением ослабления на каждой станции. Первая версия (I) делила разрыв на
затухающее ослабление у края полосы и получала траншею -57 уровня ровно там, где
маска умирает; исправлено требованием `att >= 0.9` - поправка считается только
там, где маска в полную силу, дальше держится последнее значение и гаснет
вместе с маской.

Профиль после этого действительно выпрямляется:

| курсор | A | E | J |
|---|---|---|---|
| UpArrow | +10.4 (-3.7..+30.7) | +2.7 (-3.7..+10.2) | **-0.9 (-6.8..+4.4)** |
| Arrow | +17.1 (+4.1..+34.6) | +8.1 (-2.1..+20.7) | **+0.9 (-5.2..+6.4)** |
| Arrow_Down | +9.0 (-1.4..+24.1) | +2.8 (-1.5..+10.5) | **-0.5 (-4.3..+2.5)** |
| Hand | +10.2 (+2.2..+20.7) | +5.1 (-1.6..+13.0) | **+0.4 (-5.7..+4.6)** |

**И ломает складку.** Полный гейт под J: `bad 4` - UpArrow `fold_gap` 0.125 ->
0.750, Hand `fold_wander` 0.207 -> 0.568 и `fold_gap` 2.500 -> 2.562,
Arrow_Down `fold_jag` 55.8 -> 89.4.

Сглаживание не спасает: при сигме по t 1.5, 4.0 и 8.0 числа складки
**совпадают до знака**. Значит дело не в резкости `ΔL(t)`, а в самом факте, что
уровень меняется вдоль хорды. Складка идёт вдоль той же оси, трекер читает
профиль яркости поперёк излома, и наклон уровня вдоль него он видит как
смещение излома. Постоянный сдвиг такого сделать не может по построению - у E
числа складки совпадают с A до третьего знака.

**Итог лестницы.** Плоский профиль и целая складка на этом клине несовместимы,
пока поправка живёт в яркости. E остаётся единственным вариантом с `bad 0`, и
его остаток +2.7..+8.1 - это цена за то, что складку он не трогает. Полный гейт
под E: `bad 0, debt 9`, уходит `UpArrow tip_extreme_contrast` (0.077 -> 0.149
при авторских 0.085).

Следующая ветка, если остаток всё же надо закрыть, - не яркость: поправка
должна идти там же, где живёт складка, то есть в `_tip_relight`, у которого уже
есть своя модель жёлоба вдоль хорды. Тогда уровень и излом правятся одним
объектом, и трекеру нечего рассогласовывать.
